# shotlist-studio · 设计文档（初稿 v0.1）

> 状态：v0.1.1 · 已冻结（2026-09-19）→ 动工中
> 日期：2026-09-19（v0.1.1：补 §11 代码架构）· 作者：Hermes
> 定位：新库第一份文件。全部讨论定案见「附录A」——如定案与正文冲突，以你的最新反馈为准。

## 0. 这份文档怎么用

- 审阅重点：§3 数据模型与字段字典、§5 提示词系统、§6 审计规则集、§11 代码架构、§12 开放项。
- 你在文档上直接标注删改；改完这稿冻结，按 §13 里程碑动工。

## 1. 背景与目标

- 老库 storyboard-shotlist（v1）已封存（GitHub @ 6578d37）。重做原因：使用笨重、编辑体验差、改点啥都得通过 AI、后端绑死飞书。
- 新目标一句话：**一个常驻局域网的「分镜工作台」软件**——打开就是所有工程；全部字段直改；AI 是可选动作；提示词拼积木式写作。
- 范围（不变）：价值弧线 → 节拍 → 分镜 → 提示词（审计贯穿）。
- 非目标：与 storyboard-designer 的接缝（暂不做）；跨设备/远程（将来 Tailscale 再补）；多人协作。

## 2. 总体形态与技术底座

- 形态：本地 web 应用，**局域网桌面浏览器**访问；单用户。
- 技术：Python 3 **stdlib 零依赖**（http.server + sqlite3 + urllib）+ 原生 HTML/CSS/JS（无构建链）。与 storyboard-designer 同范式。
- 服务：systemd 常驻自启（从根上治"服务掉了"）；端口暂定 **8094**（避开 8089/8091/8093），动工时核对。
- 数据：**一个 SQLite 文件装全部工程**；项目内 `data/` 目录（DB + 每日快照 + 导出件），不入 git。
- 数据安全：每日自动快照 +「锁定本场」快照 + 全库 JSON 导出；DB 单文件，好备份。
- 视觉：暗色、克制、高信息密度（延续旧库审美）；精修留 M5。

## 3. 数据层设计（SQLite 单库）

### 3.1 表结构草案

```
films          工程（片）：id / title / archived / meta / 时间戳
scenes         场：id / film_id / position / 场次号 / 场景名 / 场景价值 / 起点极 /
               终点极 / 翻转 / 视点角色 / 时值 / 台本原文 / 锁定标记 / 时间戳
beats          节拍：id / scene_id / position / 节拍序号 / 名称 / 外界动作 / 人物反应 /
               类型🟡⚪🔴 / 闭环 / 说明 / 节奏段落 / 节奏描述 / 情绪温度 /
               预估总镜头数 / 节奏密度 / 节拍动作 / 节拍属性 / 视点角色
shots          镜头：id / scene_id / beat_id / position / 镜号 / 运镜 / 空间关系 / 景别 /
               焦段 / 景深 / 机位 / 动作调度 / 台词 / 时长(秒) / 音频 / 导演备注 / 镜头职能 /
               视点角色 / prompt_group_id / 时间戳
prompt_groups  提示词组：id / scene_id / position / 提示词文本 / 时间戳
blocks         热盒积木块：id / category_id / 文本 / position / 常用置顶
block_categories 块分类：id / name / position
audit_rules    审计规则：id / 类型(程序|LLM) / 标题 / 规则参数 / enabled
audit_issues   审计问题：id / scene_id / 载体(场|节拍|镜头|接缝) / target_id /
               rule_id / 描述 / 状态(未处理|已修|豁免) / 豁免理由 / 时间戳
history        痕迹：id / 时间 / 场 / 实体 / 字段 / 旧值 / 新值 / 来源(手改|AI|系统)
snapshots      快照：id / 范围(片|场) / kind(每日|锁定|手动) / 标签 / 存放路径 / 时间
settings       设置：key / value（含 AI provider/model/key——存本地，不入 git）
```

### 3.2 字段字典（飞书两表全量平移）

**分析表（18 字段）→ 落 scenes / beats：**

| 飞书字段 | 落点 | 备注 |
|:--|:--|:--|
| 场次 | scenes | 场次号 + 场景名 |
| 场景价值 / 起点极 / 终点极 / 翻转 | scenes | 价值追踪四件套 |
| 视点角色 | scenes + beats | 两表都有，各存各的 |
| 节拍序号 / 节拍名称 / 外界动作 / 人物反应 / 类型 / 闭环 / 说明 | beats | 节拍分析主体 |
| 节奏段落 / 节奏描述 / 情绪温度 / 节奏密度 / 预估总镜头数 | beats | 节奏设计字段 |

**分镜表（20 字段）→ 落 shots / beats / prompt_groups：**

| 飞书字段 | 落点 | 备注 |
|:--|:--|:--|
| 镜号 / 运镜 / 空间关系 / 景别 / 焦段 / 景深 / 机位 / 动作调度 / 台词 / 时长(秒) / 音频 / 导演备注 / 视点角色 | shots | 逐镜字段原样平移 |
| beat序号 / beat标题 / beat类型 / 节拍动作 | beats | **去冗余**：v1 每镜重复存一份，v2 集中在节拍表，显示时合成（避免两处不一致）。如果你要照旧每镜存，说一声 |
| 节拍属性 | shots.shot_fn | **勘误（09-19 迁移实测）**：此列逐镜不同（建立/触发/动作镜/反应镜/插入＝镜头职能），非节拍级字段，落镜头行 |
| 提示词 | prompt_groups | 见 §3.4 / §5 |
| 场次 | （关联键） | |

**新增字段：** shots.position（行序，所有排序的唯一依据）、shots.prompt_group_id、scenes.script（台本）、history/snapshots/audit/settings 见上。

### 3.3 镜号规则（已定案）

- 镜号是**标签**，不是排序依据；排序一律按行 position。
- **平时不动号**：拖动不改号；插入新镜默认给**子号**（如 17A）。
- 工具栏「**整理镜号**」按钮：点击 → 按当前顺序整场顺排（01、02、03…）；旧号写入痕迹可查（"17A → 17"）；**不点击，永不自动重排**。
- 提示词组 / 审计 / 痕迹全部按内部 ID 绑定，改号不影响任何关联。

### 3.4 提示词组（一等公民）

- 一个提示词可覆盖多个镜头：文本存在 `prompt_groups` 上，镜头通过 `prompt_group_id` 入组。
- 单镜提示词 = 一镜组（隐式，界面不强调"组"）。
- 操作：选相邻镜 →「并为一组」；组可「拆开」；镜头拖进/拖出组自动处理。
- 生成时默认按「**机位连续段**」分组（老规矩保留）。

## 4. 前端结构（信息架构与交互）

### 4.1 三层结构

**工程库（首页）→ 片级页 → 场级页（主场）**

- 工程库：工程卡（片名 / 镜数场数 / 四段进度 / 最近编辑）；新建（空白或复制）、打开、重命名、归档、删除（双重确认 + 快照兜底）。
- 片级页：价值弧线清单（每场一行，全字段行内编辑）+ 场次徽章（**分析 / 分镜 / 审计 / 提示词**）+ 新建场次。
- 场级页（主场）布局：

```
┌ 工具栏：整理镜号 ｜ 锁定本场 ｜ 筛选 ｜ 列设置 ｜ 问题清单 ┐
├ 挂件带：景别序列条 ▸ 节奏视窗（可折叠）                   │
├───────────────────────────────┬──────────────┤
│ 镜头表：节拍分组行 + 镜头行           │ 右缘抽屉：     │
│ · 全字段直改 · 行拖动 · 多选批量        │  剧本 / 热盒   │
│ · 审计小灯钉在行侧                  │ （按时刻切换）  │
└───────────────────────────────┴──────────────┘
```

- 挂件：景别序列条（渐紧渐松一眼见、连续同景别现形）；节奏视窗（时长条 × 情绪温度曲线）。

### 4.2 编辑手感（对齐飞书多维表格）

- 所有字段**点击即编**；类型化控件（文本 / 数字 / 单选带色标——节拍类型 🟡⚪🔴）；**自动保存**，无保存键。
- Tab / 方向键走格；多选（Shift / 框选）→ 表底「选区条」批量改。
- 全栈 Ctrl+Z 撤销；列宽 / 列显隐可调（本地记住）。
- 筛选 / 搜索最小集：镜号跳转、关键字、快捷筛选（未写提示词 / 戏点 / 某节拍）。
- 快捷键 + **Cmd+K 命令面板**（跳场次 / 镜号 / 命令）。

### 4.3 拖动

- 拖动把手在镜号格；行拖 = 换序、跨节拍搬运；节拍头拖 = 连镜头整体重排；多选拖动 = 批量搬家。
- 拖动实时预览，松手落库；**拖动不改镜号**（见 §3.3）。

### 4.4 其它

-「锁定本场」：场次版本快照 + 锁定标记（锁定≠禁止编辑，是留底 + 标记）。
- 痕迹与历史：全操作留痕（旧值 → 新值，含 AI 操作），可检索回看，跨会话有效。
- 导出：静态 HTML 页（自带样式，可分享 / 存档）+ A4 打印版（分镜表）。
- 剧本抽屉：右侧滑出本场台本，边看边改；台本随工程存（导入一次）。

## 5. 提示词系统（拼积木）

### 5.1 范式与操作

- 写作范式固定 → 要素做成**预设块（积木）**；写作 = 在**热盒**里点点点拼装 + 少量自由文本。
- 编辑区形态：**chip 条**——每个块一片；点热盒 = 插入；拖 chip = 调序；点 chip = 替换 / 编辑；随时补自由文本。
- **插入即固化**（已定案）：块是"文字模板匣子"，插进去就跟库无关；不做活引用 / 同步。
- 智能块：块文本可含 `{时长}` `{景别}` 等占位，**插入瞬间**代入当前镜头值，此后各自独立。
- 选中提示词里一段文字 →「存为块」入库；块库随时增 / 改 / 删 / 排序；分类 + 搜索 + 常用置顶 + **套装**（几块打包一次插）。
- 逐镜拼装：拼完一镜 → 回车跳下一镜，流水线连拼；多选可批量同拼。

### 5.2 多镜合并

- 与 §3.4 一套：组提示词在表里以合并单元呈现；生成 / 拼装默认按「机位连续段」分组。
- 组内每镜的数据（机位 / 运镜 / 景别…）都是生成时可引用的原料。
- 合并时两组都已有文本 → 默认保留前者，另一个进痕迹可找回。

### 5.3 块库种子（我来炼，你删改）

- 来源：老库 s010 的 34 条实拍提示词 + M4 模板（五段结构 / 机位编码写法 / 主光方位模板 / 风格块全文）。
- 分类草案：**机位与拍摄方式 / 镜头参数（焦段·景深） / 运动（运镜·画面动态） / 灯光（主光方位） / 人物与表演 / 空间与环境 / 风格（全局风格块） / 音频**。
- 交付时附一份「种子块库清单」单独过目。
- 说明：v1 的"共享声明继承 / 断链回溯"是生成时代的管理手法——v2 里风格块等固定件进块库；人物 / 场景声明可用"拷贝自上组"辅助，不做自动继承链路。

## 6. 审计系统（注解层）

- 把老模块 3 从"报告文档"改造为**长在数据上的注解**：问题钉到具体位置（镜头 / 节拍 / 场次头 / 行接缝），点灯跳转、当场改、重检消灯。
- 三态：**未处理 / 已修 / 豁免**（豁免要写一句理由，留痕）；侧边「问题清单」汇总。
- 规则集全量平移老 SKILL.md 模块 3 十项（轴线 / 闭环 / 戏点密度 / 戏点特写 / 节奏曲线 / 空间一致性 / 机位一致性 / 景深 / 声音 / 动作具象化），逐条转成注解规则。
- 两类检查：**程序直接算**（标注完整性、计数类）+ **LLM 配方跑**（轴线 / 机位策略 / 具象化等语义判断）。

## 7. AI 集成（双层）

- **软件内 AI 动作**（不碰 Hermes 即可用）：后端直连 LLM，设置面板配 provider / model / key（默认现有 DeepSeek）。
  - 就地 ✨：字段级改写 / 具象化 / 强化——出 diff，接受或拒绝。
  - 选区指挥条：选中若干行 → 一句话命令 → 操作预览 → 应用。
  - 生成提示词（草稿档）：按组给初稿，你在热盒上精修。
  - 跑审计：§6 中语义检查那一半。
  - 一键草稿：新场次从台本出草稿（分析 / 镜头）；质量按"草稿档"预期，不满意丢 Hermes。
- **对话（Hermes）**：大活 / 深加工——整场精修、大改版、工具本身的开发维护。
- 配方：每个动作背后是"创作规则配方"（从老 skill 提炼的动作级模板），放项目内 `recipes/`，可改可迭代。

## 8. 迁移方案（电玩城）

1. **全量导出留档**：飞书两表导出 JSON 存档（纯保险）。
2. 迁入新库：s010（34 镜 + 提示词）、s020（3 节拍 + 12 镜骨架）、全片价值弧线。
3. 核对：计数核对 + 抽场对照（s010 对照现有 HTML）。
4. 老库 HTML 只读留存（read/done/），不再维护。

## 9. 继承清单（老库 → 新库）

- **平移**：references 方法论文件（cinematic-grammar / character-business / ai-prompt-pitfalls / production-notes / m4-prompt-template 等，16 个全量盘点分类）；SKILL.md 规则（约束条件 / 迭代纪律 / 审核口径）；电玩城数据。
- **改造**：M4 模板 → 块库种子 + 生成配方；三视图 → 单表结构。
- **弃置**：飞书后端全套（config / API 写法 / 多表技巧）、build_html.py / shotlist_server.py、rowspan 施工法、CURRENT_STATE.md 机制（由 AGENTS.md 四字段取代）。

## 10. Skill 拆分、仓库与开发流程

- **两个 Skill**（2026-09-22 定案落地：创作 `shotlist-studio` / 开发 `shotlist-studio-dev`；互不共享文件）：
  - 创作：方法论 + 工具使用指南（不含代码）。
  - 开发：架构约定 + 管线 + 验证协议 + 坑（不含创作方法）。
  - 互不共享文件（独立模块化）。
- 仓库：`read/Projects/shotlist-studio/`；GitHub `shaobaojia/shotlist-studio`（公开——2026-09-22 建库首推）。结构：`server/ web/ scripts/ recipes/ references/ data/(gitignore) DESIGN.md AGENTS.md README.md`（目录细则与代码分层见 §11）。
- 流程：设计先行（本稿）→ 冻结 → 动工；一条命令管线（run / migrate / test / export）；验证先行（三层，见下）；AGENTS.md 四字段（刚做完 / 正在做 / 下一步 / 坑）；需求池文件；里程碑推 GitHub。
- **验证三层**（2026-09-19 实测）：① 容器无头回归——逻辑层硬断言（unittest / DOM stub），改完即跑；② Hermes 自带浏览器实测——真浏览器直连应用：功能操作 + DOM 断言 + 截图视觉核对；③ 预览窗 / 本机过目——桌面预览窗（Agent 驱动、实时围观、可高亮标注）+ 用户本机浏览器拍板；家 PC WebBridge 退为备用通道（仅在需操作家 PC 真实浏览器时启用）。
- 纪律：凭证红线（一切 key / secret 不入库）；改动大批量模板走脚本替换，不裸 patch。

## 11. 代码架构（防屎山工程结构）

> 结论先行：老库代码不重构、不搬运（已封存，只当教训样本）；「扎实、优雅」靠新库结构从第一天保证。

### 11.1 老库审计（为什么会「不好改」）

1. **前端不是工程，是生成物**：`build_html.py`（412 行）用 Python 拼字符串生成整页；全部 CSS/JS 内嵌在一个 59KB、1183 行的单模板里。改界面 = 改生成器 + 重新生成，裸 patch 大文件必碎（「改 HTML 模板禁裸 patch」就是老库坑条之一）。
2. **CSS 由 Python 现场生成**：每个场次一条 `#scene-xxx:checked~…` 规则——样式和「场次数量」耦合，动数据就得动样式生成代码。
3. **硬编码遍地**：输出文件名、标题「测试列表」、场景名表、飞书表 ID、端口、目录全写死——老库著名坑「重建后刷不出来」即由此来。
4. **无分层**：后端只有「静态服务 + 裸代理」；前端是 39KB 全局函数的单块 JS（无模块、无状态层）；凭证链挂在浏览器端设置面板。
5. 结论：这类结构补丁不出优雅——**重做是对的选择**；老库的遗产 = 方法论 + 一份现成的坑清单。

### 11.2 结构总原则（总纲：高内聚、低耦合）

**总纲 = 高内聚、低耦合**（下面六条都是它的落地条款）——**高内聚**：一个模块只干一件事（表就是表、热盒就是热盒、db 就是 db），自包含、可单测；**低耦合**：模块之间只通过窄接口说话——api.js 是前端唯一网络出口、db.py 是唯一数据入口、前后端只认 JSON 契约；依赖方向单向，禁反向依赖与循环依赖。

1. **页面是写出来的，不是生成出来的**：静态 `index.html` + CSS 文件 + ES Modules；数据只走 JSON API。
2. **三层单向**：路由（薄）→ 领域 → 数据；前端 = 网络层 → 状态层 → 视图层。
3. **单点定义**：字段字典只有一份（后端 schema 驱动），驱动表格列 / 控件 / 校验 / 导出，前后端不各写一份。
4. **配置表驱动**：字段类型、热盒块分类、审计规则 = 配置数据 + 通用引擎（不写死 if/else 分支）。
5. **文件纪律**：单文件 ≈ ≤400 行，超了拆；全小写英文命名；代码目录禁中文文件名；大文件改动走脚本，禁裸 patch。
6. **对标 designer 终局形态**（它已趟过一遍屎山治理：CSS 拆 4 份 / queue 拆 3 份 / 编辑模板合一 / 89 项回归网）——v2 直接吃终局，不重走弯路。

### 11.3 后端目录（Python stdlib，包化）

```
server/
  app.py         # 入口：HTTP 服务 + 路由表 + 静态托管（薄层，只做分发）
  api/           # 按资源分文件：films / scenes / shots / prompts / audit / ai / export
  core/
    paths.py     # 路径推导唯一实现（防硬编码）
    db.py        # 连接、schema、迁移（user_version）、快照
    models.py    # 行 ↔ 对象转换（轻量）
    ops.py       # 领域操作：排序 / 整理镜号 / 编组 / 痕迹（纯函数优先，可单测）
    audit.py     # 审计引擎（程序类规则）
    ai.py        # LLM 客户端 + 配方（软件内 AI 动作）
    export.py    # 导出：静态页 / A4 打印版 / JSON 包
  schema.sql
  tests/         # 无头回归（unittest，一条命令跑全量）
```

### 11.4 前端目录（原生 ES Modules，零构建）

```
web/
  index.html     # 唯一页面（手工维护，非生成物）
  css/           # 变量 / 基础 / 组件样式，分文件（级联顺序标注）
  js/
    api.js       # 唯一碰网络的地方（fetch 封装 + 错误处理）
    store.js     # 客户端状态 + 乐观更新 + 撤销栈
    table/       # 表格：渲染、就地编辑、拖动、选区、镜号整理
    prompts/     # 提示词：chip 拼装、热盒、组（多镜合并）
    panels/      # 剧本抽屉、问题清单、挂件（序列条 / 节奏视窗）
    main.js      # 装配
```

根目录其余：`scripts/`（迁移 / 回归 / 工具）、`recipes/`（AI 动作配方）、`references/`（方法论）、`data/`（运行数据，不入 git）。

### 11.5 数据流

```
浏览器（web/） ⇄ JSON API ⇄ app.py → api/* → core/ops → core/db.py → SQLite
                                     core/ai.py ⇄ LLM API（设置面板配置）
```

### 11.6 验收（防屎山硬门槛）

- 全库无硬编码路径 / 标题 / 清单：一切从 DB 与配置来——老库同款坑从根上不存在。
- 路径推导只有一处实现（paths 工具层），禁散落手拼；输出转义 / URL 编码统一走工具层。
- 每次交付前跑全量无头回归；沿用 designer 开发铁律：测试确认 → 修改 → 验证 → 全过才交付。
- 任何「先凑合」必须进需求池条目，禁静默积累。
- 结构体检：改一个需求通常只动 1~2 个文件——超过 2~3 个 = 耦合警报，当场拆；不为设计模式而设计，标准的唯一目的是「改一处不牵十处」。

## 12. 开放项（等你）

1. §3 字段字典语义审（重点：去冗余设计、"场 / 节拍 / 镜头"三层拆分）。
2. §5.3 块库种子——我出清单后你删改。
3. §6 审计规则集表述审。
4. ~~§10 两个 Skill 命名~~ ✅ 已定（`shotlist-studio` / `shotlist-studio-dev`）。
5. §13 里程碑切分。
6. 如有：给 1-2 条你最近新写法的提示词样例，用于校准块库（非必须）。
7. 镜号子号格式（17A 式）如无异议按此执行。

## 13. 里程碑（草稿）

- **M1 底座**：DB + 迁移脚本 + 服务骨架 + 只读镜头表。验收：浏览器里看到电玩城全量。
- **M2 编辑手感**：全字段直改 + 拖动 + 整理镜号 + 撤销 + 快照 + 筛选。验收：手动完成一次真实修改流。
- **M3 提示词**：组 + 拼积木 + 热盒 + 种子块库 + 逐镜拼装。验收：给 s020 拼出成套提示词 / s010 抽 5 镜对照老提示词。
- **M4 审计 + AI**：注解层 + 就地 ✨ + 指挥条 + 设置面板。验收：跑一次 s010 审计、改一个字段。
- **M5 打磨**：挂件 / 抽屉 / 导出 / 命令面板 / 性能。验收：冻结 v1.0。

## 附录A：讨论定案汇总（2026-09-18 ~ 09-19）

1. 功能范围：价值弧线 → 节拍 → 分镜 → 提示词；v1/v2 合并，只有一个分镜。
2. 提示词 = 拼积木（热盒 + 预设块 + CRUD）；**插入即固化，无活引用**。
3. 多镜合并（提示词组）一等公民；生成按机位连续段分组；不可丢。
4. 飞书砍；单 SQLite 库装全部工程。
5. 数据模型照搬飞书两表字段。
6. 全字段手动编辑；镜头 / 节拍可拖动。
7. 形态：本地 web、局域网桌面、systemd 常驻、stdlib 零依赖。
8. 前端只留单表结构；卡片视图 / 时间线视图砍。
9. 场次徽章：分析 / 分镜 / 审计 / 提示词。
10. AI 双层；"skill 嵌入软件"= 配方内嵌；整场流程先草稿档。
11. 审计 = 注解层（三态）。
12. 与 designer 接缝不做。
13. 双 Skill 拆分（创作 / 开发）。
14. 迁移：电玩城全量。
15. 库名：shotlist-studio。
16. 保留：痕迹与历史、导出（静态 + A4）、景别序列条、节奏视窗、快照锁定、命令面板、筛选、逐镜拼装、剧本抽屉、每日快照。
17. 砍：卡片 / 时间线视图、放映模式、台词条、v1/v2 双轨、活引用、口述改、便签格、回手掏摘要、重掷、跨视图联动、双击镜号复制。
18. 镜号：整理按钮制——点了才重排；平时不改号；插入给子号；旧号进痕迹。

## 附录B：坐标（供迁移 / 开发）

- 老库：`/opt/data/skills/scriptwriting/storyboard-shotlist/`（GitHub shaobaojia/storyboard-shotlist @ 6578d37）；触发词按老 SKILL 原样平移。
- 飞书：base `OwBSbEQS5aY9HksVVBYcYUnVnlg` · 分镜表 `tbl2gBoybDUPpPz2` · 分析表 `tbl9L7UG4kJ2nuSr`（凭证在 feishu_config.json，不入库）。
- 电玩城：Vault/Projects/2026_04_14项目：电玩城的大小孩/（银幕剧本 V01 / 处理台本 V02）；read/done/ 旧 HTML 存档。
- 本项目：`read/Projects/shotlist-studio/`（本文档所在）。
