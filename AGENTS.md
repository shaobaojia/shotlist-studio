# AGENTS.md — shotlist-studio

分镜工作台 v2 的交接备忘录（给 AI 协作者：Hermes / CLI 子代理）。
规矩：开工前先读本文件；收尾时更新下面四字段。状态权威 = 本文件 + git log。

## 刚做完
- 2026-09-19 仓库骨架建立；设计稿 v0.1.1 冻结（DESIGN.md）。
- 2026-09-19 飞书两表全量导出留档：分镜表 46 条（s010×34 + s020×12）、分析表 3 条（s020 节拍），在 data/archive/feishu-2026-09-19/（不入 git）。
- 2026-09-19 **M1 迁移完成**：电玩城全量入库 data/studio.db —— 8 场 / 10 节拍 / 46 镜 / 14 提示词组（↑引用已链式解析并组）；脚本 scripts/migrate_feishu.py（--reset 重跑）。
- 2026-09-19 schema 修订：shots.shot_fn（镜头职能，原「节拍属性」列；实测逐镜字段，DESIGN §3.2 已勘误）。
- 2026-09-19 **M1 服务上线**：python3 server/app.py（:8094）—— 三层（app.py 路由 / api/handlers 接口 / core/db + core/fields 字段字典）；前端 web/ = 全片价值弧线页 + 场级只读镜头表（行点击展开详情 + 提示词组）；curl 冒烟 + 真机浏览器实测通过。

- 2026-09-19 **M1.5 视图层移植（老库成果继承）**：列序/列宽/聚合列照搬老库最终方案（# → 运镜 → 空间关系 → 摄影机 → 机位 → 动作调度 → 台词 → 时长 → 音频 → 导演备注 → 提示词）；摄影机两行式（★行 + 焦段 lens-tech 暗排 + ↓叠层）、机位五色缩写（如 🔵环）、音频/空间关系多行格式、EDL 暗配色（:root 原样取老库）；新增：自动换行开关（默认开，localStorage 记忆）、显示提示词开关、表头点击排序（升/降/复位，视图级、激活自动平铺 + 「节拍」列）；统计行=规模+总时长+机位图例；页面标题低调化「测试列表」（老库同款、不显示片名）。渲染器 web/js/cells.js（纯 DOM 构建，数据不进 innerHTML）。
- 2026-09-19 **摄影机列收敛（用户拍板）**：列 = 景别 + 焦段；景深实测用处不大、移出列（拆分正则识别后缀后丢弃，叠层等其余格式不变）。数据层与详情区不动；改动单点：web/js/cells.js 一处正则。

- 2026-09-19 **M2 批次 1（编辑底座）完成**：①全字段点击即编——镜头 15 字段（表格列 + 详情区含隐藏字段）、节拍（名称 / 类型单选 / 节拍动作）、场景（场景名 / 价值 / 翻转 / 视点）皆可直改；单行 INPUT / 多行 TEXTAREA / 类型 SELECT（⚪填充 🔴戏点 🟡空间建立）；自动保存无保存键（Enter / Ctrl+Enter / 失焦保存，Esc 取消）；乐观更新、失败回滚 + toast；②「整理镜号」按钮（点了才排，旧号入痕迹）；③Ctrl+Z 撤销栈（字段编辑 + 整理镜号，走同一写路径）；④痕迹 history 表全量记录 + /api/history 接口；⑤每日快照（当日首个写操作前自动整库拷贝 data/snapshots/daily/）；⑥写白名单单点 core/ops.py write_keys（id/position/prompt 不可写）；⑦单测 server/tests/test_ops.py 8 项全过；真机 E2E（备注 / 隐藏字段 / 节拍类型 编辑→保存→撤销 + 整理按钮，零 JS 报错）通过；⑧前端模块化：新增 edit.js（编辑引擎）/ table.js（表格，自 scene.js 拆出）/ ui.js+toast。

## 正在做
- M2 进行中：批次 1 = 编辑底座 ✅（本次提交）；批次 2 待做 = 行拖动 / 筛选 / 锁定本场 / 多选批量 / Tab 走格。

## 下一步
- M2 剩余：①行拖动（DESIGN §4.3：镜号格把手、换序 / 跨节拍搬运、节拍头整体拖、多选拖）②筛选 / 搜索最小集（镜号跳转 / 关键字 / 未写提示词）③锁定本场 + 快照按钮 ④多选 / 框选 + 表底选区条批量改 ⑤Tab / 方向键走格 ⑥列宽 / 列显隐本地记忆 ⑦痕迹回看 UI。
- 视图层余项（未做，等点名）：列宽拖拽（老库 bindColResize，跨节拍表同列同步）；提示词面板（属 M3 热盒范畴）。
- 补齐一条命令管线（run / migrate / test / export）
- 后续里程碑见 DESIGN §13（块库/热盒 M3、审计 M4）

## 坑

- 老库教训清单见 DESIGN §11.1（生成器拼 HTML、硬编码、无分层——勿重蹈）。
- 本仓库在 NAS 共享卷：容器内新建文件后注意权限（保持 a+rwX 双向可写）。
- 凭证红线：一切 key / secret 不入库；飞书凭证在库外（feishu_config.json）。
- 大文件改动走脚本替换，禁裸 patch（老库坑条）。
- 老库页面服务 :8089 保留运行（迁移对照用）。
- 老库表视图规格出处（移植对照用）：storyboard-shotlist 仓库 references/frontend-notes.md、templates/feishu-backed.html、read/done/*_feishu_backed.html；渲染器 web/js/cells.js。
- 数据注意（M2 编辑前先定规则）：『景别』复合串内含焦段/景深信息（列上已不再显示景深，但数据里仍在），与独立 focal/dof 字段是两份来源；全量比对 46 镜仅 2 例不一致（均 s010：镜02 串内 50mm vs 独立 35mm；镜22 串内 35mm vs 独立 50mm；疑似当年单边手改）。两份均为原始数据、未动；M2 需先定同步/分工规则。
- 迁移实测：①「节拍属性」= 逐镜字段（已落 shots.shot_fn）；② 镜24 beat标题（被确认屈从）与序号（6）不一致（已按序号归组、标题取多数，留档）；③ s010 节拍明细（外界动作/反应/闭环/说明）已从 s010_第一场_分析.md 补录。
- s030–s080 现状：价值弧线已入库、镜头为空（待创作填入）。
- 编辑控件多行判定：按「类型集合 + 键集合」分开判（blocking 的 type 是 text、不是独立类型；单一集合混判会踩坑——动作调度开出单行框）。
- 撤销走同一写路径 → history 同时有「改」与「撤销改」两条记录，属预期（痕迹 = 全操作留痕）；撤销栈是客户端内存栈，刷新页面即清空（数据层不依赖它）。
- 写白名单与类型判定以 fields.py 单点为准；前端多行判定（table.js MULTILINE_TYPES / KEYS）与之对齐，改字段时两处一起看。
- 每日快照落 data/snapshots/daily/studio-YYYYMMDD.db（幂等；snapshots 表暂未启用，以文件为准）。
- 服务运行：python3 server/app.py（默认 :8094，SHOTLIST_PORT 可覆盖）；**本容器无 fuser/ss**——杀进程用 /proc 扫描匹配 server/app.py 后 SIGTERM；改 py 必须杀净旧进程再重起（否则旧进程占端口、新进程静默退出、旧代码继续服务——09-19 已踩坑）；当前由 Hermes 托管进程跑，容器重启后需重起。
