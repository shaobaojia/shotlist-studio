# AGENTS.md — shotlist-studio

分镜工作台 v2 的交接备忘录（给 AI 协作者：Hermes / CLI 子代理）。
规矩：开工前先读本文件；收尾时更新下面四字段。状态权威 = 本文件 + git log。仓库：GitHub `shaobaojia/shotlist-studio`（公开）。

## 刚做完
- 2026-09-24 **第 2 波批1 · 刀6 落账（S2-L1 key 单源迁移）**：UNIQUE 索引双防线（schema + 迁移）+ seed 按 key + 运行链全 key 化（_RULE_BY_TITLE 除名）+ 顺修 _TITLE_KEY NameError；**270/270**；真库迁移实测 + 负向咬合。
- 2026-09-24 **第 2 波批1 · 刀5 落账（S4 余项包）**：NEW core/backup.py（dump_db+prune+dump_to 自 scripts 迁入）+ 虚拟列驱动（fields.virtual → export/ops 判据）+ NEW tests/test_snapshot.py；咬合（撤脱敏→咬住）；**266/266**；CLI 端到端 + 真服务热重载。
- 2026-09-24 **第 2 波批1 · 刀4 落账**：路由安全网与前后端对账（S1-L3）——NEW tests/test_routes.py 12 例（api.js 34 路径 ↔ 38 路由双向对账 + 逐条契约 + 分发形状）；负向咬合（注入破坏→3 例咬住）；260/260。
- 2026-09-24 **第 2 波批1 · 刀3b 落账（刀3 api 收口全录）**：ai/draft/recipes/export/prompts 五文件收编（err/read/write 信封、AiError/RecipeError/SceneNotFound 转译统一）；248/248、沙盘 13 例。
- 2026-09-24 **第 2 波批1 · 刀3a 落账**：api 连接信封与错误收口（S1/S3-L2 前半）——conn_ro/conn_rw 连接信封 + guard 四单点（err/read/write/scene_or_404）；handlers 14 全收编 + audit 全收编（body or {} 除名）。248/248、沙盘真机 11 例。
- 2026-09-24 **第 2 波批1 · 刀1/刀2 落账**：①拆包双子——core/ops.py（854）→ ops 包 6 模块、core/prompts.py（486）→ prompts 包 3 模块（全量重导出、调用面零改动）；②db 边界重构——core/paths.py 单点、快照族迁 core/snapshot.py（断 db↔ops 循环）、open_ro/open_rw 显式入口（统一 FileNotFoundError）、调用面全迁 + 测试双入口同拦；structure 再切 scenes（全 ≤400）。回归 248/248；台账 = AUDIT-M8/12-第二波台账.md。
- 2026-09-24 **AUDIT-M8 修复侧全清**：第 0 刀 P0（11 条）+ 模块轮九站 F1–F5/S1–S4「一轮全清 B+P+W」全落地——密钥残留全数处置（活库/快照/导出件/副本）、测试引导单点（conftest/_boot）、写盘单点 fsutil、导出域口径（FORMATS/消毒/半角/兜底）、serve 退避+轮转、seed 管线登记、一次性工具归档除名（scripts/archive/）。回归 248/248；修复台账 = AUDIT-M8/11-模块轮台账.md（九册）。
- 2026-09-23 **M7b 挂件带微调**（用户）：视口 hover 提示改自定义 300ms 延时（条条+情绪点，取代原生 title）+ 面板圆角归零（通栏直角）。
- 2026-09-23 **v1.0 冻结**（批7 收口：性能实测 / 一条命令管线 / 全库 JSON 导出——详见「正在做」区末条）。
- 2026-09-19 仓库骨架建立；设计稿 v0.1.1 冻结（DESIGN.md）。
- 2026-09-19 飞书两表全量导出留档：分镜表 46 条（s010×34 + s020×12）、分析表 3 条（s020 节拍），在 data/archive/feishu-2026-09-19/（不入 git）。
- 2026-09-19 **M1 迁移完成**：电玩城全量入库 data/studio.db —— 8 场 / 10 节拍 / 46 镜 / 14 提示词组（↑引用已链式解析并组）；脚本 scripts/migrate_feishu.py（--reset 重跑）。
- 2026-09-19 schema 修订：shots.shot_fn（镜头职能，原「节拍属性」列；实测逐镜字段，DESIGN §3.2 已勘误）。
- 2026-09-19 **M1 服务上线**：python3 server/app.py（:8094）—— 三层（app.py 路由 / api/handlers 接口 / core/db + core/fields 字段字典）；前端 web/ = 全片价值弧线页 + 场级只读镜头表（行点击展开详情 + 提示词组）；curl 冒烟 + 真机浏览器实测通过。

- 2026-09-19 **M1.5 视图层移植（老库成果继承）**：列序/列宽/聚合列照搬老库最终方案（# → 运镜 → 空间关系 → 摄影机 → 机位 → 动作调度 → 台词 → 时长 → 音频 → 导演备注 → 提示词）；摄影机两行式（★行 + 焦段 lens-tech 暗排 + ↓叠层）、机位五色缩写（如 🔵环）、音频/空间关系多行格式、EDL 暗配色（:root 原样取老库）；新增：自动换行开关（默认开，localStorage 记忆）、显示提示词开关、表头点击排序（升/降/复位，视图级、激活自动平铺 + 「节拍」列）；统计行=规模+总时长+机位图例；页面标题低调化「测试列表」（老库同款、不显示片名）。渲染器 web/js/cells.js（纯 DOM 构建，数据不进 innerHTML）。
- 2026-09-19 **摄影机列收敛（用户拍板）**：列 = 景别 + 焦段；景深实测用处不大、移出列（拆分正则识别后缀后丢弃，叠层等其余格式不变）。数据层与详情区不动；改动单点：web/js/cells.js 一处正则。

- 2026-09-19 **M2 批次 1（编辑底座）完成**：①全字段点击即编——镜头 15 字段（表格列 + 详情区含隐藏字段）、节拍（名称 / 类型单选 / 节拍动作）、场景（场景名 / 价值 / 翻转 / 视点）皆可直改；单行 INPUT / 多行 TEXTAREA / 类型 SELECT（⚪填充 🔴戏点 🟡空间建立）；自动保存无保存键（Enter / Ctrl+Enter / 失焦保存，Esc 取消）；乐观更新、失败回滚 + toast；②「整理镜号」按钮（点了才排，旧号入痕迹）；③Ctrl+Z 撤销栈（字段编辑 + 整理镜号，走同一写路径）；④痕迹 history 表全量记录 + /api/history 接口；⑤每日快照（当日首个写操作前自动整库拷贝 data/snapshots/daily/）；⑥写白名单单点 core/ops.py write_keys（id/position/prompt 不可写）；⑦单测 server/tests/test_ops.py 8 项全过；真机 E2E（备注 / 隐藏字段 / 节拍类型 编辑→保存→撤销 + 整理按钮，零 JS 报错）通过；⑧前端模块化：新增 edit.js（编辑引擎）/ table.js（表格，自 scene.js 拆出）/ ui.js+toast。

- 2026-09-19 **M2-1a 编辑控件无缝化（体感修正）**：点击即编控件从「描边盒子」改为无缝编辑——无框 / 无聚焦圈 / 无拉伸手柄 / 无占位文字，自动长高贴合内容、与展示态同格对齐（字体/行高/内边距一致），编辑态改用格子自身 1px 强调描边 + 微亮底提示；单选（节拍类型）保留细灰框。改点仅两处：web/css/app.css（.cell-editor 块）、web/js/edit.js（fitEditor 自动长高）。教训：老库的 textarea 方案（玫红描边 + resize 手柄）不适用于「点击即编」的飞书体感——**设计目标 §4.2 是飞书，照抄老库控件样式即返工**。

- 2026-09-19 **M2 批次 2（拖动 + 筛选 + 类型化控件）完成**：①行拖动（DnD 事件委托 drag.js）：把手=镜号格、行换序/跨节拍搬运（落点指示线）、节拍头拖=整节拍重排；服务端 ops.move_shot / move_beat（全场 position 致密重排、镜号不动、痕迹记 drag）；松手落库、Ctrl+Z 可撤；筛选/排序激活时禁拖。②筛选最小集：关键字（全字段、防抖）/ 未写提示词 / 镜号跳转（Enter、闪烁定位）/ 计数 + 清除；DOM 级过滤不重建表格；独立模块 filter.js。③类型化控件：摄影机列=复合控件（景别1/景别2/焦段）；预设单点 fields.py options（景别 7 档、焦段 5 档）；机位=五色单选；详情区职能/焦段同步。④旧格式归一化：串内嵌『焦段·景深』编辑时迁入独立字段、串重写纯景别。⑤单测 15/15；E2E 全链路 + 撤销还原、零 JS 报错。
- 2026-09-19 **M2 批次 3（浮动菜单 + 剪贴板 + 反哺修复）**：①修复·卷展失灵——筛选用输入框清空后详情行残留 display:none（行恢复了详情没恢复；点「清除筛选」按钮反而不显，因走重建）；②修复·焦距消失——摄影机串归一化后单元格显示未回落 focal 字段（值存了看不见）；cells.js cellContent 增 extra 回落（串内嵌优先、无则字段）；③单选与复合控件全面去原生 select——自绘浮动菜单 menu.js（一次点击直达列表、✓ 当前值、↑↓/Enter/Esc、点外关闭、菜单内点击不算点外）；④右键菜单 cellmenu.js：复制本格 / 复制整行（TSV）/ 粘贴（Excel 式块粘贴：1×1 单格、N×M 向右下铺、上限 400 格）/ 清空本格；⑤剪贴板 clipboard.js——复制走 execCommand 兜底（局域 http 无 clipboard API）；粘贴走「Ctrl+V 待命」（无法程序化读剪贴板）；⑥拖动把手改真实元素 .drag-dots（td 不再 draggable，消除误拖），setDragImage 用整行；⑦编辑器开启贴合格子高度（不推挤下方行、少一次强制回流）。E2E 全绿（菜单单选/复合三槽/块粘贴+撤销/键盘选单/拖动+撤销/卷展回归/终检数据完璧），零 JS 报错。

- 2026-09-19 **M2 批次 3b（拖动修正 + 行首手柄 + 卷展预热）**：①拖动「原地不动」——落回自己行/自己节拍 = 客户端直接 no-op（修前落自身会走『插到节拍末尾』分支=乱跑）；悬停自身行也不显示落点线。②拖动把手移到行首：.drag-dots 进 toggle 格左侧（悬停显示，靶区含 padding≈10×18），镜号格不再带把手。③卷展冷成本：帧级实测稳态开/关=17ms/帧（60fps 无掉帧）；首开一次性冷成本偶现 ~50ms → 加 scheduleWarm 空闲预热（paintScene 后 700ms 起分片强制布局折叠详情行、同任务内还原、不闪）。

- 2026-09-19 **M2 批次 4（选区系统 + 走格 + 批量设值）**：①框选——单元格拖拽出格即成选区（同格内仍是点击即编，互不干扰）；Shift 点选/拖拽扩选；Esc/点外部取消；**选区限定单张表内**。②表底选区条（selbar.js）：计数（单列「已选 N 镜 · 列名」/ 多列「N 格 · N 镜 · N 列」）+ 批量设值（12 字段；枚举走浮动菜单拾取即套用、文本输入回车套用）+ 复制（TSV）+ 清空 + 关闭。③键盘走格：方向键走格（多选先塌缩再移动）、Shift+方向键扩选、Enter 开编、Tab 右移；编辑中 Tab/Shift+Tab=保存并走格、Enter=保存并下移（单行）。④右键菜单选区内变体：复制选区 / 清空选区 / 粘贴（从选区左上起）。⑤服务端 /api/batch：逐项白名单+痕迹、单连接一次 commit、上限 400；单测 17/17。⑥修 applyFieldValue 越界写（见坑）。E2E 全绿（框选/设值只动选中行/撤销还原/键盘/走格/右键/回归三件套），数据完璧。

- 2026-09-19 **服务常驻化（掉线根治）**：:8094 与老库 :8089 两个服务在会话收拢时被 Hermes 连带清掉（同刻 SIGTERM、termination_source=agent_close——托管后台进程随会话/应用动作被回收，已实证；主机与容器均未重启）；根治 = 脱会话守护：仓库新增 scripts/serve.sh；老库同款包装器 /opt/data/bin/serve-shotlist-archive.sh（不改 skill 内容）。两者实证 PPID=1 脱离 + 健康通过；浏览器端到端复核（9 场签 / 34 行 / 7 分区）。

- 2026-09-19 **M2 批次 5（创建行副本）**：右键菜单（两变体）新增「创建行副本」——源行后插入（同场同节拍）；13 个内容列全拷（不含 id/position/shot_no/提示词归属）；镜号自动取字母后缀（05→05A、17A→17B；数字基 + 首个空闲字母，A-Z 后 AA…）；位置致密后移；留 create 痕迹；新行闪烁定位；**Ctrl+Z 撤销 = 删除副本行**（delete 痕迹）。服务端新增 /api/duplicate + /api/delete（delete 暂仅供撤销用、表限 shots）。单测 22/22；E2E 全绿（菜单两态/字段全拷/编号递增/连撤/空间建立区/数据完璧/零 JS 报错）。提示词归属不带（新镜头待 M3 纳入规划；如需随拷，改 ops.duplicate_shot 的 INSERT 列即可）。

- 2026-09-19 **M2 批次 6（三层结构操作全集）**：①镜头行——右键「上方插入空行 / 下方插入空行」（中插=前邻字母后缀，追尾=数字顺延）、「删除本行」；节拍区尾部「＋ 镜头」（空节拍/空场的创作入口）；多选删行（选区条「删除行（N）」+ 选区内右键）——一次删 N 行、单步 Ctrl+Z 整组回插。②节拍——页尾「＋ 添加节拍」（空场先搭骨架再填镜）；节拍头右键「创建节拍副本（含 N 镜）」（深拷：节拍+其下镜头，编号 1→1A、镜头字母后缀）与「删除节拍」（镜头落「未归节拍」可拖回）。③场次——导航末尾「＋」加场（sNN0 步进）；标签右键「创建场次副本」（整场深拷：场+节拍+镜头+提示词组、外键重连、紧跟源场）与「删除场次（含 N 镜）」（整场级联）；标签拖动排序；场号点击即改（唯一性双护栏）。④服务端——/api/create（空镜头定位插入/空节拍/空场）、/api/duplicate 与 /api/delete 泛化三层、/api/restore（撤销回插 shots/beat/scene）；**还原优先复用原 id**（修撤销链悬空真 bug）；scenes 写白名单补 scene_no、路由放开任意场号。⑤配套——删除不弹窗（即时删 + Ctrl+Z 完整还原 + 痕迹 + 每日快照三层兜底）；结构操作不重排其他镜号；新行/节拍/场闪烁定位；筛选排序下锚定行操作可用。⑥单测 36/36（夹具补 FK pragma 对齐生产）；E2E 三批全绿（插入序列 02→02A→02B、副本→删→撤×2、框选删行、节拍深拷/未归节拍/还原、空场三连（＋场→＋节拍→＋镜头→'01'）、整场深拷、改名→撤销、拖动排序→撤销、删场→还原；终检 8 场 / s010 35镜7节拍14组 / s020 12镜3节拍 / 用户 04A 完好 / 零残留）。

- 2026-09-19 **M2 批次 7（锁定本场 + 列宽列显隐 + 痕迹回看 + 节拍概述空态）**：①锁定本场（工具栏）——留底+标记：拍场次版本快照（JSON 落盘 data/snapshots/scenes/ + snapshots 表 kind=locked）+ 场头「🔒 已锁定」徽标；锁定 ≠ 禁止编辑（设计稿 §128）；再点=解锁（只清标记、快照文件保留）；接口 /api/lock + ops.lock_scene。②列宽拖拽——表头右缘手柄（36–620px 夹取、双击复位、按列 key 跨节拍表实时同步），落 localStorage。③列设置——原「显示提示词」开关升级为全列显隐统一菜单（✓ 勾选 + 全部显示），旧 prompt 开关自动迁移。④痕迹回看面板——工具栏「痕迹」：当前场最近 200 条（/api/history），时间/实体/字段/旧值→新值（中文映射），可搜索、刷新、操作后自动跟新。⑤节拍概述空态入口——beat_action 空时也渲染概述位（修「新建节拍无处添加概述」：原条件渲染只在非空时出块）；【＋ 填写节拍概述（外界动作 / 人物反应 / 闭环…）】暗淡提示点击即编。⑥单测 39/39；E2E 两批全绿（列宽+100 跨 7 表同步/双击复位/隐藏列重载持久/痕迹 127 条可搜/临时场九连：建场→空态→建节拍→填概述→锁定→痕迹 4 条→解锁→删场；零 JS 报错；零残留——临时场快照文件/快照行/history 全清）。

- 2026-09-19 **视觉定案 + 落地 v1「案头 · A卡条 × 昼案」**（设计哲学专项 · 仅场级页 · 纯视觉）：过程＝三方向皮肤稿 → 用户隐喻「桌面 / 卡纸 / 纸条」→ v1 纸条稿（功能红线校准，撤回）→ v2 表格原样稿 → v3 双浓度（A 卡条 / B 卡片）→ **定案 A×昼案**。交付＝①《视觉宣言.md》（三条军规：功能红线 / 美观为功能服务 / 逐条核对；含色板与组件速查）；② design/案头-A卡条-昼案.html（定稿参考，真实 s010 数据）；③ web/css/app.css 全量重排「案头」皮肤（桌面 #d2c7ad + 节拍卡纸 #f6f0e1 + 镜头卡条 #fdfaf2；border-collapse:separate + border-spacing 行卡 + tr 细影）。核验＝选择器结构与交互逻辑零改动；计算样式 / 编辑开合（可信 Esc）/ 菜单开合 / 零 JS 报错 / 列对齐 / 全片页一致，逐条过。

- 2026-09-19 **视觉落地 v2（三个体验件：吸顶 / 阶梯 / 平铺开关）**（场级页 + 全片页场头同规则）：①**场次信息吸顶**——.scene-head 变「场签」纸带（磨砂纸带 + 圆角 + 发丝边），sticky top: var(--topbar-h)；顶栏高度由 main.js syncTopbarVar() 实测回写（ResizeObserver 自适应换行/缩放），别写死像素。②**三级亮度阶梯**（由外向内一层比一层亮）：顶栏「桌沿」#c4b693（实底）→ 场签纸带 rgba(226,216,193,.92) → 节拍卡纸 #f6f0e1 → 镜头卡条 #fffef9（--card；hover #fffffb、展开 #ffffff）。③**「分组/平铺」正式开关**——工具条 seg 分段控件；平铺＝一张平表 + 节拍列（等价于点 # 表头那个效果，但独立于排序）；平铺下拖动禁用、加行/加节拍入口隐藏、表头排序照常；选择入 prefs.viewMode 本地记忆（默认 group）。核验＝E2E（吸顶咬合 46=46 无缝、平铺 34 行 1 表 0 分区、平铺内排序、刷新记忆、编辑回归、零 JS 报错）+ 截图视觉过审（含全片页零副作用）。

- 2026-09-19 **视觉落地 v3（吸顶区扩容 + 平铺表头冻结）**：①吸顶区由「场签」扩为 **.scene-freeze 整块**（场次信息＋工具条）：一组 sticky top: var(--topbar-h)（磨砂底 + 圆角 + overflow:hidden），底边界＝工具条自身下划线；scene.js 把 sceneHead+viewTools 装进 freeze，syncFreezeH() 实测回写 --freeze-h（resize 跟随）。②**平铺表头冻结**——平铺表的 .table-wrap 加 .holdhead：overflow: visible（关键根因：wrap 的 overflow-x:auto 使其成为滚动容器，sticky 被关在内层、表头从来没真正冻住过）；th top = calc(--topbar-h + --freeze-h)、z-index 5；分组视图表头照旧（只按点名）。代价：平铺表放弃内部横滚（窄屏整页横滚，桌面无感）。核验＝E2E（冻结缝 diff=0 严丝合缝、滚到 2200 仍 0、分组行为不变、痕迹面板/空场正常、零 JS 报错）+ 截图/裁切视觉过审（工具排下划线为界成立）。

- 2026-09-19 **视觉落地 v4（场次信息＝通栏布）**：吸顶区从「卡片」改为「盖在桌面上的布」——与桌面（视口）同宽、左右通栏、无圆角无边框。层级逻辑升级为四层四材质：**桌沿[顶栏] → 布[场次信息＋工具] → 卡纸[节拍] → 卡条[镜头]**（卡片是桌面上的"物"，布是盖面的"层"——场次信息不属于内容物）。实现：.scene-freeze { margin-left: -22px（＝#view 侧 padding）; width: 100vw } + html,body { overflow-x: clip }（防 100vw 按滚动条宽产生横滚）；内衬 22px 与内容栏左对齐。核验＝E2E（left=0 / width=1440=视口 / radius=0 / 零横滚 / 吸顶与平铺冻结 diff 仍 0 / 编辑回归 / 零报错）+ 视觉过审（「盖在桌面上的一块布」，摆脱卡片感）。


- 2026-09-19 **视觉落地 v4b（左对齐修正）**：布内文字基准线＝卡纸药丸首字 **45px**（4 变体裁切 + PNG 像素扫 + DOM Range 三方交叉）；工具排按钮按「字」对齐 → 内衬 36、字落 47（与标题同线）。
- 2026-09-19 **M3-1（提示词域 + 块库底座，服务端）**：①新域模块 core/prompts.py（提示词组 set_text / merge / detach / split / restore；块库块与分类全 CRUD + 排序 + 置顶；cat_delete 块落未分类不连带删）+ api/prompts.py（/api/prompt/<action>、/api/blocks）。②并组口径：保留首个来源组文本、其余来源组文本入痕迹（可找回）、空组即删；拆组/独立＝文本留首镜（原组全拆出时随首个被拆镜）；**restore 复用原 id**（撤销完整还原不悬空）。③单镜特例：未组镜头 merge＝建自己的组（「写入自动建组」支点）。④单测 server/tests/test_prompts.py 20/20。
- 2026-09-19 **M3-2（前端拼装台接线）**：①三新模块——web/js/hotbox.js（拼装台：提示词格 → 就地编辑面＝正文＋按钮行＋热盒条）/ blocks.js（热盒：搜索·分类筛选·chips·套件暂存·右键块菜单）/ blockman.js（块库管理器面板）。②接线：table.js（提示词格→toggleComposer；详情 prompt-box→buildPromptBox）、scene.js（initHotbox）、selbar.js（＋「并为一组」「独立成组」）、api.js、history.js（prompt_groups 中文映射）。③交互口径：点格开编 · 失焦/点外＝收起＋保存 · Esc＝不保存 · Ctrl+Enter＝**保存并下一镜**（跳过同组）· 「存为块」＝选中文字入库 · 组头「本镜独立/整组拆开」。④保存失败＝编辑面留着不丢字；皮肤 m3.css 追加（案头色系）。
- 2026-09-19 **M3-3（种子块库 + 全面自测 + 验收演示）**：①scripts/seed_blocks.py——s010 十四组提示词 + M4 模板提炼 **7 类 25 块**（骨架/人物/场景道具/镜头段句式/主光/风格固定件/运镜），风格块=505 字固定件；幂等 --reset；清单＝仓库根 种子块库.md。②全操作模拟自测（s010 只读 + 临时自测场全写入），发现并修复两处真 bug（单镜建组后取空组 return＝文本未保存；保存失败静默吞错→编辑面留住）。③验收：自测场用积木拼两组成套提示词（1089/948 字，含「拷上组」），**场已清零残留**（场次/孤儿/痕迹全清、s010 完好）。④工具排「使用哲学」三提案（库外 :8095，待选）。

- 2026-09-20 **工具排 B 案落地（用户选型）**：①**场务抽屉**——整理镜号 / 锁定本场 / 自动换行 / 列设置 / 痕迹 收进吸右「场务 ⋯」菜单（.vt-drawer margin-left:auto；锁定项动态标签（已锁定→「解锁本场（当前已锁定）」）；自动换行带 ✓ 当前态；列设置二级菜单锚回抽屉按钮）。②**查找区对调**——筛选 → 镜号跳转 → 未写提示词 → 计数 → 清除。③核验：临时场 s120 实测（整理 01/03/05→01/02/03、锁定→留底+徽标→动态解锁）+ s010 只读回归 + 全项菜单开合（列设置/痕迹/换行），零 JS 报错；临时场连带快照文件/快照行/痕迹全清（零残留）。④提案页已标「已采纳」（库外 :8095）。

- 2026-09-20 **修复拼装台光标锁死（用户实测反馈，当日修复）**：根因＝只读视图的 body click 监听在编辑态仍挂着——编辑面内任何点击（点 textarea 定位光标 / 热盒条 / 搜索框）冒泡 → activateBox → focusEditor → setSelectionRange(文末)，光标被强拽文末且页面跳底。修法＝①点击目标在 .hotbox 内则直接跳过（hotbox.js renderBox 守卫）；②focusEditor 仅当 textarea 未聚焦时才把光标放末尾。复验（CDP 可信事件）：点首行光标=2、点中段=119、打字落光标处、块插入落光标处、拖选 13 字、搜索框焦点正常、无跳动；零 JS 报错。另：测试期间两处误存（组1 ← 274 号痕迹旧值、组3 ← 288 号痕迹旧值）已还原至各自原文（1316 字；备份 cache/m3/group{1,3}-*old.txt；痕迹 274/288/289/290 全留档）。
- 2026-09-20 **M3.5 两批（提示词迭代）**：a＝编辑面右键独立菜单 / 拖选文字→添加提示词块 / 块拖动调分类 / 「添加块」可撤销（commit dd6fb4f；drag.js 放行非本系统拖拽源修连坐）；b＝右键菜单去「插入提示词块…」、**「声明段」概念整体退役**（脚部/右键均「拷上组=全文直拷」）、**热盒条块↔块拖拽换位**（前后插半判定；moveBlockTo/siblingList 上收共用）+ 编辑面 Ctrl+Z 栈空让位全局撤销（peekUndo 分流，避免重渲染带关拼装台）+ 拖完/Shift 点后焦点还编辑面（commit 787607a）。

- 2026-09-20 **simplify-code 审计 M3（报告未入 git）**：8 审查员（复用/质量/效率/高度 × 前端批/后端批）产出仓库根 AUDIT-M3.md：15 真 Bug + ★12 先修 + ☆38 值得做 + ◆9 立项 + 小项；关键项经内存库一手复核（含 9 项实测输出）。

- 2026-09-20 **审计修复批1·后端地基**（用户点名修 一/二/三/五）：①core.ops.record_history 单点入口（替换 20 处内联 history INSERT，列序只定义一次）；②core.ops.reseq 位置原语（替换 8 处「读序→逐行写 position」循环；touch 参数对齐 beats updated_at 口径）；③每日快照升级＝SQLite backup 接口 `.tmp`→`os.replace` 原子落盘（不再裸拷 live 文件，中断不留半截正式备份）+ 保留 30 天自动清理 + **下沉到 db.connect(rw=True) 写边界**（删 handlers×9 + api/prompts×2 调用点；脚本等非 HTTP 写者从此自动覆盖）；④db.py shots/prompt_groups/beats 排序补 `, id` 兜底。单测 39+21 全过；快照实测（原子/幂等/保留/零残留）通过。
- 2026-09-20 **审计修复批2·后端域**：①块写白名单化＝core/fields.BLOCK_WRITE_KEYS 单点，prompts 拒绝未知键（400），api 显式投影（不再透传整包 body；空投影 400）；②守卫与文案拆分：分类参数错误 vs 分类不存在，分类名/镜数上限/块长/组文全走常量单点；③merge 预取（一条 IN 分组查询）＋内存判空组；detach 差集算 left（去掉三处自写自读）；restore 预取既有组＋只动未涉及镜＋all_ids 去重＋走 ops.insert_restore（重命名为公开；不再吞 IntegrityError、不再改写调用方入参）＋更新 updated_at；④分桶共享 db.attach_group_members（handlers.scene 与 prompt_state 共用；prompt_state 补 empty 标记）；⑤block_move 委托 block_update、cat_move 收口 reseq——到头统一 moved:False（不再抛「已经到头了」）；position 严格整型；块操作全走 _load_block/_place_block；⑥api 守卫前置（坏请求不进写连接、不拷库）；⑦seed 内容级幂等（按正文查重，半载可续）＋单事务＋settings.seed_blocks_version＋--reset 前自动快照；⑧测试同步（cat_move 边界改判＋审计契约小测）。探针：内存库 15 项契约全过、种子副本幂等/重置 25 块、场景接口冒烟 200；服务已重启上线。
- 2026-09-20 **审计修复批3·前端**：①操作族：togglePin / deleteBlockWithUndo / moveMenu 上收 blocks.js（热盒条+管理器同源，均带撤销+成功 toast）；②B7 置顶块退出拖序（置顶浮顶与 position 索引两序不同源——置顶块不可拖、不作落点，取消置顶回拖序）；③B1 订阅泄漏修：releaseComposer＝标收起+清块库订阅+清点外监听（刷新/组操作前统一走它）；④B5 点外收起改「指针点外」判定（menuEl 豁免；替代 blur 启发式＋撤 chip tabindex 补偿）＋paintScene 重绘前释放编辑面（防悬空 activeBox 写库）；⑤F1 allShots 单点注入 ctx；F2 占位符清单 blocks.PLACEHOLDERS 单点（管理器文案派生）；F10 写响应就地套用 applyPromptGroups（不再整页重拉）＋Ctrl+Z 让位无条件刷新（B6a）；⑥blockman：inlineCommit 统一行内提交（失败保留输入可重试）、拖放指示 markedEl（O(1) 清除）、搜索防抖 120ms＋单趟分桶、moveBtn/catMoveBtn 吃 moved:False 语义；⑦新模块 hbedit.js（编辑面撤销栈独立：栈深 200 / 分段 600ms 具名）＋ui.js growTextarea/durText 共享＋剪贴改走 writeClipboard。E2E（s110 夹具）：打字/点外折叠/剪贴/块换位/分类拖放/换分类菜单/拆开撤销链/管理器星标+行内改块 全绿零报错；夹具已清场（块库回 16/6）。
- 2026-09-20 **审计修复批4·测试收尾**：①`server/tests/_fixture.py` 共享夹具（test_ops / test_prompts 同源：schema 载入 + 三种基准库，手写 SQL 建状态）；②F11 补缺：restore id 回退（原 id 被别场占 → 换号回插、别场行不动）、删镜不剪组（空组保留 → restore_shots 带组插回）、位置不变量（组位置恒 0..n-1、提示词操作不动镜位置）；③断言加固：装饰字符 assertIn → 精确文案断言、补 ORDER BY 与 len、裸 `[0]` → 按名取值；④规模冒烟：merge 67 镜 SELECT ≤8 次 + 耗时上限（防 N+1 回归）；⑤死导入清理（test_prompts 的 ops）。**四批收官**：d0ccd85 / 9d94266 / 14470ad / 本提交；测试 40+25 全过；◆ 立项区与 §六 刻意边界未动。
- 2026-09-20 **M4 提案 v0.1 出稿（库外 :8095/m4-proposal.html，待过目）**：①审计体验——灯钉在载体左缘留白（镜头行/节拍头/场签/接缝；绝对定位不占列）；点灯 = 行下展开问题卡（详情行同机制）；问题清单 = 痕迹回看同款浮卡（状态分组/搜索/重跑）；五步生命周期（跑→灯亮→跳转→当场改→重检消灯；豁免必写理由、留痕）；②规则清单十项（程序 3 / LLM 2 / 混合 5；「景深」条按 v2 现状提议改「景别完整+景深抽查」，待拍板）；③M4b 轮廓（设置面板 / 就地 ✨ 出 diff / 指挥条 / 草稿档）；④切分：M4a 审计（含最小 AI 通道+设置面板）→ M4b AI 动作。三段截图视觉过审。
- 2026-09-20 **M4 提案 v0.2 拍板 + 四处修订（用户）**：①灯两态收口——表上只有「亮（未处理）」与「没（处理完直接消失，不留灰）」；记录只在清单留档；②豁免＝一键即豁、理由选填（写不写自由）；③规则十项全保留 + 新增「审计设置」逐条开关（参数可改；跑审计严格按设置执行，不写死）；④切分确认：M4a（审计，含最小 AI 通道+设置面板）→ M4b（✨/指挥条/草稿）。施工批次：M4a-1 后端引擎 → M4a-2 前端注解层 → M4a-3 联调验收。
- 2026-09-20 **M4a-1 后端引擎（审计全链）**：①`core/audit.py`——规则注册表（十项：程序 5 / LLM 5）＋程序规则（闭环 / 戏点密度 / 戏点特写 / 景别完整 / 声音完整性）＋LLM 规则（配方驱动：轴线 / 节奏曲线 / 空间一致性 / 机位一致性 / 动作具象化；配方在 `recipes/audit/*.md`）＋问题对账（键 = carrier+target：重跑幂等、同键消息合并；open 未再命中自动转 fixed；fixed 再命中重开；**waived 不自动重开**）＋`JobManager` 后台任务（每场至多一个、进度逐规则上报、服务重启即清）；②`core/ai.py`——OpenAI 兼容通道（配置存 settings 表；key 明文永不出后端）；③接口：`GET /api/audit`（含 job 进度）、`POST /api/audit/run`（秒回任务快照）、`/api/audit/issue`（waive / unwaive / recheck→单规则后台重跑）、`/api/audit/rules`（开关＋参数）、`/api/ai/settings`、`/api/ai/test`；④`scripts/seed_audit_rules.py` 种子十规则；⑤五张 LLM 配方；⑥测试 +11（状态机 / 开关 / 注入桩 / 任务管理器）。实测：s010 干跑五规则质量过审（镜08「看着」、镜09→11 越轴、戏点段无正打读脸、插入镜用反打等）；临时场全链冒烟 21s（含 5 次 LLM 并行）、豁免/取消、清场零残留、s010 未动。⚠️ 延迟特性：LLM 规则耗时与 digest 长度强相关（数千字 = 1~3 分钟），已后台任务化＋digest 瘦身，前端按进度展示。
- 2026-09-20 **M4a-2 前端（审计注解层）**：①`web/js/audit.js`——灯（绝对定位钉载体左缘，锚点四类=镜头行 / 节拍头 / 场签 / 接缝；**只锚「未处理」**，修好或豁免即消失；同处多条带角标记数；有灯行的拖点自动下移让位）＋问题卡（行下就地展开：规则 chip / 「去改·重检·豁免」/ 已豁免可取消＋「加/改理由」内联输入（选填留痕）；点灯开合）＋后台任务轮询（2s，跑完自动刷灯并 toast）＋动作（去改＝滚动＋闪烁＋按规则映射直开字段编辑器；重检＝单规则后台重跑）；②`auditpanel.js`——问题清单浮卡（状态分组 / 搜索 / 跑审计 / 「审计中 n/m」进度 / 规则错误行 / 点击跳转展开）；③`auditset.js`——审计设置（十规则开关＋参数就地改；AI 通道字段＋连通测试；key 明文不回传）；④`api.js` 审计端点；后端两枚只读口 `GET /api/audit/rules`、`GET /api/audit/summary`；⑤场次徽标同步记数（nav chip）；⑥痕迹面板接 audit 实体（审计·状态）。E2E（s130 夹具全链）：灯渲染 / 卡片 / 豁免↔取消 / 去改 / 重检 / 设置开关持久化 / AI 连通 / 徽标 / 筛选联动 / 痕迹条目——全过；**列几何核验：有灯行与无灯行逐格等位、摘灯前后零位移（功能红线守住）**；s010 只读零写入。⚠️ 平铺视图不放节拍灯（无节拍卡载体；清单与跳转不受影响）。

- 2026-09-20 **M4a-2c 宽表修复（用户实测反馈 · 当日修复）**：用户在 s010 实际使用时报「找不到处理控件」——根因：问题卡横跨整张宽表（s010≈1550px），卡内动作钮（去改/重检/豁免/✕）落在可视区外（得横向滚动才见；预览窗窄更甚）。修：`tr.audit-card-tr .audit-card { position: sticky; left: 0; max-width: min(100%, calc(100vw - 110px)); }`——横滚时卡吸左、恒完整可见（实测横滚 0↔294px、按钮位置 [1049..1193] 恒定）。附：完成 toast 改「N 处问题已亮灯（点灯处理）」+ 清单顶部指引行。**教训入坑区：窄表夹具测不出「宽表类」几何 bug**。commit eb34725。
- 2026-09-20 **M4a-3 联调验收 ✅（用户在真实使用中完成）**：用户在 s010 实跑审计（86s、十条规则、8 条问题）→ 自行处理 4 条（豁免：镜8×2 / 镜31 / 镜32，含一次「取消豁免→再豁免」回环；余 4 条 open：镜3节奏 / 镜19机位 / 镜9、镜34声音——留用户决定）→ 翻看审计设置与规则面板 → 实测暴露并促修 M4a-2c。数据完整性对账：与当日快照逐行比对，s010 的 scenes/beats/shots **零差异**；近程写入全部为 audit 实体（豁免痕迹 5 条）——镜头数据毫发无动。验收成立：跑→灯→跳→改→检 全链用户体感可用。
- 2026-09-20 **M4b 提案 v0.1 出稿（库外 :8095/m4b-proposal.html，待过目）**：AI 创作面细化到可施工——①就地 ✨（双入口=编辑态格内 ✨ + 右键「AI 改写…」；diff 浮层卡〔接受/拒绝/再来一版〕；动作族 ×4=改写/具象化/强化/扩写；适用五字段）②指挥条（选区条右侧长出：一句话→预览清单〔逐条 diff+勾选〕→应用；边界=只改写不增删、≤30 条、预览制、一步撤销）③草稿档（从台本出草稿〔空场专用、两段生成〕+ 组级提示词初稿〔进编辑面不自动存〕）④设置维持现状 + recipes/ai/ 配方 ×8。待拍板 6 条（均附推荐）。批次提议：M4b-1 引擎 → M4b-2 前端 → M4b-3 草稿档。

- 2026-09-21 **用户实测三修复（灯换场消失 / 编辑改双击 / 选区条灰显）**：①**灯换场消失真 bug**——`onPainted` 每次重绘都走 2.5s 限流 fetch，快速换场回来那一次「唯一能补状态」的拉取被闸门静默吃掉 → `cur` 恒 null、灯永久不回（1 秒来回实测复现）；修：换场（scene 变）强制重拉。实测含「审计中切走再回」：进度 5/9 切走 → 回来 8/9 轮询续上 → 完成亮灯。②**单击=选格、双击=编辑**（回飞书体感）——`edit.js` 两控件加 `dbl` 配置（仅网格两处传 dbl；详情区/节拍头/场头维持单击编辑）；`selection.js` 单击塌缩为单格选区；合成开启点全改派 dblclick（Enter 开编 / Tab·Enter 走格 / 审计「去改」/ 摄影机与机位双击开控件）。③**选区条按可行性灰显**——判定口径「会当场报错/空转的才灰」：并为一组 <2 镜置灰（其守卫本来就弹错）+ title 说明；其余命令（独立成组/删除行/设值/清空/复制）对单镜选择均有效，保留。E2E（s010 只读 + s130 夹具）全绿、零报错、零写入。commit c70c384。
- 2026-09-21 **JobManager.start 规则来源对齐（测试随线上设置漂移的真错位）**：回归时 `test_audit` 红 1（`9 != 10`）——根因：`JobManager.start` 读规则用**进程默认库** `db.connect()`，而 `_run` 用注入的 `connect_factory`，两源不一致（生产同源不显形；测试夹具被线上设置污染——表因：线上「动作具象化」开关被关，设置功能按预期工作）。修：start 与 _run 同源（`connect_factory() if connect_factory else db.connect()`）。77/77 全绿。

- 2026-09-21 **M4b 提案 v0.2 拍板 + 修订**：①六条全按推荐 ✅（双入口 / diff 浮层 / 四动作×五字段 / 指挥条边界 / 草稿档范围 / 设置条）；②**新增需求「设置与配方中心」**——AI 通道（含 key）+ 配方编辑收进一个页面统一管理；**排期拍板：放 M4b 批 3、不拖 M5**（理由：配方是 M4b 可调心脏，验收后第一波需求必是调配方，调参闭环需自足；M5 留打磨）；③批次修订：1 引擎 → 2 前端 → **3 设置与配方中心** → 4 草稿档。提案页 :8095/m4b-proposal.html 已更新为 v0.2。

- 2026-09-21 **M4b-1 后端引擎 ✅（创作通道，2c4e28e）**：新 server/core/rewrite.py——POST /api/ai/preview（动作族 4 件 + 指挥条「人话」；单次调用出一版全稿；目标 ≤30；shots(blocking/dialogue/director_note) + beats(beat_action) + text 目标）→ GET /api/ai/job 轮询 → POST /api/ai/apply（source=ai 落库、一步事务；「原值未变」守卫防覆盖手工改动；text 目标只出稿不入库）。预览零写入（items 只在内存）。配方 ×5 入库 recipes/ai/（rewrite/concretize/strengthen/expand/cmdbar；统一输出 {"items":[{"i":N,"after":"…"}]} 序号对位、严格 JSON）。测试 test_ai.py 15 例全绿（全库 92/92）；线上冒烟：临时场 s110 真调 DeepSeek（1.0s 出 2 条具象化稿）→ 应用 2/2 → 痕迹 source=ai → 场与痕迹清理零残留。

- 2026-09-21 **M4b-2 前端 ✅（就地 ✦ + 指挥条，cea2620）**：新 web/js/aiwrite.js + 七件接线（edit.js 编辑态 ✦ / api.js 三接口 / table.js 三字段 aiOpen＋refreshBeatAction / cellmenu.js 右键三路（格·选区·节拍头）/ selbar.js「✦ 说一句人话」指令框 / hotbox.js 选中段改写 / scene.js 装配＋重绘收起）。口径落实：文本目标只出稿不入库（前端替换走编辑面栈）；接受=source=ai 落库+一步撤销（单格 field 撤 / 批量 custom 撤）；无改动条目零落库（勾 3 应用 2 实测）；上限 30 格。E2E（临时场真调全绿）：接受→编辑器就地新值→Esc→Ctrl+Z 复原（toast「已撤销：AI 具象化」）；勾选过滤应用 / 指挥条全收 / 再来一版两稿不同 / 拒绝零写入 / 拼装台选段替换+撤销 / 节拍头菜单在场 / s010 只读装载零错。施工修复两处：✦ 改钉格右下（吸顶冻结头死区）；textAccept 回调名对齐。

- 2026-09-21 **M4b-3 设置与配方中心 ✅（1c631db）**：顶栏右角「设置」→浮卡两区。新件四：core/recipes.py（注册表 10 份＝审计5＋创作5；白名单＋resolve 前缀双保险；保存先备份 data/recipe-backups/（每份留 30）再落盘；恢复默认＝data/recipe-defaults/ 出厂副本；≤200KB；空白拒）、api/recipes.py（list/get/save/default）、test_recipes.py（7 例）、web/js/settings.js（AI 通道自审计 ⚙ 搬迁＋配方分组列表→就地编辑→保存/恢复默认→返回）。接线六件：app.py 四路由 / auditset.js 去 AI 段留提示 / auditpanel ⚙ title / main.js 绑按钮 / api.js 四方法 / index.html＋app.css。验收：全量 99/99（＋7）；冒烟＝base_url 暂指本地 mock 截获服务端发出的 system 提示词，改→恢复→再改三连逐次对账（模型出局、现读硬证）；E2E 全绿（开卡/编辑/保存/恢复/返回/关闭＋审计设置搬迁核对＋ERRS 空）。修复：备份名 μs 回绕致排序倒挂（测试抓）。

- 2026-09-21 **M4b-4 草稿档 ✅（6368453）**：两块——①从台本出草稿（空场专用：空态「＋ 添加节拍」旁长「✦ 从台本出草稿…」；贴台本 → 两段生成后台任务（①节拍骨架→②镜头行）→ 线框预览 →「落入草稿」整批写入、一步撤销）；②组级初稿（拼装台 pb-head「✦ 初稿」→ 按组内镜头数据+块库出稿 →「插入编辑面」＝replaceAll 入编辑栈（Ctrl+Z 可撤）草稿态——保存才落库）。服务端：core/draft.py（DraftJobs 内存任务；解析规整＝kind 归一三值/机位归一五色/越界丢弃；apply＝只新增顺延编号＋痕迹 source=ai；重复落入拒；台本 30～6000 字）、api/draft.py（start/prompt/job/apply）、配方×3（注册表 10→13，设置面板自动可见）。前端：draft.js 两卡 + scene.js 空态入口 + hotbox 组头按钮 + hbedit.replaceAll + 两卡入「点外监听」豁免（hotbox/selection）。验收：全量 112/112（＋13）；真调冒烟（两段 14.8s→5 拍 12 镜编号全对、预览零写入、痕迹 17 条 source=ai、初稿 293 字未自动落库、重复落入 400）；E2E 三幕全绿（空场全链 / 落入＋Ctrl+Z / 初稿→插入→Ctrl+Enter 保存）。

- 2026-09-21 **simplify-code 审计 M4（报告未入 git）**：16 审查员（复用/质量/效率/高度 × 4 批：服务端审计域 / 服务端 AI 域 / 前端审计域 / 前端 AI 域）产出仓库根 AUDIT-M4.md：29 真 Bug + ★12 先修 + ☆约 45 值得做 + ◆10 立项；关键项经内存库一手复核（3 项实测复现：reset 孤儿 / 草稿并发双写 / action 500）+ 浏览器实测 9 项；全程零写库（审查窗口零 POST）。

- 2026-09-21 **M4 修复批1 · 数据安全线 ✅**（`de21316`）：AUDIT-M4 十条落地——G1 seed `--reset` 按 title 回迁 issues（孤儿根治）/ G2 回包不可解析走规则级 error（不假熄灯）/ M1 审计 start 锁内认领 + joined 标记 / L5 重检如实提示 + 空 rid 400 / G3 草稿落入锁内 CAS / G6 草稿 job 契约 404→200+null / G5 初稿卡定时器实例化 / M12 pollJob 取消通道 / M13 选段锚点校验（indexOf 退化）/ M8 重复目标去重（留最后）。测试 113→118 全绿；浏览器四幕实测（替换存活 / 重启丢失 / 取消即停 / 过期锚点）+ scratch 场 API E2E（并发 joined / 守卫 / 级联清场）；s010 全程零写入。

- 2026-09-21 **M4 修复批2 · 审计线 ✅**（`2953b61`）：十一条落地——G4 载体解析单点 `resolveCarrier`（灯/卡/清单跳转/去改同源；平铺节拍回退首镜行）｜M2 接缝灯防裁（wrap-off 下 `td.cell-toggle` 豁免，特异性压过 `table.shots td`）｜M3 closeCard 补派 resize（吸顶 `--freeze-h` 开↔合重算）｜M4 跳转未落地如实报 + 切「全片」自动关清单｜M5+P2 轮询四小改（无变化不 notify / 换场即停 / 页面隐藏不拉 / 单飞+时序号；回前台补拍）｜M6 筛选隐藏联动同行卡行｜L2「去改」按载体摆钮（scene/seam 不再空转）｜L4 引用归一 `_norm_ref`（节拍/beat 前缀不再丢）｜L7 静默重绘零 toast 打扰（openKey 保留）｜F8 审计设置↔顶栏设置互斥｜P5 余项条件写守卫 `ops._guarded_set`（防丢更新）。测试 118→120 全绿；浏览器 T1–T9 全过 + 请求特征实测（2s 拍零叠发 / 隐藏期 0 拉取 / 恢复 0.4s 补拍 / 完成即停）；scratch 清场零残留；s010 零写入。

- 2026-09-21 **M4 修复批3 · AI 通道线 ✅**（`7374775`+`4159e51`）：M10 任务闸门（同场在跑 → 并入 `joined`；全通道并发 ≤ `TASKS_MAX`＝4；前端并入 toast）｜M9 剪枝护 running（只淘汰完成件，不够删允许超 keep）｜M11 配方原子写（tmp + `os.replace`）｜M7 preview 类型防线（非文本 action/instruction → 400，不再 500）｜L6 校验单源（api 删重复校验，空白 instruction 口径归 core）｜清尾：`load_recipe` 走注册表白名单（不再裸拼路径）、`ai.test`→`probe`、注册表↔引擎 AI 组对账测试、`scripts/seed_recipe_defaults.py` 出厂副本自举｜包：P6 ✅ P7 ✅（含轮询期轻载：在跑时大文本不回传 + `beats_n` 计数）。测试 120→133 全绿（+13）；真调实测（第二枪并入同 id、一次外呼；轻载轮询键位核验）；UI 实测（并入 toast + 共享任务出稿 + 零 JS 错误）；s010 零写入。

- 2026-09-21 **M4 修复批4 · 前端与设置线 ✅**（`cccde33`）——**第一轮（一/二/三）全收官：29/29 Bug + 修复包 P1–P12 全部修复**：M14 设置卡三缺陷（三键恒定发送、空串=清回默认、保存后回读+key 置空）｜L1 点外豁免单点（浮卡统一挂 `.float-card` + `ui.isFloatTarget`；hotbox/selection/相机表单三处名单收编——点卡不再清选区/收编辑面）｜L3 审计参数守卫（空值/非法拒绝不折算 + 保存失败回滚）｜L8 AI 卡锚点被重绘销毁后原位保持（lastAnchorRect 兜底）｜L9 编辑基线同步（`ed._syncBaseline`——AI 接受后收起不重复写、不多压撤销）｜P8 单源（`/api/meta` 下发 ai_fields / ai_max_targets；前端 AIS + 30 格上限改读 meta）｜P9 单格卡（`[it.i]` 按项定位、接受禁双击、skip 终态卡、并入任务按目标匹配）｜P10 ✅（L2 批2 已修 + L3/M14 本批）｜P11 死件清理（ctx.refresh 注入 / export closeCard / qb primary+死样式+重复 rowFlash / `write` 死参 / `cur.rules` 前后端同步删 / 灯具 mousedown）｜P12 测试网（+13：api 守卫 / meta / ai 契约 / dict 回包 / 夹具上收 make_base_db+conn_factory / 桩键改标题级）｜D 尾（fieldOf 单点、cellmenu 复用 targetsFromSel、草稿撤销批删、settings 未保存返回提示）。测试 133→146 全绿；浏览器实测（L1 三项保持 / L3 拒绝回滚 / L8 原位 815px / L9 零重复写+单次撤销 / M14 清空回读）；scratch s100 清场零残留。

- 2026-09-21 **M4 二轮 · 第1步（L2+L1）· 任务基建与通道契约 ✅**（`7c82222`）：新建 `core/jobs.py`（`Gate` 并发计数闸 + `JobBoard` 基类：锁内认领/同场并入/剪枝护 running/轮询期轻载/收尾释放恰一次）｜audit `JobManager`（无闸、语义不变）/ rewrite `PreviewJobs` / draft `DraftJobs` 三簿迁移（各留条目形状与 `_run`；`_task_acquire/_prune×2/_snap×2/_default_chat/_reply_text` 全退役）｜`ai.channel()/require_key()/reply_text()` 契约单点（配置读五处→一、key 预检同源、归一宽进 dict→text；api 层预检同源）｜测试桩改真形状 `{text}`（「桩 str、真调 dict」两形态分歧根因铲除）。测试 146→157 全绿（+11）；真调（活服 preview 出稿 4.3s、s010 history 零写入）；audit 基类对生产库副本全流程；服务已重启。

- 2026-09-21 **M4 二轮 · 第2步（L8+L4+L3）· digest 单点与数据规则 ✅**（`9fcf99e`）：新建 `core/digest.py`（场线 `scene_line(sc, terse)` / 速览 `shots_lines(rows, spec, colon)` / `LABELS` 短标签表）——audit 五条 digest、rewrite 镜头速览、draft 场线+组内速览全切换（黄金对拍 10 例逐字节等；唯一有意归一：草稿「空间关系」→「空间」）｜`ops.append_beats/append_shots` 追加行原语（编号顺延/位置续尾/痕迹/不 commit）；`draft.apply` 只做映射｜`audit.RULES` 规则注册表单点（key/kind/recipe/params schema/desc 一张表；引擎字典换 slug；`_rule_key` 老行回退）｜DB 迁移（schema.sql key 列 + `_ensure_key_column` 自动补列 + 种子按 key 幂等/回填/reset 回迁 + `rules_state` 下发 schema）。测试 157→174 全绿（+17）；真调（preview 3.5s 零写入；副本草稿 5 节拍 16 镜全顺延落入 + 二次拦）；活库十行回填；服务已重启。

- 2026-09-21 **M4 二轮 · 第3步（L7+L9）· 写入出口单点与表单 schema ✅**（`48242b6`）：`edit.commitField`（字段落定三件套单点：本地模型 / 编辑面基线 / 撤销；AI 单格接受改走此口）｜`selection.batchWrite` 泛化（`opts.write` 写口注入 + table·resolve·refresh 换域 + done·fail 收尾；AI 批量应用已接入——一次写口一步撤销）｜`hbedit.replaceRange` 范围替换单点（选段改写 / 剪切 / 插入 / 整文替换四路收敛）｜`web/js/formkit.js` 表单件单点（fieldRow/collect；设置卡+审计设置卡切换、零视觉变化）｜auditset 参数控件由后端 `params_schema` 派生（PARAM_CN 退役）｜「去改」目标列随问题下发（RULES.field → issue.field；FIELD_HINT 退役——改标题/换列不再静默失能）。测试 174→176 全绿（+2）；浏览器五幕实测（含接受双分支 / 参数往返 / 选段替换 / 去改定位）；两卡截图逐像素一致；s010 数据净回。服务运行中。

- 2026-09-21 **M4 二轮 · 第4步（L6+L5）· 浮层治理与任务卡原语 ✅**（`6610f74`）：新建 `web/js/float.js`（互斥注册表：同层开新关旧 + `panelShell` 外壳基类）——外壳收敛：设置卡 / 审计设置 / 审计面板 / 草稿两卡（panelShell）＋ AI 两卡（taskShell）；互斥三层入表：`'panel'` 设置↔审计设置（F8 收编）/ `'ai'` 单格↔批量（含文本卡）/ `'draft'` 场次草稿↔组级初稿；`closeAiCards/closeDraftCards` 改走 `floatClose` 全量入口；**审计面板补挂 `.float-card`——修前点面板＝清选区/收编辑面/关相机表单（本轮实测复现，已修）**。新建 `web/js/aicard.js`：`pollJob` 轮询单点（间隔/上限/取消/错误契约一处）+ `cardLife` + `taskShell/spinHead/cancelBtn/renderFail`——四卡（单格/批量/场次草稿/组级初稿；文本卡随单格）全迁移，手写轮询（`setInterval` 两处 + pollJob 一处）退役。测试 176/176 全绿；结构化对照：设置 3181 / 审计设置 3971 / 单格 485 / 批量 571 / 初稿 402 / 草稿表单 517 / 运行 283 逐字节一致（草稿预览＝表头+尾部逐字节、单元集合相等，拍镜数差异为内容变体；面板差异＝补挂类+实时数据）；行为回归全绿：G5 换卡 / M12 ✕ 中途取消 / L8 锚点销毁后滚动原位 / L9 接受+ESC+Ctrl+Z 净回（恰 2 条痕）/ 文本卡选段替换零落库 / 互斥与重绘收起。s010 净回、s060 零写入；服务运行中（无 py 改动）。

- 2026-09-22 **M4 二轮 · 第5步（L10）· 测试网补强 ✅**（`4a941bc`）：api 三域薄层 **+24**（设置薄层：短键→全键映射 / key 永不回传 / 探活双形状｜应用守卫：job 三连 + 域错映射 + 成功形状｜草稿 prompt·apply｜**提示词/块库薄层零覆盖补齐**——守卫先于写连接以 dead-db 实锤｜审计薄层：issue_op 守卫 / recheck 孤儿 400 / joined 透传 / run / rules_get 契约 / summary）｜对账扩展 **+3**（`rules_state` 每行与 RULES 全字段对齐〔含 API 边界〕/ 种子幂等 / 审计注册**反向无孤儿**）｜core/ai 契约 **+3**（配置默认兜底 / 注入桩跳预检 / probe 预检先行）｜并发三例清点（draft 双线程 / audit start 并入 / rewrite 并入——随各步已补齐）｜修正 `api/prompts.py` update 投影空检查前移（坏请求不触写连接）。单测 176→**206/206** 全绿（+30）；**咬合力实测**：降级旧版 prompts → 新守卫测试翻车 → 还原全绿。

- 2026-09-22 **GitHub 建库推送 + 双 Skill 拆分落地**：GitHub `shaobaojia/shotlist-studio`（公开）建库首推——M1–M4 全量代码 + 设计 + 配方 + 测试（206/206）；Skill 拆分（DESIGN §10）：创作 `shotlist-studio`（使用指南 + 创作方法论 + 7 份方法论 references 自老 skill 平移）＋ 开发 `shotlist-studio-dev`（架构/管线/验证/坑），互不共享文件；老 `storyboard-shotlist` 封存标注（触发词已平移至新技能）。

- 2026-09-22 **AUDIT-M3 / AUDIT-M4 入库**：审计报告与修复台账进 git（README 索引同步、库外待定项清零）；GitHub 已推送——设计 / 接力 / 体检三簿闭合。

- 2026-09-22 **M5 批1（速修包，`24e8373`）✅**：六件——①A1 平铺空场（有节拍无镜头）原近白页 → 引导 + 「＋ 添加节拍」入口（scene.js flat 分支）；②A2 Shift 点选蓝斑根因修（selection.js：mousedown preventDefault + 清存量原生选区）；③A4 移除「M2 · 可编辑」陈旧硬编码标签（index.html）；④N3 场签 flex-nowrap + 6px 细条横滚不换行（app.css）+ 路由活动签 nearest 入视（main.js）；⑤C4 运镜 presets 常用档 11 项——fields.py 定义 + 编辑面浮条（edit.js）+ 格/详情两处接线（table.js），一键落值 + 自由手写；⑥N2 选中框「稳定帧复绘」（selection.js）——分组模式点击同步窗口内首绘偏 ~6.9px（帧探坐实），rAF 补绘归位、各入口统一。验证：206/206 + 浏览器全项实测（临时场 C4 两态）+ 零报错 + 净回（scenes 10 / shots 55）。

- 2026-09-22 **M5 批2（表格操作流）✅ 全收官**：①2a Excel 键位核心包（`f5c4f16`）——打字即编（IME 首键直进）、Esc 还原零写库、Ctrl+C/V/X 直连、Ctrl+D 向下填充、Ctrl+A 全选本表、Delete/Backspace、F2/Home/End、机位枚举列打字弹菜单；②2b 多选行拖动＝整组搬家（`00e93e9`）——`move_shots` 一次事务逐行留痕、组内顺序不变、落点按「去掉整组后的插入位」计算，单行拖动一字不差；③2c 选区条批量设值扩展到节拍/场景（`3a14563`）——节拍 14 项按所在节拍、场景 6 项按本场、平铺跨节拍一次多设、类型走 beat_kinds 单源；④2d 撤销完整性核对 + 快捷键速查卡（`5906a2a`）——16 项动态电池全过（编辑/插删拷/整理镜号链/节拍副本/合拆组/场次副本/删场全量还原/空值归一 ''），速查卡 68 行（走格/选区/编辑面/拖动/右键/选区条/场务）。收口补丁（`976b954`）：`/api/delete` 防呆（table 必填显式、缺失/非法 400，不再默认 shots）＋右键菜单滚动条修复（max-height min(72vh,430px)＋细滚动条，单元格右键 9 项全显不再滚）＋s010 遗留 7 条审计复核豁免清零（用户拍板，可取消）。过程留档：shot 41 误删事故全量恢复（当日快照 + `insert_restore` 原 id、窗口约 90 秒零净损失）；E2E 四配方教训入 dev 技能坑区 17。验证：213→214/214 全绿 + 全程净回（10 场/55 镜/14 节拍）。

- 2026-09-23 **M5 批3（右缘抽屉体系）✅ 全收官**：①3a 浮层抽屉基础件 + 提示词抽屉（N5，`ed453b0`+`1f9e5ac`）——新件 `web/js/drawer.js`（标题栏拖拽/八向缩放/右·下贴附回弹/钉住/位置尺寸记忆/弹性动效/panel-btn 按钮行）；拼装台从「详情行就地编辑」迁入「旧版样式独立抽屉」：查看态徽标着色（人物绿/场景琥珀/空间锚蓝/镜头紫/[镜XX] 红注）+复制过滤+空词防崩；编辑态块库拼装/✦初稿/存→下一镜/拷上组/存为块全量保留；口径：Ctrl+Enter 存→下一镜 · Esc 编辑→查看（不保存）→再按关闭 · 点外=保存并关闭（未钉住）· 钉住=不关+点行切镜；详情行保留只读预览（点开抽屉）。②3b 剧本抽屉（`ee13730`）——`scenes.script` 接线（ops 写白名单表级例外+3 测试）；「台本」按钮（工具条+空场入口）开抽屉（查看⇄编辑/一步撤销/钉住切场跟随/复制）；「导入台本…」整本切分（行首「s010…」「10、…」双模式 → 逐场勾选覆盖，已有台本默认不勾，整批一步撤销）；「✦ 从台本出草稿」自动预填。③3c N4 分组标识（`c360ec8`）——提示词列徽标「组N · x镜」（初表/刷新共用 paintPromptCell）+查看态成员 chips（点跳闪烁）+右键「并入上一组」（无上一组禁用）+commitClose 切镜防补关。验证：pytest 214→**217/217**；浏览器全链实测（两轮临时场）；零报错；净回 10/55/14/17。教训（测试侧）入 dev 技能坑区 18/19/20。

- 2026-09-23 **M5 打磨·追问轮 ✅（三件一次实装，`99fc913`）**：①覆盖高亮＝**B 聚焦降噪**（打开提示词抽屉 → 覆盖镜行茜红浅铺+左轨、其余行退噪 42%，悬停恢复；查看态/切镜跟随、关闭还原；刷新点＝renderDrawer 尾 + applyPromptGroups）。②布局＝**② 三段固定**（抽屉头状态行「镜 · 组/覆盖 n 镜 · 字数」；正文滚动区与底部动作条固定；块库条退役）。③**块库卡**（新件 `web/js/blockcard.js`）＝提示词面板（上层卡纸）下压着的下层小卡纸：收起只露 12px 边 + 左缘小**索引标签**拉手（用户自四款备选拍板，要求「精巧不傻大」）；点标签抽/收（180ms）；拖外缘调宽 180–360（下贴附调高 130–380）并记忆（localStorage `studio.blockcard`）；随贴附换向（浮窗/右贴附＝左抽，下贴附＝上抽、标签钉上缘）；只在编辑态出现；「管理」入口移入卡头；内容＝分类筛选 + 搜索 + 点插。跟随机制＝MutationObserver 观察面板 style/class/hidden + 几何读内联样式（动画期 rect 会漂）；fixed 定位换算用 `clientWidth/clientHeight`（scrollbar 15px 差，右贴附收起态踩过）。实测：pytest 217/217；浏览器全链（展开/调宽记忆/插入/贴附互切/切镜跟随/管理/收场）零报错；净回零写（组7 文本 1526 未动、history 无新条目）。教训（跟随/几何/避让）入 dev 技能坑区 21/22/23。
- 2026-09-23 **M5 追问轮②（M5d 实测反馈修复批 ✅，用户 7 条 + 自查复现一次修齐）**：①三段固定真落地——抽屉体＝[状态行][滚动区][操作区]，底部按钮排/查看态操作行常驻（`.hb-scroll`/`.pd-scroll`，`[data-drawer="prompt"]` 限定不扰台本抽屉）；②打字跳顶根因＝`growTextarea` height=auto 塌缩把祖先滚动位挤 0 → 改高前后存还祖先 scrollTop＋ta.scrollTop 归零；生长 textarea 必须 `flex:none`；③影子方向化（`.bc-under(-up)/.dragging` 负 spread 单向分量）——面板投影不再落卡纸；④右缘避让方向化（左模式右避让 42/40、上模式列表滚尾 34）——块行文字不再被压；⑤块样＝白卡+分类色条+悬停「＋插入」、底色换与面板同族纸色；⑥弹出时序＝块库等面板 `.enter` 摘除后浮现（`.bc-appear`）；⑦死机级回路根治——观察者跟随×对面板的 class 写（本机 headless 幂等 classList 写也发 mutation）→ 400+/秒回调冻页；改**镜像状态守卫** `applyUnder(mode)`（状态真变才写）；⑧收起态「纸口带」`.bc-clps`。验证：pytest 217/217；浏览器全链（三区/打字/时序采样/上抽/查看态/插入/纸口/贴附互切）零报错；净回零写。教训入 dev 技能坑区 24–27。
- 2026-09-23 **M5 追问轮②·二批（用户二次实测四条 ✅）**：①**A3 拍板落地**——块库分类栏改「分段标题列表」：取消分类 chips，列表按分类分段（吸顶段头：色点+名+计数+折叠箭头），顶部 `全部 / ★ / 收起全部`；折叠集记忆 localStorage `studio.blockcard.fold`；搜索态＝平铺。②**上贴拉手回归修复**——m5d4 重写丢 `bc-up` 类切换（`.bcard.bc-up` CSS 成死代码 → 下贴附标签按左模式摆位被面板盖住）；补回并立「重写对账」坑。③**脚排对齐 mock**——`.drawer .hotbox-foot` wrap→nowrap 单行；提示语右端截断；四按钮改**面板按钮语言**（红底白字 hover 反白，✦初稿紫底）——即用户所说「那行按钮」的观感根源。④**分组操作不再自动弹面板**——`runPromptOp` 尾「drawer 未开 → openPromptDrawer(edit)」改 `jumpToShot`（滚到+闪烁）；**分组编辑提案（G1/G2/G3）用户拍板暂缓**。验证：临时场 s100 实弹（选 2 行→并为一组：不弹面板/行闪烁/徽标「组1·2镜」）+ 点格开抽屉回归 + 净账（57/58 场全清、history 剪枝 7 行、10 场/55 镜复原、组7 1526 未动）；0 报错。
- 2026-09-23 **M5 追问轮③·头两件（用户三问）**：①**「💾 保存」钮退役**——辨析：它实为「保存+切查看」，且保存路径已四条（Ctrl+Enter／点外面／✕／钉住切镜）→ 编辑态不再渲染（查看态保留「编辑」回程钮）；初稿 toast 与速查卡同步改写。②**状态行「已选 N 字」**——单点 `setSelCount`；编辑面走 ta `select/keyup/mouseup/blur`，查看态走 document `selectionchange`（锚点在抽屉内才计）；`S.selStatEl` 每渲染重建。③**块库右键菜单+管理面板退役** → 提案页 `m5d3-proposals.html`（:8095）——右键三处（块行 6 项／段头 5 项／空白 5 项）+「整理态」吸收管理器 10 项能力对照，**待拍板**。
- 2026-09-23 **M5 追问轮③·下半场（一把全做：右键菜单 + 卡内整理态 + 管理器退役 ✅）**：新增 `web/js/bc-ops.js`（操作族 · 行内提交协议 · 三处右键菜单 · 入口）与 `web/js/bc-org.js`（整理态渲染 + 拖拽；单向依赖 bc-org→bc-ops）；blockcard 卡头「管理→整理」/「✓ 完成」绿钮；**blockman.js 引用清零（管理器退役，文件留存待观察）**。菜单：块行＝插入/复制/置顶/移动到分类▸/编辑块/删除 · 段头＝＋块/改名/上移/下移/删类 · 空白＝新建块/新分类/全收全展/整理开关；三处入口同源。整理态：段头 ✎↑↓＋块✕、行点文本即编、⋮⋮ 拖拽排序换类（drop-end 指示）、＋新建块/新分类、空类也显（供新建/拖放）。E2E：临时类/块全链（建→改→拖→删）净回 6类/7块、置顶往返净回、插入 +325 字、Esc 撤弃、0 报错；重载终态一致。
- 2026-09-23 **M5 追问轮④（滚轮穿透 / 拖拽直操 / 就地编辑 / 大三角）✅**：①**滚轮护栏**——`ui.js installWheelGuards()`（document capture）：`.drawer/.bcard/.menu` 内无可消费滚轮的滚动层时吞掉滚轮，根治三种穿透（非滚动区链滚 / 滚到边界链滚 / 块库边缘链滚；实测六场景 page 零泄漏）。②**拆整理模式**（用户判定过度设计）：`bc-org.js` 退役 → 新 `bc-list.js`（列表一屏装全部）＋ `bc-ops.js` 去 setMode；块行＝[⋮⋮ 拖拽][☆][点击插入][✕]，**⋮⋮ 常开拖拽**：行间排序 / 跨组 / 拖到组头＝进该组末尾（真鼠标拖往返实证）；段头操作全走右键；**右键「编辑块…」＝就地编辑**（row._startEdit，不切模式）；组名重命名同样就地。③段头折叠三角 10px→13px。④块行回归白卡＋左缘分类色条；空组＝22px 可见拖放落区。E2E：净回 6类/7块；真拖交换→复原；搜索/★态自动禁拖；0 报错。
- 2026-09-23 **M5 追问轮④·补（M5g：计数贴名 + 编辑态点击守护）✅**：①组计数改「骨架（1）」贴组名——段头 name 不再撑开（flex:0 1 auto ＋ min-width:0——**拆掉 m5e 残留的 76px 底线**，短名不再留空盒），count 近贴（margin-left:-3px），实测七段头 gap 均 3px。②**编辑态点击守护**——行 click 先查 `row.querySelector('.bco-edit')`：行内编辑时点击（编辑框内点选／点行空白＝提交）不再冒泡触发「点击插入」；点别行仍正常插入。E2E 三场景（框内点击零插入／点空白提交零插入／点别行 +40 字）＋ 0 报错。③**测试泄漏还原**：19:19 一条 127.0.0.1 set_text（+325 测试插入，真鼠标坐标落空触发「点外面＝保存」）→ serve.log 来源 IP 定性 + history 还原 group7=1526（#965 留痕）。教训入 dev 技能坑区 32。
- 2026-09-23 **M5 批4（视觉核验·第一轮）✅**：五档扫描（1024/1280/1440/1920/2560——主界面×5＋抽屉·块库卡×4＋下贴附/台本/行菜单/设置@1280）：零横溢出、零截断错位、抽屉全在视口、脚排单行、行菜单 10 项完整、设置 620×640 在视口；**顶栏「＋≤1440 换行孤立」未复现**（现为横滚不换行，细滚动条系设计内）。修复：**滚动定位避让吸顶**——`tr.shot, section.beat` 加 `scroll-margin-top`（topbar+freeze+10），跳镜/闪烁不再藏进吸顶下（chips 跳镜行顶 152→184＝吸顶底+10 实测；咬合：margin 归零即不动作）。次级浮层（审计/AI/草稿/痕迹）未重扫（批次内无改动）。

## 正在做
- 第 2 波立项 · 批1（S 系 15 条）：刀1 拆包双子 ✅、刀2 db 边界 ✅、刀3 api 收口全录 ✅、刀4 路由安全网 ✅、刀5 S4 余项包 ✅、刀6 S2-L1 key 单源迁移 ✅；下一刀＝S2 余（scene_ctx 单点 / canonicalizer）；批2（F 系 20 条）随后。
- M5 追问轮④ 收口：滚轮护栏 + 拖拽直操（排序/换组/组头）+ 就地编辑（拆整理模式）+ 大三角 ✅ 全链实测（真鼠标拖往返净回）；M5g 补丁（计数贴名「骨架（1）」+ 编辑态点击守护；测试泄漏还原 #965）✅。批4 视觉核验·第一轮 ✅（五档扫描零问题＋滚动避让吸顶修复）。下一步 → 无（v1.0 已冻结 2026-09-23；新需求走需求池）。
- M5 批5 挂件带（形态 B）✅：表头上方细带、默认收起（localStorage 记忆）；①景别序列条一格一镜——高度/色阶＝档位（渐紧渐松）、同档相连（连续同景别）、点击跳镜；②节奏视窗——条长＝时长时间轴 × 情绪温度曲线（按节拍读 `mood_temp`）。实测：34 格/34 块、跳镜闪烁可见、曲线假数据（8→y10.8／空→虚线／3→y22.8）、持久化 ✓、平铺共存 ✓、空场守卫 ✓、0 报错。跳镜收敛单点 `filter.jumpToShotById`（镜号跳转/挂件带共用）。**情绪温度曲线候数据**：`mood_temp` 全表为空——节拍行填「情绪温度」后曲线自动出。
- M5j 实时件 ✅（用户报「改景别/时长，挂件带与总时长没反应」）：根因＝字段落定（save()/commitField/摄影机复合/batchWrite）只更单元格，场景头与挂件带是绘制时快照。修法＝行级落定广播 `shotlist:rows-changed`（`edit.js notifyRowsChanged` 单点，四处已接）＋ `scene.js syncLive` 活体同步（「规模/总时长」就地重算＋挂件带原位重建，不整页重绘）。实测：时长 4→14 实时（1′49″→1′59″、块 32→112px、节点不换＝零重绘）；景别 中景→近景 序列条 lv 3→5 实时；批量双行 2s→9s 联动。全链净回：快照逐行 diff=0（history #981–993 留痕）。教训：时长格显示四舍五入（1.5→显示 2s），回写取值以 DB/原文为准。
- M5k 挂件带 v2 ✅（用户拍板新形态）：**底部冻结通栏**（吸底 33px 细带·默认收起·LS `studio.dock` 记忆）＋展开＝**向上浮层**（不挤内容：行位/页高实测零变化）。一条条带三种读法：①景别序列（高/色＝档位）②节奏视窗（宽＝时长 8px/s、横轴 镜号⇄时长）③情绪曲线（叠加共存）。切换全动画（条条 morph＋曲线淡出重定位淡入＋轴标淡切）。`--dock-h` 联动 #view 余量与 #sel-bar 让位。实测：五态矩阵＋跳镜＋假数据曲线（M0 14.4→L48／M50 24／M67 30.4）全对；0 报错。
- M5k-2 挂件带交互五件套 ✅（用户精修）：①上沿**窗口式拖拽缩放**（40–380px，条条按比例同放，LS 记忆）②情绪曲线改**平滑曲线叠加**（Catmull-Rom 三次贝塞尔＋纸色光晕＋节拍数据点，首尾平延）③三开关**绝对居中＋定宽 74px**（切换零漂移；折叠态三开关与图例隐藏）④**滚轮＝横向缩放**（0.35–4×，光标锚定，面板区内页面不滚）／**中键拖拽＝平移**／shift+滚轮＝横滚 ⑤节奏态横轴＝**时间刻度尺**（0″→1′40″ 自适应步长、缩放随动；取代每镜时长标签，关节奏回镜号）。实测：44↔164、zoom 2.94 锚定 Δ1%、平移 394=694−300 精确、居中 633=633；0 报错。
- M5k-3 情绪温度直填入口 ✅（用户问「在哪填写」咬出的缺口）：节拍头右侧加「温度」芯片（`renderMoodChip`＋attachEditable 复用 kind/name 同款机制；空态虚线「温度 ＋」/有值实底「温度 N」）。写入走 commitField → rows-changed 广播 → 挂件带曲线**实时出**（节点标记实测零重绘）；Ctrl+Z 撤销即回。旧入口（选区条「设值…」→ 节拍字段 → 情绪温度）保留为批量口。实测：温度 ＋→7→曲线 `M26 17.6…`（点 y=17.6 精确）→ Ctrl+Z → 全还原；三表 diff=0、history #1006/#1007 留痕。**注意**：编辑器 base 把 NULL 归一为 ''——撤销后 NULL 字段变 ''，净回核验须手工归位 NULL＋updated_at。
- M5k-4 情绪曲线自动显示 ✅（用户报「切场应自动出」）：`buildRibbon` 内 `hasMood → ST.mood=true+save()`——载入/切场/syncLive 重建同一入口；逐场各自判定（有数据才出）；手关＝当场景内隐蔽，下次切场再出。实测：mood 关载入 s010→自动出（LS 同步）；切 s020→3 点；手关→切 s020 自动回。
- M5k-5 曲线渲染根因修复 🔥（放大目检咬出「属性全对但看不见」）：M5k-2 给 svg 设数值宽度时读 `inner.offsetWidth`——build 期 root 未挂载=0 → svg 宽 1px、整条曲线被视口裁掉（假数据断言只验 d/cx/cy，验不出）。修法＝宽度一律 CSS 100%（=strip 宽=inner 宽，1:1 用户单位）。教训：**渲染类改动必须过「截图+目检」——属性断言 ≠ 可见**（svg 默认 overflow:hidden，1px 视口全裁）。
- M5k-6 挂件带三角方向 ✅（用户指正）：状态向语义——收起 ▾（收在底下）/ 展开 ▴（升起来），初始与点击翻转两处同改；实测 ▾→▴→▾。
- M6 批6-1 命令面板（⌘K）✅：新模块 `web/js/cmdk.js`——Ctrl/⌘+K 开关（toggle）＋顶栏「⌘ K」芯片；三组结果：命令（全片/分组/平铺/台本/审计——复用点工具栏按钮，缺则提示）/ 场次（state.scenes→hash 跳转）/ 镜头（DOM tr.shot→`filter.jumpToShotById` 单点）；纯数字查询镜头组排前（防「03」被 s030 抢第一格）；↑↓ 选择、Enter 执行、Esc 关、点遮罩关；capture 键处理防串扰。实测：开关/toggle、「02」标签（镜 02 · 近景 ★★★★ · 5s）、跳场次 s020、平铺⇄分组、「03」跳镜闪烁、s030 场次优先，0 报错。
- M6 批6-2 导出 ✅：`server/core/export.py` + `GET /api/export?scene=s010&format=page|print`（app.py 特case 直发原始响应；`_send` 支持 extra 头）。静态分享页（米色宽版）/ A4 打印版（@page A4 横向 + thead 重复 + 行不跨页）；10 列单源自 fields.py（# 运镜 空间关系 摄影机 机位 动作调度 台词 时长 音频 导演备注）；节拍分组保留、未归节拍殿后（与图面同序）；总时长口径＝图面 fmtDur（floor + 秒补零）；DB 直读不受视图筛选影响；自含 HTML 零依赖。入口：场务 ⋯ 两项 + ⌘K 两条命令。实测：curl 矩阵（200/404/400 + Content-Disposition 双文件名）；菜单真点下载 ✓；⌘K 命令下载 ✓（两份文件名 UTF-8 生效）；两版截图目检 ✓；printToPDF → pypdfium2 栅格化＝A4 横向 842×595pt × 3 页、续页表头重复、无断行；pytest 222/222（+5 新用例）。
- M7 批7 收口 ✅ → **v1.0 冻结（2026-09-23）**：①性能实测（页内 hash 切场计时 + PerformanceObserver 长任务）：s010（34 镜）切场 ≈155ms；s900 大字场（160 镜·临时场，测后净删）首载 749ms / 切场 ≈230ms；挂件带开合 8ms；平铺⇄分组 43–58ms；全链无 >125ms 长任务、JS 堆 ~10MB——**无需优化**。②一条命令管线 `scripts/studio.sh {run|stop|status|test|migrate|export}`（run/stop/status 透传 serve.sh，test=uv pytest，migrate 参数透传）。③全库 JSON 导出 `scripts/export_json.py`（12 表逐行 + schema + meta，原子写 → data/exports/，studio.sh export 入口；+2 用例）。④工程核验：密钥扫描零泄漏（仅测试桩）、无大文件、硬编码路径仅迁移脚本原始来源默认值（一次性工具，保留）。⑤README 管线化 + DESIGN 状态刻印。net-zero：临时场删除后与快照全表 diff=0（含 settings 逐键）；pytest 224/224。

- **M2 主体 ✅ 收官**（批次 1–7 + 视觉专项 v1–v4b 全部落地）。
- **M3 提示词系统 ✅ 首轮完成**（M3-1 服务端域 / M3-2 前端拼装台 / M3-3 种子+自测+验收演示；HEAD 见 git log）。
- 待用户（早间复核）：种子块库过目（7 类 25 块，可删改）。
- 工具排 B 案 ✅ 已落地（见「刚做完」末条）。
- **审计修复四批**（用户点名 一/二/三/五，跳过 ◆立项 与 刻意边界）：批1 后端地基 ✅ → 批2 后端域 ✅ → 批1–4 ✅ 全部收官（审计 一/二/三/五 落地；◆ 立项区未动）。
- **M4 开工**：提案 v0.2 已拍板 → **M4a-1 ✅** → **M4a-2 ✅** → **M4a-2c ✅**（eb34725）→ **M4a-3 ✅**（用户实跑 8 条 → 处理 4 条 → 数据对账零差异）→ **M4b 提案 v0.1 → v0.2 拍板已收**（六条按推荐 + 新增「设置与配方中心」排批 3）→ **M4b-1 后端引擎 ✅**（2c4e28e）→ **M4b-2 前端 ✅**（cea2620）→ **M4b-3 设置与配方中心 ✅**（1c631db）→ **M4b-4 草稿档 ✅**（6368453）→ **M4 全链审计 ✅**（AUDIT-M4.md，只报不改）→ **修复批1 · 数据安全线 ✅**（de21316）→ **修复批2 · 审计线 ✅**（2953b61）→ **修复批3 · AI 通道线 ✅**（7374775+4159e51）→ **修复批4 · 前端与设置线 ✅**（cccde33）→ **二轮 · 第1步（L2+L1）✅**（7c82222）→ **二轮 · 第2步（L8+L4+L3）✅**（9fcf99e）→ **二轮 · 第3步（L7+L9）✅**（48242b6）→ **二轮 · 第4步（L6+L5）✅**（6610f74）→ **二轮 · 第5步（L10）✅**（4a941bc）——**二轮 10/10 全清 ✅**。

- **M5 打磨**（批1 ✅ `24e8373`｜批2 ✅ 表格操作流全收官｜批3 ✅ `ed453b0`+`ee13730`+`c360ec8` 右缘抽屉体系：N5 提示词抽屉 + B3 剧本抽屉 + N4 分组标识｜追问轮 ✅ `5599500` 闪修复 + `99fc913` 覆盖高亮 + 三段固定 + 块库卡）：批4 视觉核验·第一轮 ✅（五档）→ 批5 挂件带 ✅（B→v2.1 五件套）→ 批6 命令面板 ✅ / 导出 ✅ → ✅ v1.0 冻结（2026-09-23）。

## 下一步

- **第 2 波立项（35 条）**：批1 = S 系 15 条（已落：拆包双子、db 边界、api 收口、路由对账、S4 余项包、S2-L1 审计 key 迁移；余：场景上下文、canonicalizer、ai_out、任务簿——施工中）· 批2 = F 系 20 条（前端结构；随后）。
- **M4 修复·第一轮 ✅ 全收官**（批1–4）：**29/29 Bug + 修复包 P1–P12 全部修复**（批1 `de21316` / 批2 `2953b61` / 批3 `7374775`+`4159e51` / 批4 `cccde33`；见 AUDIT-M4.md 五表进度列 + 修复台账，☐ 清零）。**二轮 = §四 ◆立项 10 条**（2026-09-21 拍板：每条先出小方案 → 拍板 → 逐条施工；方案书五步分组）**全部收官 ✅**：第1步 L2+L1（`7c82222`）；第2步 L8+L4+L3（`9fcf99e`）；第3步 L7+L9（`48242b6`）；第4步 L6+L5（`6610f74`）；第5步 L10（`4a941bc`）——**10/10 清零**。AUDIT-M3.md / AUDIT-M4.md ✅ 已入库（2026-09-22）；双 Skill 拆分 ✅（`shotlist-studio` / `shotlist-studio-dev`）；GitHub 推送 ✅（`shaobaojia/shotlist-studio` 公开）；M5 打磨施工中（批1 ✅、批2 ✅、批3 ✅、追问轮 ✅、追问轮② ✅、追问轮③④ ✅（保存退役/已选字数/右键菜单/管理器退役；滚轮护栏/拖拽直操/就地编辑/大三角），见「刚做完」）→ 批4 视觉核验·第一轮 ✅（五档）→ 批5 挂件带 ✅（B→v2.1 五件套）→ 批6 命令面板 ✅ / 导出 ✅ → ✅ v1.0 冻结（2026-09-23）。
- 视图层余项 / 需求池：多选拖动 ✅、选区条扩展 ✅（M5 批2 已落地）；跨节拍框选已砍（用户 2026-09-22）；运镜「常用档+自定义」✅（批1）。
- 补齐一条命令管线（run / migrate / test / export）。

## 坑

- 老库教训清单见 DESIGN §11.1（生成器拼 HTML、硬编码、无分层——勿重蹈）。
- 本仓库在 NAS 共享卷：容器内新建文件后注意权限（保持 a+rwX 双向可写）。
- 凭证红线：一切 key / secret 不入库；飞书凭证在库外（feishu_config.json）。
- 大文件改动走脚本替换，禁裸 patch（老库坑条）。
- 多编辑补丁脚本：同一文件 N 处替换必须按「累积文本」逐处做、最后统一落盘——各自基于原文计算会「后写覆盖前写」，只剩最后一处生效（批1 实测踩中，git checkout 回滚重来）。
- CSS 豁免要压过既有规则：`#view.wrap-off table.shots td`＝(1,2,2)——豁免选择器需提级（加 `table.shots` 段至 (1,3,2)）否则被压、接缝灯仍被裁（首版 (1,2,1) 浏览器实测才发现）。
- `#toast` 盒懒创建（首次 toast 才建）——页面级断言「没弹过 toast」直接判 `getElementById('toast')` 为 null 最准（读文本会读到陈迹）。
- 菜单条目 textContent 带「✓」前缀（格子菜单/动作菜单）——脚本点击须先 strip `^✓\s*` 再比对，否则「点不中且零报错」（批3 实测踩中）。
- AI 任务簿两类任务字段形状不同（草稿 scene 有 beats/shots、prompt 无）——轻载/裁剪类改动一律 `job.get(k) or []` 取值，勿直取键（批3 测试先抓出 KeyError）。
- 接口 handler 与 core 函数同名易混（`ai.test`）——core 侧已改 `probe`；新增 core 函数先查 handler 名簿再定名。
- 老库页面服务 :8089 保留运行（迁移对照用）——管理：bash /opt/data/bin/serve-shotlist-archive.sh {start|stop|status}（同款脱会话守护，顺带治它自退老毛病）。
- 老库表视图规格出处（移植对照用）：storyboard-shotlist 仓库 references/frontend-notes.md、templates/feishu-backed.html、read/done/*_feishu_backed.html；渲染器 web/js/cells.js。
- 数据注意（M2 编辑前先定规则）：『景别』复合串内含焦段/景深信息（列上已不再显示景深，但数据里仍在），与独立 focal/dof 字段是两份来源；全量比对 46 镜仅 2 例不一致（均 s010：镜02 串内 50mm vs 独立 35mm；镜22 串内 35mm vs 独立 50mm；疑似当年单边手改）。两份均为原始数据、未动；M2 需先定同步/分工规则。
- 迁移实测：①「节拍属性」= 逐镜字段（已落 shots.shot_fn）；② 镜24 beat标题（被确认屈从）与序号（6）不一致（已按序号归组、标题取多数，留档）；③ s010 节拍明细（外界动作/反应/闭环/说明）已从 s010_第一场_分析.md 补录。
- s030–s080 现状：价值弧线已入库、镜头为空（待创作填入）。
- 编辑控件多行判定：按「类型集合 + 键集合」分开判（blocking 的 type 是 text、不是独立类型；单一集合混判会踩坑——动作调度开出单行框）。
- 撤销走同一写路径 → history 同时有「改」与「撤销改」两条记录，属预期（痕迹 = 全操作留痕）；撤销栈是客户端内存栈，刷新页面即清空（数据层不依赖它）。
- 写白名单与类型判定以 fields.py 单点为准；前端多行判定（table.js MULTILINE_TYPES / KEYS）与之对齐，改字段时两处一起看。
- 每日快照落 data/snapshots/daily/studio-YYYYMMDD.db（幂等；snapshots 表暂未启用，以文件为准）。
- 服务运行：**bash scripts/serve.sh {start|stop|status}**（官方姿势）——脱会话守护（setsid 独立会话 + 崩溃 2 秒自动重拉；日志 data/serve.log、pid data/serve.pid）；容器重启后手动跑一次 start。换端口：SHOTLIST_PORT 可覆盖。**本容器无 fuser/ss**——异常清退用 /proc 扫描匹配 server/app.py 后 SIGTERM；改 py 后走 serve.sh stop → start（不杀旧进程会让新进程静默退出、旧代码继续服务——已踩坑）。
- **常驻服务禁用 Hermes 后台进程方式跑**：会话收拢/应用动作会连带 SIGTERM 回收（2026-09-19 两服务同死实证）——一切常驻走 scripts/serve.sh（脱会话）。
- 拖动 index 语义：目标节拍「去掉被拖者后」的插入位；move_shot 重排全场 position（0 基）；拖动不改镜号（镜号整理是独立按钮）。
- 摄影机归一化规则：任何对摄影机/焦段的编辑 → 串重写为纯景别（内嵌焦段/景深迁入独立字段，仅在字段为空时迁）；展示=『串内嵌优先、无则字段』——历史行不改动。
- 表单件单点（L9）：新表单控件走 `web/js/formkit.js` 的 `fieldRow/collect`（行骨架类名 `.as-p/.as-p-k/.as-input` 定版）——别再手搭行骨架。
- 「去改」目标列随问题下发（`issue.field`，注册表 RULES 单点）：前端已无 FIELD_HINT；改标题/换列只动 `server/core/audit.py`。
- 写入出口三件（L7）：单格落定走 `edit.commitField`、批量写走 `selection.batchWrite`（写口可注入 `opts.write`）、编辑面文本范围替换走 `hbedit.replaceRange`——新功能别再重抄。
- AI 单格接受两条回显分支（编辑面开=就地+基线 / 关=`refreshShotCell` 重画）——动接受链两分支都要过测（二轮3 删 import 误伤 renderCell、靠非编辑面实测咬出）。
- 浏览器验证纪律：同 URL `goto_url` 不算重载（旧 JS 模块残留会假失败/假通过）——硬刷新走 `Page.reload {ignoreCache:true}`；表重建后旧节点引用即游离，断言必须重查活 DOM。

- 镜号跳转数字归一：'6' = '06'（filter.js norm：纯数字去前导零、含字母转大写）；曾踩坑 '6' 匹配不到 '06'。
- 服务端写接口全集：/api/update（字段）、/api/batch（批量）、/api/move（拖动，含 scenes）、/api/scenes/<no>/renumber（镜号整理）、/api/duplicate（副本：shots/beats/scenes）、/api/delete（删除：多行镜头 / 节拍 with_shots / 场次级联）、/api/create（空镜头/空节拍/空场）、/api/restore（撤销回插：shots/beat/scene）、/api/lock（锁定本场：场次快照 + 标记）——均走 core/ops.py + 每日快照 + 痕迹。
- **/api/delete 形状**：`{table:'shots'|'beats'|'scenes', id/ids}`——`table` 缺省会静默按 shots 走（曾因此误删一行真数据，走 /api/restore 原样回插）。调删除前先对 handlers.delete_row 现读，别猜形状。
- **还原必须复用原 id**（_insert_restore）：撤销栈按原 id 寻址，恢复若换新 id 会让栈里更早的闭包悬空（实测踩中：副本→删→撤→再撤 失效）；被占才退回自增。各表 id 为 AUTOINCREMENT，新行不会抢占已释放 id。
- 结构操作编号规则：镜头中插=前邻字母后缀（02→02A），追尾/「＋镜头」=数字顺延（34→35）；节拍副本=数字+字母（1→1A）；场次副本=下一空场号（步进 10）。都不重排其他镜号（连续性归「整理镜号」）。
- 删除哲学（三层统一）：不做确认弹窗——即时删 + toast + Ctrl+Z 完整还原（内容/位置/编号/提示词归属全带回）+ 痕迹 + 每日快照三层兜底；节拍删除=镜头落「未归节拍」；场次删除=整场级联（依赖 FK ON DELETE CASCADE——rw 连接已开 pragma；外部脚本直连记得自己开）。
- 场号可编辑（场头点击即改）：唯一性双护栏（前端查 state + 服务端 /api/update 校验）；路由对场号放开（decodeURIComponent），改名后 hash/地址栏同步。写白名单在 core/fields.py（改字段记得看它）。
- 测试手势经验：合成 mousedown 不建选区——选区由「拖出格子的 mousemove」或「Shift 续选（需已有选区）」建立，单纯点击只开编辑。E2E 测选区要按此构造。
- 筛选恢复行时**必须同时恢复详情行**（tr.detail display）——只处理 tr.shot 会留下卷展失灵（输入框清空路径踩过；按钮路径走重建所以不显）。
- 摄影机列显示回落已实现：cells.js cellContent(type, value, extra) 第三参 {focal}——串无内嵌焦段时显示独立字段；调用点在 table.js renderShotField（目前唯一，新增调用别忘传）。
- 原生 select 已全面退役（单元格单选/复合槽位/详情区都走 menu.js 浮动菜单）——不要再往编辑路径加 select。关闭协议：弹层内 mousedown 不算「点外」；Esc 先给菜单（表单 onEsc 里 menuOpen() 时让行）。
- 剪贴板限制（实测）：局域网 http 下 isSecureContext=false、navigator.clipboard 不可用；复制/剪切统一走 clipboard.js 的 writeClipboard（内部 execCommand 兜底，需在用户手势上下文内）；粘贴无法程序化读取 → 「Ctrl+V 待命」模式（6 秒超时、Esc 取消）。
- 拖动把手 = .drag-dots 真实元素（非伪元素、非 td 属性）；td.cell-shot_no 不再 draggable（整格可拖会误触拖动、点编辑发飘）。
- 拖动落点语义（补）：放回自己（自己行/自己节拍）= 原地不动——客户端直接 return 不发请求；悬停自身行不显示落点指示。若不判自身，ti===-1 会误入『插到末尾』分支（乱跑根因）。
- 拖动把手位置：.drag-dots 在 toggle 格（行首）左侧、悬停显示；靶区含 padding（≈10×18）。td.cell-shot_no 与把手无关。
- 卷展性能：稳态开/关 17ms/帧干净；首开有一次性冷成本（偶现 ~50ms 级）——scheduleWarm 空闲预热兜底（只处理折叠态、同任务还原、不闪）。改动详情区结构时记得预热逻辑仍适用。
- 行首把手对齐：td 垂直对齐=top（行内文本都在第一行线）；.drag-dots 必须 top:10px 而非垂直居中——否则高行里把手与箭头/镜号错位 ~20px。
- 选区坐标制：sel.rows = **整表行索引基**（不是选中子集）；一切「对选中行操作」必须经 rectOf() 取 r1..r2 范围内的行——applyFieldValue 曾踩：框选 2 镜写成了整表 4 镜（已修；数据经撤销还原）。
- 框选/扩选**限定单张表**；跨节拍表拖拽无效（v1 有意；已入需求池）。
- 选区遇 筛选 apply / 排序 / 重绘（paintScene）一律清空（有意）——这些路径上加功能时注意联动。
- /api/batch：上限 400 项；逐项校验（error 只跳该项不中断）；单次 commit；走同一白名单/痕迹/每日快照。
- 走格语义：Tab=右移、Shift+Tab=左移（均保存后走）；单行 Enter=保存+下移；多行 Enter=换行、Ctrl+Enter=保存（不跳）；表尾/表首停住；摄影机格走格=开复合控件。
- 复制依赖可信用户手势（execCommand 兜底）：合成 el.click() 测试会报「浏览器限制」，真人点击正常——验证复制必须用 CDP Input.* 可信事件。
- 行副本编号规则：数字基 + 首个空闲字母后缀（05→05A、17A→17B）；「整理镜号」会把字母号一并顺排成连续数字（预期行为）。
- duplicate/delete 痕迹字段为 create / delete；/api/delete 已泛化三层（批次 6：多行镜头 / 节拍 / 场次），无弹窗即时删 + Ctrl+Z 完整还原。
- 节拍概述 = beats.beat_action（渲染在节拍名与镜头表之间那一行）；空值也渲染编辑位（hint 提示），非条件渲染——改节拍区结构勿退回「有值才出块」。「未归节拍」伪节拍（无 id）不出概述位。
- 锁定语义：留底（快照 JSON + snapshots 行 kind=locked）+ 标记；不限制编辑；解锁不删快照文件。快照 = data/snapshots/scenes/<场号>-<时间戳>.json；单测传 snap_root=tmp 防写真库。
- prefs 结构（localStorage shotlist_prefs_v1）：{ wrap, hidden:{key:true=隐藏}, widths:{key:px} }——旧版 {prompt:false} 自动迁移为 hidden.prompt；列宽/显隐全由它驱动（scene.js loadPrefs/savePrefs）。
- 痕迹回看面板为只读（/api/history 按场过滤）；field 中文映射在 history.js SPECIAL_FIELD（drag/create/delete/locked），其余走 fields 字典 label。
- 批量脚本改前端锚点：含括号的普通替换勿开 regex 模式——括号被当分组符会导致锚点匹配不到自己（M2-7 施工踩过）；普通替换走纯文本，正则只留给需通配的整段替换。
- 行副本不拷 prompt_group_id（提示词归属）——E2E 对账「字段全拷」按此口径（13 列）。
- 「案头」皮肤（昼案）= web/css/app.css 全量重写；改界面 / 加界面前先读《视觉宣言.md》——军规：视觉不得删减任何既有能力（列对齐 / 排序 / 列宽 / 直编 / 框选 / 拖动 / 撤销）。过程稿在 Hermes 工作区 /opt/data/cache/themes/（concept-desk*.html），不入库；库内定稿参考 = design/案头-A卡条-昼案.html。
- 顶栏高度走 CSS 变量 --topbar-h（main.js syncTopbarVar 实测回写；:root 45px 为兜底）——场签（.scene-head）吸顶 top 就是它；以后任何吸顶元素照此对齐，别写死像素。
- 视图模式 prefs.viewMode（'group'|'flat'）：平铺与排序共用 paintScene 的 flat 分支（beatCol:true）；拖动与「＋」入口在平铺下关闭；seg 开关在 viewTools（scene.js）。
- 「冻结」变量链：--topbar-h（main.js 实测）→ .scene-freeze（--freeze-h 由 scene.js syncFreezeH 回写）→ 平铺表头（.table-wrap.holdhead th 用两变量 calc）。改吸顶区结构不用手调高度（会自动回写）；但新加任何吸顶元素都要停在这两个变量之后。
- ⚠️ .table-wrap 默认 overflow-x:auto = 滚动容器，会吃掉内部 sticky（表头冻不住的老根因）；平铺冻头靠 .holdhead{overflow:visible} 放回文档滚动流。
- 通栏布实现细节：100vw + margin-left:-22px（#view 侧 padding，改它要同步）+ html/body overflow-x:clip；clip 代价＝极窄窗口下平铺表不再整页横滚（桌面无感）。全片页标题带未动（未点名）。
- 布面文字对齐：标题/元信息 45px（＝卡纸药丸首字线 45）；工具排 36px（＝按钮盒线；按钮无可见边框、按「字」对齐——字落 47 与标题同线）。经 4 变体（22/36/45/49）裁切对比 + 像素取证 + DOM Range 首字三方复核；引用文字 49、首节「▸」38 为元素自身层级，不追。改卡纸内衬（.beat padding / 药丸 / td）时留意耦合。
- M3 写路径：提示词 /api/prompt/{set_text|merge|detach|split|restore}（core/prompts.py）；块库 /api/blocks（action 分发）。都走每日快照；块库操作不记痕迹（库不是场数据）。
- 拼装台单例：同一时刻一个 editing composer（activeBox），切换先收旧（带保存）；结构操作 refresh 重绘后 focusShotComposer 重新落座。提示词格重复点击＝开/收切换。
- Ctrl+Z 全局守卫含 INPUT/TEXTAREA/SELECT——编辑面里 Ctrl+Z 是文本撤销（有意）；测全局撤销先 blur（合成事件不移动焦点=测试坑）。
- 并组后自动落座首镜编辑面（别再多点一次提示词格——那是「收起」）。
- 种子块以正文识别（无名称字段）；块库变更经 onBlocksChange 自动重绘热盒；管理器在右侧浮动（z-index 45，与痕迹面板可能叠，介意就只开一个）。
- history 实体＝prompt_groups；字段 text/merge/merge_in/split/detach/restore/create 的中文映射在 history.js SPECIAL_FIELD。
- 工具排结构（B 案，2026-09-20 定）：行内只放「看/找」（分组|平铺 · 筛选 · 跳转 · 未写提示词 · 清除）＋吸右「场务 ⋯」抽屉（整理镜号/锁定本场/自动换行/列设置/痕迹）；新增工具优先考虑抽屉，别再加回行内混排。自动换行现为抽屉菜单项（原行内 checkbox 已撤，prefs.wrap 语义不变；mkBox 已删）。
- 拼装台编辑面点击守卫（hotbox.js）：编辑面内部的点击绝不能触发 activateBox——否则光标被拽回文末（2026-09-20 实测：误插骨架块 xN + 用户「光标锁死」反馈）。普通单元格编辑（edit.js attachEditable）已有同类守卫（host.querySelector('.cell-editor')）——拼装台当时漏了，新做交互面照此自查。
- 提示词类 E2E 必须验「光标机制」四件：可信点击落点 / 打字落点 / 块插入落点 / 拖选——只验值（保存内容、撤销内容）不验光标=漏网（本条即教训：20 项单测+全操作自测都没抓到，因为都在测「值」）。
- 改前端 JS 后验证必须真重载：同 hash 导航（#/s010 → #/s010）不触发页面重载，测到的仍是旧 JS——用 Page.reload(ignoreCache) 或换 hash（2026-09-20 踩过：修完复现，实为旧 JS 在跑）。
- 测试纪律：写操作一律只在临时场。s010 上哪怕「只读」打开拼装台也危险——点另一格/点外收起=自动保存（commit），会把编辑面内存值写库（2026-09-20：一次切镜收起把测试块写进组3；同理用户误插也是这样进库的）。
- M3.5 拼装台手感轮（2026-09-20 落地）：①编辑面右键独立菜单＝添加到提示词块…（选中→分类二级）/ 插入提示词块…（分类→块两级、插到光标处、代入占位符）/ 拷上组直选 / 复制 / 剪切 / 全选——菜单走 {x,y} 坐标锚点（menu.js 原生支持）；Chrome 右键天然落光标，浏览器默认菜单已拦。②编辑面撤销栈（hotbox.js hb* 系列）：插入块 / 剪切 / 打字片段（600ms 分段）入栈，Ctrl+Z 撤、Ctrl+Shift+Z / Ctrl+Y 重做——⚠️ 程序化写 value 会清空 textarea 原生撤销栈，编辑面内撤销必须走这层自研栈。③块拖动：管理器行手柄（行间定位＋跨类；position 语义已入 block_update 后端）＋热盒条 chip → 分类芯片（末尾插入）；两者都是「拖=管理、点=原行为」。④撤销补齐：存块 / 管理器建块 / 建分类 / 改块文 / 置顶 / 上下移 / 换类 / 分类改名与移动全部 recordUndo——块库操作全可 Ctrl+Z。⑤修复两处「连坐」：drag.js 容器级 dragstart 原把非行把手的 dragstart 一律 preventDefault（芯片拖拽被静默杀死：只有 start、无任何后续 drag 事件）——改为「非本系统源直接放行」；拼装台 ta blur 守卫（失焦且新焦点不在 .hotbox 内 → 收起+提交）会因 chip 不可聚焦误触发——chip 加 tabindex=-1 让焦点落芯片自身（守卫放行），配套 .block-chip user-select:none / :focus{outline:none}，操作后 backToEditor() 还焦点（Esc 随手可用）。⑥管理器新增搜块过滤（.bm-search，空结果提示）。
- 拖拽自动化测试纪律（2026-09-20）：CDP 合成鼠标能起原生拖拽（表格内／浮层内都行）；三个坑——(a) 最后一次移动后太快松手＝dragover 未送达、drop 判取消：到点后抖动 2~3 次并轮询到指示类再松手；(b) Esc 递不进原生拖拽循环，「用 Esc 取消拖拽」在自动化里不成立（会照常落下）——别把 Esc 当取消断言；(c) 判断「拖死」看 dragstart/dragover/dragend 三计数：只有 start、无后续＝被连坐或源被销毁。另：body 级浮层（#block-manager 等）的拖拽不受表格容器守卫影响；表格容器内新增可拖元素时必查 drag.js 源头守卫。
- M3.5 回改轮（2026-09-20 下午）：①右键菜单去「插入提示词块…」（与热盒条块库重复）；②「声明段」概念退役：右键/脚部的拷段项全去，拷上组＝整组全文直拷（脚部按钮点即拷、不再弹菜单）——提示词单一概念，不拆声明段；③热盒条块↔块拖拽＝换位：悬停目标块前/后半（.drop-before/.drop-after 内阴影指示），同分类换位、跨分类连类搬；④moveBlockTo / siblingList 上收到 blocks.js 导出，管理器与热盒条共用（blockman 本地副本删除）；⑤【撤销可达性】编辑面 Ctrl+Z 在编辑栈为空时让位给全局撤销（peekUndo 判型；custom 撤销体自带刷新→不刷，field/renumber 由让位侧 ctx.refresh）——修「拖完块按 Ctrl+Z 静默无反应」；⑥块移动 toast 统一带「（Ctrl+Z 可撤）」。
- 审计卡宽表坑（M4a-2c）：卡横跨整张表 → 宽表（s010≈1550px）上卡里的动作钮/关闭钮落在可视区外（横向滚动才见，预览窗窄更甚）——**窄表夹具测不出这一类**，宽表验收必查。修：`tr.audit-card-tr .audit-card { position: sticky; left: 0; max-width: min(100%, calc(100vw - 110px)); }`（横滚时卡吸左、恒完整可见）。
- 双连接路径必须同源（M4a-2d）：`JobManager` 的「读规则」与「跑任务」必须走同一条连接来源（`connect_factory`/默认库看齐）——曾因 start 走默认库，测试夹具被线上设置污染（9 != 10）。
- 换场状态重拉不走限流（M4a-2c）：`onPainted` 的 fetch 带 2.5s 限流闸门——换场（scene 变）必须 force 重拉，否则回程那次拉取被吃掉、灯/徽标永久不回（限流只配给同场重绘）。
- 审计灯几何约束（M4a-2）：灯钉在 td.cell-toggle 内绝对定位（left:1px），**不能探出表格左缘**——table-wrap 是 overflow-x:auto 滚动容器，向左溢出直接被裁（且无左滚可见）；接缝灯 top:100% 落进 border-spacing 9px 行缝；有灯行拖点经 .has-lamp 让位（left:10px/top:24px）。调灯位置先念这条几何。
- 拖拽自动化三坑补遗（2026-09-20）：(a) CDP 原生拖拽偶发「起了但目标不接受」（时序抖动）——货架同款序列（悬停→按下→小抖→密步→到点抖动→轮询指示→松手）重试可过；判定接线可用「拖拽进行中合成 dragover/drop」（handler 无 isTrusted 依赖，合成即达）+ dragstart/dragenter/dragover/drop/dragend 五计数；(b) 窄视口（1280）下拼装台脚部按钮中心可能在视口外（elementFromPoint=null、可信点击落空）——自动化用 1600 宽视口，或按钮用程序化 .click()；(c) 撤销时要保拼装台：不要 blur 后按（失焦守卫会收起），焦点留在编辑面（空栈自动让位）或丢给热盒内元素。

- 吸顶冻结区会「吃」单元格内浮标（M4b-2）：场景头（.scene-freeze）吸顶后格顶区域不可点——✦ 钉格顶时若格顶被压住，点击落到冻结头（编辑器失焦关、菜单不出）。定稿 ✦ 钉**格右下角**（bottom:0——格子只要还露着，右下必然可见可点）。泛化：往表格/单元格放绝对定位浮标，先问「吸顶区/滚动容器怎么裁它」。
- 浮层入「点外监听」豁免清单（M4b-2）：selection.js 点外清选区 / hotbox.js 点外收编辑面各有豁免名单——AI 预览卡（.ai-diff/.ai-cmd）须逐条加入；新增浮层时对全体「点外监听」grep 一遍（selection / hotbox / menu），漏一个＝交互面被误关。**（批4/L1 起已单点化：浮卡挂 `.float-card` 即入名单，见坑区后条。）**
- AI 接受回显分叉（M4b-2）：编辑器开着时接受不能 renderCell（会把手里的编辑器 DOM 拆掉）——编辑器在→只改 ed.value（模型已 onLocal）；编辑器不在→renderCell。单格卡接受要保住编辑器焦点：靠卡级 mousedown preventDefault。
- 写痕溯源法（2026-09-21 实测）：判断某段写痕是否代理所为 → 查 Hermes state.db messages 全库时间窗——代理每步动作必留消息，窗口内零消息=人操作（本案：s010 一段改-回对即用户手测）。
- 验证 AI 链路别赌模型纪律（M4b-3）：给配方塞「最高优先级·输出必须恰好为 X」的钉值，模型照样走正常改写——「配方现读生效」不能用模型输出当证据。正解＝机械可观测：base_url 暂指本地 mock（http.server 起在冒烟进程里），截获服务端实际发出的 messages[0].system，与盘上文件逐字节对账——三连（改→恢复→再改）逐次中靶，模型彻底出局。凡「服务端给模型喂了什么」类验证，一律走此路。
- 按时序命名的产出文件用单源时间戳（M4b-3）：备份名「秒_微秒」若秒与微秒分两次取时，毫秒回绕会把后写的排到前面（测试抓到排序倒挂）。修：一次 time_ns() 推出秒+微秒（ns//1e9 → strftime；ns%1e9//1000 → 微秒后缀）。
- 桩测过了≠真调会过（M4b-4）：测试桩返回 str、真 ai.chat 返回 {text,…}——只挂桩的模块真调必炸（冒烟当场抓「'dict' object has no attribute 'strip'」）。新 AI 模块两道保险：单测加一条「字典回包」用例＋冒烟必须真调；归一照 rewrite 口径（reply["text"] if isinstance(reply, dict) else reply）。
- ⚠️ 子串选择器会咬人（批4 实测）：UI 自动化点按钮用 `[title*="设置"]` 匹配「审计设置 ⚙」——结果咬中「跑审计」（title＝"按审计设置跑全部启用规则"）→ 误触发 s010 一次真实审计重跑（9/9 跑完、issues 对账正常刷新、零数据损坏）。铁律：UI 自动化定位一律「作用域 + 精确文本/类名」，禁用裸子串 title 匹配。
- 悬浮卡必挂 `.float-card`（批4/L1）：点外判定已单点化——`ui.isFloatTarget()` 认 `.float-card/.menu`；新增任何浮卡根元素先挂类，否则点卡＝清选区/收编辑面/关相机表单（三处监听全走单点，改时别再手写豁免清单）。
- 编辑基线同步钩子 `.cell-editor._syncBaseline`（批4/L9）：AI 接受落库后必须同步编辑器基线——否则收起时「nv !== original」再发一次同值写 + 多压一条撤销（修复前实测 edit 路径 +1 次 /api/update）。外部改值路径落库后照此调用。
- `core/ai.save_config` 语义（批4/M14）：文本字段（ai_provider/ai_model/ai_base_url）**空串 = 清回默认**（删 settings 行）；api_key 空串 = 不改。`data` 用全键名；api 层做短键映射（provider→ai_provider）。写用例别用短键（踩过：静默全跳过、断言假红）。
- ESM 语法自检：`node --check foo.js` 按 CommonJS 解析会误报 import——复制成 `.mjs` 再 check（批4 用此法一次验 13 个前端文件）。
- 审计参数数值输入（批4/L3）：空值/非正整数一律拒绝并回滚显示（`/^\d+$/` + ≥1），不再 `Number(x)||0` 折算——曾「显示 0、实跑 3」（服务端 `or 3` 兜默认）。
- 多配方分派的判别键取标题级唯一串（M4b-4）：测试桩按 system 文本分派时，用正文词「镜头行」会串派——镜头行配方正文里含「节拍骨架」词。改用配方标题（草稿·节拍骨架 / 草稿·镜头行 / 草稿·组级初稿）。凡一处分派多配方的桩都按此选键。
- 任务簿与契约测试纪律（L1/L2，二轮1）：①`_gate_acquire()` 与 `_register()` 锁内必须成对（认领成功才登记挂闸）——测试直接 `_register` 会误挂名额；②mock `ai.get_config` 必须给全真字段（`api_key` + `has_key`）——只给 `has_key` 会被 `require_key` 误拦（test_api 三红根因）；③AI 桩一律返回真形状 `{"text": …}`（归一宽进，形状不再分叉）。
- digest 单点（L8，二轮2）：场线/速览/短标签表全在 `core/digest.py`——审计体 `colon=": "` 与创作体 `colon=":"`、`terse` 开关是历史口径原样保留；改进 digest 样式只动这一处，勿在域内改回。
- 规则注册表（L3，二轮2）：`audit.py RULES` 是唯一点（key/kind/recipe/params schema/desc）；**title 不再当键**（引擎字典全 slug）；老库跑 `scripts/seed_audit_rules.py` 自动补 key 列 + 回填（幂等，可与常规种子同跑）。
- 追加行原语（L4，二轮2）：`ops.append_beats/append_shots` **不自行 commit**（调用方收尾保持一步事务）——勿顺手加 `con.commit()`；编号顺延规则（beat `str`、shot `%02d`）改动前先对拍 draft 落到流程。
- 浮层治理（L6，二轮4）：互斥走 `float.js` 注册表——`floatEnter(layer, close)` 开卡（同层先关旧）/ `floatLeave` 关卡销号 / `floatClose(layer)` 全量闭合（重绘入口）；三层：`'panel'` 设置↔审计设置 ｜ `'ai'` 单格↔批量（含文本卡） ｜ `'draft'` 场次草稿↔组级初稿。新浮卡＝挂 `.float-card` + 按层登记，别手写互斥对；**审计面板曾漏挂 `.float-card`（批4 遗留，二轮4 已补）——点面板曾清选区/收编辑面/关相机表单。**
- 外壳基类（L6，二轮4）：面板类浮卡骨架走 `float.panelShell({id, headCls, bodyCls, title, onClose, fillHead})`；任务卡（ai-diff / ai-cmd）走 `aicard.taskShell({cls/id, headCls, bodyCls, footCls})`（内含 mousedown 防失焦守卫，带 textarea 的卡别套）。新卡别再手搭骨架。
- 任务轮询单点（L5，二轮4）：任务类轮询一律 `aicard.pollJob(fn, {interval, limit, alive, tolerant, onTick})`——间隔/上限/取消/错误契约一处；卡生命周期用 `cardLife()`（closed + onClose 清理钩子 + alive 给轮询），别再造 closed/aborted 双标记；「取消」＝关卡后轮询静默退场（不打扰已付费任务）。
- UI 自动化定位纪律补遗（二轮4 实测）：①点击前 `scrollIntoView` + `elementFromPoint` 双校验（横滚/纵滚下 rect 可能为负＝点到视口外，曾 x=-211 / y=-1498）；②点外浮层会整列遮住目标格（审计面板 x≥806 覆盖区），先看落点元素；③显隐断言用 `getComputedStyle().display`——「元素存在」不算数（selbar 常驻 DOM）；④文本编辑面走 **blur 关闭**（非 mousedown），合成事件测不出、须真实点击；绑在特定元素上的键事件要 dispatch 到该元素本身。
- 薄层守卫顺序纪律（L10，二轮5）：api 薄层「参数守卫先于写连接」——新增写操作自查守卫落点（`api/prompts.py` update 投影空检查曾落在连接之后，已前移）。
- 「咬合法」验证测试网（L10，二轮5）：①guard-only 分支用 dead-db 断言（`mock.patch.object(core_db, "connect", side_effect=AssertionError)`）——既证明坏请求不触连接、又防测试误连生产库；②测试网有效性验证＝把旧实现临时降级跑一把必须翻车（实操：prompts 换回旧守卫顺序 → `test_blocks_guards_pre_connect` 即红）。
- presets 浮条三坑（M5 批1）：①绝对定位浮条在窄容器（72px 格）里收缩拟合把宽度塌成容器宽 → 显式 `width:max-content`（配 max-width 封顶）；②`.table-wrap` 双轴 auto＝纵向也裁——表底行浮条翻到上方（`.cell-presets.above`）；③按钮 mousedown `preventDefault` 保输入焦点，防 blur 提前提交。
- 交互回调里量 DOM 画浮层 → 必「稳定帧复绘」（M5 批1）：点击同步窗口读到的 rect 可能是重排前值（N2 实测：分组模式首绘偏 ~6.9px、不自愈；平铺不复现）——`selection.paint` 已内建 rAF 二次补绘（queueStabilize）；新做同类浮层照此。
