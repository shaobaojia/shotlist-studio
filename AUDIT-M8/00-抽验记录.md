# AUDIT-M8 抽验记录（主控一手，收尾前）
- 日期：2026-09-24 凌晨 ｜ 基线：`0ca3eb0`（抽验时复核：HEAD 未变、工作树仅 `?? AUDIT-M8/`，冻结完好）
- 方法：主控代理亲读代码 + 定点证据（不重跑全部探针；审阅员探针证据见各批报告与 probes/）

| # | 条目 | 出处 | 判定 | 一手证据 |
|---|---|---|---|---|
| 1 | refreshCurrentView 陈旧回包写错路由 | F1-B1（高危） | ✅ 确认 | `scene.js:65-68` renderScene 有 hash 守卫；`:115-123` refreshCurrentView 无任何在飞/哈希复检直接 `paintScene`（不对称=实锤）；`main.js:284-285` 撤销后亦走此路径 |
| 2 | Ctrl+Shift+Z 执行撤销 | F1-B2 | ✅ 确认 | `main.js:280` 仅判 ctrl/meta + key==z，无 `e.shiftKey` 排除 |
| 3 | 每日快照并发竞态 | S1-B2 | ✅ 确认（机制） | `ops.py:59-70` dest 缺失时多写者共用同一 `*.tmp` 名 backup+`os.replace`，无锁无 O_EXCL；同行并发首写必撞（审阅员 4 线程复现 500） |
| 4 | 「跳过=干净跑」假熄灯 | S2-B1（中高） | ✅ 确认（机制） | `audit.py:297-301` body=None → `return []`；叠加 reconcile 空结果 fixed 化（探针 P1 复现） |
| 5 | JSON `true` 当 1 用 | S3-B1 | ✅ 机制确认 | `prompts.py:20-22` is_id 明确排除 bool（注释点题）；全库 24 处手写 `isinstance(...,int)` 未排除；实测 `isinstance(True,int)=True` → 触发需输入含 true（防御缺口实锤） |
| 6 | aiwrite 两处 pollJob 缺 tolerant | F4-B1 | ✅ 确认 | `aicard.js:25` 契约在；`draft.js:94/223` 传 `tolerant:true`；`aiwrite.js:253/421` 未传 → 瞬时抖动=假失败而付费任务继续，重试=重复付费 |
| 7 | migrate --reset 先删库后读输入 | S4-B2 | ✅ 确认 | `migrate_feishu.py:85-88` 先 `unlink()`，`:90-93` 才读 storyboard/analysis/弧线；输入缺失→异常退出且旧库已删；裸 sqlite3 无快照 |
| 8 | batchWrite 撤销记默认表 | F2-B1（中高） | ✅ 确认（结构） | `selection.js:330-331` 撤销回填全量使用捕获的默认 `table` 变量（逐项表仅前向写路径；以 02 号报告 B1 探针为准） |
| 9 | 全库 JSON 导出含明文密钥 | S4-B1（中危·数据安全） | ✅ 确认（含一次纠偏） | 证据链：`core/ai.py:15 KEY_FIELD="ai_api_key"` → `save_config` 明文 upsert `settings`（schema.sql:157-160）→ `export_json.py:20-38` 遍历 sqlite_master **无排除名单**。**存量实证**：真导出件 `data/exports/studio-20260923-223559.json` 的 `tables.settings` 确含 `ai_api_key`（非空，值未读取）。纠偏：主控首查误用顶层 `settings` 键（实为 `tables.settings`）致假阴性，修正后确认。 |
| 10 | Ctrl+Z 后全片页被场级覆盖（全片页卡死） | F1（质量 B1 附） | ✅ 确认（机制） | `main.js:285` 撤销成功即 `refreshCurrentView()`，而该函数不校验当前路由=全片页 `#/` 时亦按 `currentData` 重绘 |

**结论：10/10 无翻案，无误报翻出。**

## 密钥痕迹清洗（伴随动作）
- 审计缓存与仓库报告内，密钥形态串（`sk-…` 掩码/全形态）**27 文件 80 处**已统一替换为 `[REDACTED-KEY]`；抽查上下文均为密钥值位置，无普通文本误伤；残留 `sk-` 命中经查为字面量前缀描述（非密钥）。
- **存量风险留档（待用户定夺）**：① 真导出件 `data/exports/studio-20260923-223559.json` 含真钥（本地文件，未外泄）；② `probes/` 内若干库副本亦含（同机 scratch）；③ 建议：修脚本（导出排除 `settings`）→ 清理/重建存量导出件 →（可选）轮换 key。

**优先级提示**：6（付费风险）与 1（高危）建议明早第一批处理；3/7/9 为数据安全类，修复成本都小。
