# 12 · 第二波台账（◆立项 35 条）

**口径**：第 2 波按两批推进——批1 S 系 15 条（服务端结构收口）、批2 F 系 20 条（前端结构）。逐刀施工、每刀全量回归、每刀入账。
**基线**：AUDIT-M8 修复侧全清（248/248）之上；「刀」= 一个自洽的施工单元，刀内可含多条立项。

## 批1 · 刀1：拆包双子（S1-L1 + S3-L1）✅

**改动**：
- `server/core/ops.py`（854 行）→ `core/ops/` 包：write ~170 / snapshot ~101 / structure ~438 / numbering ~58 / restore ~73 + `__init__` 全量重导出（63 名）；`from core import ops` 调用面零改动。
- `server/core/prompts.py`（486 行）→ `core/prompts/` 包：groups ~258 / blocks ~164 / text ~31 + `__init__` 重导出（33 名）。
- 搬迁法：块级切片 + 自动依赖分析（注释/字符串剥离防 docstring 假依赖）+ 环检测；断环归置：prompts 的 `MAX_BLOCK_TEXT`/`MAX_CAT_NAME`/`is_id` → text（共用判据单点）。
- 伴随修正：`test_ops.py` `mock.patch.object(ops, "_guarded_set")` → `ops.write._guarded_set`（Python 名字绑定语义——patch 须打查找位置）。

**验证**：回归 248/248 全绿；test_ops 直跑 57 OK；nimport PASS；py_compile ✓。

**核销/留项**：
- S3-L1 的「api/prompts.py 分派表分家」→ **核销**：S3-P7② 重构后已收口为 BLOCK_SPEC/PROMPT_SPEC（~150 行），无拆分必要。
- structure ~438 行（超 ~400 软线）→ 再切 `scenes`（create/move/duplicate/delete/renumber_scene + _scene_payload，~110 行）随刀2 顺手。
- 提交：刀1a `e01fc40`、刀1b 见下。

**批1 刀序（依序）**：✅刀1 拆包双子 → 刀2：S1-L4 db 边界（paths.py/open_ro/rw）+ structure 再切 → 刀3：S1-L2+S3-L2 api 收口 → 刀4：S1-L3 路由安全网 + S4-L2 dump_db 归位 + S4-L4 咬合力 → 刀5：S2 三连（key 迁移 / scene_ctx / canonicalizer）→ 刀6：S3-L3 ai_out + S3-L4 任务簿 + S4-L1余 / S4-L3余。

## 批1 · 刀2：db 边界重构（S1-L4）✅

**改动**：
- NEW `core/paths.py`：ROOT / DB_PATH / 快照目录单点——db 与 ops 共同依赖。
- NEW `core/snapshot.py`：快照族迁出（ensure_daily_snapshot / _prune_snapshots / _snapshot_date / _snapshot_lock / _snapshot_done / SNAPSHOT_RETAIN_DAYS）——只依赖 paths，**断掉 db↔ops 旧循环**（延迟导入删除）。
- `db.connect(rw=…)` → **`open_ro()` / `open_rw()` 显式入口**：库缺失统一 FileNotFoundError（双入口同型）；写前快照落于写边界（open_rw 语义化）；db 退回纯连接器（ROOT/DB_PATH 兼容保留）。
- ops 归位：`kv_set`→write、`lock_scene`→scenes（db.DB_PATH→paths）；ops/snapshot.py 除名。
- **structure 再切 scenes**（438 → 335 / 118，双达标 ≤400；刀1 留项销账）。
- 调用面全迁：生产 13 文件（api×6 / core×4 / scripts×3）+ 测试 patch 面 **双入口同拦**（helper 经 patch.multiple，防迁移漏网）。

**验证**：248/248 · nimport PASS · py_compile 全 ✓ · 烟测（缺库双入口同型 / 真库双入口 / 临时库快照相对落盘 / ops 兼容面 / core.snapshot 无 db 依赖 / 真服务 :8094 热重载后健康）。

**账**：`e3126c1`（22 文件 +274/−234）。刀2 完。

## 批1 · 刀3a：api 连接信封与错误收口（S1-L2 + S3-L2 前半）✅

**改动**：
- `core/db.py`：+`conn_ro()` / `conn_rw()`（contextmanager，with 版连接——出 with 自动 close；与 open_ro/open_rw 工厂双层）。
- `api/_guard.py`：+`err(msg, code)`（错误信封单点）/ `read(fn)`（读信封）/ `write(fn)`（写信封：open_rw + ValueError→400）/ `scene_or_404(con, no)`（场装载 + 404 双判单点）；`run_actions` 内联样板收编走 write。
- `api/handlers.py`：14 handler 全收编——13 处 try/finally 样板除名、31 处自拼信封→err()、`isinstance(x, int)`→`fields.is_id`（判据单点）、scene/renumber 的 404 三连归一（文案统一长版「—先跑迁移脚本」）。
- `api/audit.py`：8 函数全收编；`body = body or {}` ×3 除名（app.py 边界归一契约 S1-P4② 成立）。
- **核销**：S3-L2 的「qint/body_int/_req_str」——守卫单点已归 `api/params.py`（as_int/req_int/req_str，S3 修复批已做）→ 复核通过、无新对象。

**验证**：248/248 · nimport PASS · py_compile ✓ · 沙盘 :8097 真机 **11 例**——update 坏体 400 / batch 逐项降级语义实测（单项错误进 results[].error，非 500）/ 写路径真实落库 + 复查 / scene·renumber 404 文案 / issue 坏 action 400 / lock 守卫 400 / read·write 信封端到端 / 正常 batch 200。

**账**：`de7461c`。**刀3b 余**：ai / draft / recipes / export / prompts 五文件收编（下刀）。

## 批1 · 刀3b：api 收口余部（S1/S3-L2 后半）✅ —— 刀3 全收录

**改动**（ai / draft / recipes / export / prompts 五文件全收编）：
- 连接样板除名：settings_get / settings_set / apply_op / blocks / export_get → guard.read/write 信封；test / preview 短段 → `with db.conn_ro() as con`。
- 信封：自拼 `{"error": …}, 4xx` → `guard.err()`；AiError（require_key）/ RecipeError / SceneNotFound → err 转译点位统一。
- export_get：SceneNotFound 404 收进读闭包；双文件名 `_disp` 不动。
- prompts：`_req_int` 错误侧、`_block_precheck` 的 pin/update 守卫 err 化（分发表走 run_actions 不动）。
- **核销**：S3-L2「映射单表（OUTER_FIELDS · P7⑤）/ 分发表（run_actions · P7②）/ name 守卫单点（req_str · P7①）」复查均已在位。

**验证**：248/248 · py_compile ✓ · 沙盘 13 例——settings get/set 真写+回读、recipes 坏名 400、export 200 双文件名头 + 坏 fmt 400 + 坏场 404、blocks 200 + 坏 action 400、prompt/draft/preview 守卫 400、apply 坏 job 400。

**账**：`9af0faf`。**刀3（api 收口）全录**：刀3a（handlers/audit）+ 刀3b（余五文件）。

## 批1 · 刀4：路由安全网与前后端对账（S1-L3）✅

**改动**（NEW `server/tests/test_routes.py`，12 例）：
- ① 前后端对账：自动提取 `web/js/api.js` 的 /api/ 路径（**34 个 unique**——blocks / audit/rules / ai/settings 三对 GET/POST 同路径），逐条服务端命中；**反向**：38 条路由覆盖检查（唯一白名单 = /api/health）。
- ② 逐条契约：38 条（GET 15 / POST 23）样例路径「唯一命中 + handler 身份」断言（顺序无关）。
- ③ 分发形状（假 self 直调 Handler.do_GET/do_POST，零 socket）：未命中 404 / bad json 400 / 非 dict 400 / 413 / 命中链路（自造 route）。
- 提取防回归：路径计数断言 + 动态段样例（scenes→s010、prompt→set_text、/renumber 后缀）。

**咬合验证**：破坏注入（/api/lock → lockX）→ 3 例失败（对账×2 + 契约）；恢复 → 全绿。

**验证**：**260/260**（248 + 12 新例）· py_compile ✓。

**账**：`46ef520`。**刀4 完**（S1-L3 全条结）。

## 批1 · 刀5：S4 余项包（L2 backup 归位 + A1 虚拟列驱动 + B9 快照直测）✅

**改动**：
- **NEW `server/core/backup.py`**（S4-L2）：`dump_db`（含脱敏 B1）/ `prune_exports`（C3）/ `dump_to`（open_ro → dump → 原子写 → 剪枝；毫秒后缀 C2）自 scripts 迁入——**写盘点集中 core**。
- `scripts/export_json.py` → **CLI 壳**（参数/退出码；能力全在 backup）——importlib 手装除名。
- **S4-A1 虚拟列驱动**：`fields.py` prompt 加 `"virtual": True`；`export.py` EXPORT_COLS 判据 `!= "prompt"` → `not c.get("virtual")`；`ops/write.py` skip_types 从 virtual 派生（不再手抄类型名）。
- **NEW `tests/test_snapshot.py`**（S4-L4·B9）5 例：`_snapshot_date`（合法/坏名/坏日期）/ `_prune_snapshots`（40 份→留当天+30 天=31，非快照文件不动）/ `ensure_daily_snapshot` 幂等。
- `test_export_json.py`：backup 化 + `dump_to` 落盘剪枝直测（tmp 库，不触真库）。

**咬合实测**：撤 dump_db 脱敏 → `test_mask_keys_redacts_settings` 咬住（1 failed）→ 恢复全绿。

**验证**：**266/266**（260+6）· py_compile ✓ · nimport PASS · **CLI 端到端**（真库只读导出 520.6 KB，12 表行数打印）· 真服务 :8094 热重载健康（export 200 + 双文件名头）。

**账**：`9fd7a30`。**S4 清项**：L1 ✅(P8) / L2 ✅ / L3 ✅(S4修复轮+本刀A1) / L4 ✅(本刀B9 + 前轮 A5/A7/B12)。

## 批1 · 刀6：规则注册表 key 单源迁移（S2-L1）✅

**改动**：
- **迁移升级**（`_ensure_key_column`）：补列 + **重复 key 收敛**（保最小 id；挂靠问题重指保留行）+ **UNIQUE 索引**（老库防线）。
- **schema.sql**：新库直带 `idx_audit_rules_key`（新库防线；NULL 行豁免）。
- **seed 按 key 查重**（title 认领仅存迁移链一处：老行无 key → 认领回填）；reset 段经 `_TITLE_KEY` 映射重映。
- **运行链全 key 化**：`_RULE_BY_KEY` 单点（`_params` / `_check_params` / `issues_state` / `rules_state`）；`_rule_key` 删 title 回退；`_RULE_BY_TITLE` 死代码除名（grep 归零）。
- **顺修**：`_TITLE_KEY` 未定义潜在 NameError（reset 链中老行失联场景——原代码短路掩盖）。

**验证**：**270/270**（266+4）· py_compile ✓ · **真库迁移**（seed 幂等「新增 0」、`idx_audit_rules_key` 在位、10 行 key 全在）· 真服务热重载 + `/api/audit/rules` key 下发正常 · **负向咬合**（撤迁移建索引行 → `test_migration_dedupes_and_remaps` 咬住；schema 首道防线独立成立已实测）。

**账**：`6cdb9e5`。**S2 进度**：L1 ✅｜L2（scene_ctx）· L3（canonicalizer）待开。

## 批1 · 刀7：镜号/节拍号 canonicalizer（S2-L3）✅ —— S2 清项全结

**改动**（`core/audit.py` 3 处换算法）：
- `_norm_keys`（枚举猜测表：3 变体 × 7 连 replace × 5 分隔符族）**退役** → **`_canon_no`**：全角→半角（**数字+字母**）、剥空白、数字去前导零（int 语义）、子号统一小写；不可解析 → `None`。
- `_index_by_no`：**双层结构**——精确层（原样键先注册）+ 规范层（canon 键，同键冲突保留先注册者并告警）。
- `_lookup`：原样精确 → canon 规范；**S2-B2 语义保持**（同数异写精确引用各归各行）。
- seam 引用（`_resolve_ref`）自动获得**子号大小写 / 全角**归一能力（旧算法落空即静默丢弃——B1 站点 2 的根因面收敛）。

**实测咬合（换算法价值实证）**：矩阵测试**首跑咬出**「全角字母（１７ａ）未映射」bug → 补全角字母表 → 全绿。

**验证**：**274/274**（270+4）· py_compile ✓ · 真服务热重载 + audit 端点健康。

**L2 核销论证**（scene_ctx 单点）：① 装载单点 `db.scene_ctx`（含 cols 投影）已全接驳（`rewrite.py:43` / `handlers.py:48` / audit）；② `shots_by_beat` 分桶单点（:102 建，五用点全走）；③ W10（`_REF_PREFIX_RE` 一次 sub + `SEAM_SEPS` 常量）已落；④ 索引派生（norm 语义）留 audit 域为**有意分层**（db 不知编号语义——避免反向依赖）——**立项四项全达标**。

**账**：`4f2c6a1`。**S2 三连全结**：L1 ✅ / L2 ✅ / L3 ✅。

## 批1 · 刀8：模型输出解析单点 `core/ai_out.py`（S3-L3）✅

**改动**（三域 rewrite/draft/audit 同迁）：
- **NEW `core/ai_out.py`**：`extract_json(text, on_fail="raise"|"empty")`（**口径显式化**——audit=raise / rewrite·draft=empty）、`fail_note`（**留痕单点**：错误+原文长度+首 200 字）、`parse_items`（自 rewrite 迁）、`norm_option`（自 draft 迁）、`strip_fence`（自 draft 迁）、`clip`（截断单点，audit 首用）；`ITEM_TEXT_MAX` 随迁。
- `ai.py` 的 `extract_json` **家迁**（原「自 audit 上收」版）；`rewrite.parse_items` / `draft._norm_option` / `draft._strip_fence` **全部退役**，调用方直连 ai_out。
- 旧家残留 grep 归零；三口径（静默/抛错/丢弃）从「没一处声明」→ **单点显式参数**。

**验证**：**283/283**（274+9）· py_compile ✓ · nimport PASS · **负向咬合**（撤 `empty` 分支 → 2 例咬住）· 真服务热重载（health / ai/settings / audit/rules 全 200）。

**账**：`5887ab4`。**S3 进度**：L1 ✅(批1刀1b)｜L2 ✅(刀3)｜L3 ✅(本刀)｜L4（任务簿）待开。

## 批1 · 刀9：任务簿契约与生命周期（S3-L4）✅ —— 批1 收官

**改动**：
- **start_job 模板收 audit**（三域启动序列全收口）：`key` 参数（audit 传 scene_id）+ build 锁内读规则（临界区语义保）+ `started_at`/`joined` 单点。
- **deadline（任务级超时）**：缺省 900s（epoch 浮点）；`get` 惰性看门（到点置「任务超时」终态 + 归还闸门）；`_finish` 晚到**不覆盖终态**；`_release_gate` 单点（_finish/看门共用）。
- **协作式取消**：`cancel`/`_cancelled` 原语 + **audit 逐规则检查点** + **draft 段间检查点** + **`POST /api/ai/job/cancel`**（39 路由，domain ∈ rewrite/draft/audit）。
- 核销：S2-A04（keep=20 到位 / `_light` 全覆写）· S1-A14（`_evict_keys` 注册序）——均已在位；命名统一（chat/ai_chat）**挂账**（56 处纯名义改动，与机制无关）。

**实测**：**288/288 × 8 连跑**（烧掉两处 flake：`round()` 抹秒→浮点保留；prune mtime 同秒→(mtime,name) 确定序）· 沙盘端到端（audit/run 收模板快照 deadline/joined/started_at ✓、cancel 双向 ✓、**咬出并修复 `draft.JOBS` 跨层引用 bug**）· nimport PASS。

**账**：`a6420c9`。

**批1 收官**：S 系 15 条全录——S1（L1/L2/L3/L4）· S2（L1/L2/L3）· S3（L1/L2/L3/L4）· S4（L1/L2/L3/L4）。

## 批2 · 刀1：CSS token 层（F5-L1）✅

**改动**（web/css/app.css + 6 JS：settings/auditset/formkit/float/scriptdrawer/draft）：
- **:root 立刻度**：z 8 档（--z-sticky/topbar/float/drawer/panel/menu/toast/modal）+ 圆角 5 档（--r-1..5）+ 字号 5 档（--fs-xs..xl）+ 语义色（--llm/--err2/--star/--dim3/--field-bg）。
- **z 回填 22 处**（浮层域全收；值微调：40/55/57→42、47/48→46——同屏层序核实无冲突；撞号组归同 token）。
- **圆角回填 54 处**（4/6/8/10/12→var）；**字号回填 119 处**（10/11/12/13/15→var；·5 台阶 44 处保留为密排专例）。
- **色**：#fffdf6×10→--field-bg；#fffefa/#fffef9→--card；#6b5a8e→--llm；#b3402f/#c0392b→--err2；#c99a2e→--star；#b3aa97→--dim3；#bc3f2c→--accent。
- **cmdk 圆角 12→10**（浮卡统一档——立项拍板项执行）。
- **W2 改名**：as-* 18 类→form-*、sc-* 10 类→pane-*；全库遗留 0。
- **P3-B3 复核**：var(--ink) 已于批1 落地（:863 注释在案）。

**实测**：沙盘 :8097 真机目检 5 界面（主界面/场景 s010 68 行/设置卡 form-*18/审计问题/审计设置 form-*83/台本抽屉 pane-*57）——渲染与改前一致、改名零遗留；288/288 回归兜底。

**账**：`660f77f`。

## 批2 · 刀2：表格骨架通用件（F1-L1）✅

**改动**（NEW `web/js/grid.js` + table.js + film.js）：
- `buildGrid(entries)`（colgroup 构建 + sum 单点）· `syncMinWidth(t)`（重算）· `applyColWidthTo(t, cols, key, w)`（改宽+重算）三件。
- table.js：`applyColWidth` 收编（→ applyColWidthTo）；`buildTable` 建 colgroup 收编（widths 决策留调用方，sum 由 buildGrid 返回）。
- film.js：全片页自建 colgroup 收编（COLS 映射 entries）。
- 口径统一：build 侧 `sum += parseFloat(c.style.width) || f.w` 与改宽侧 `|| 0` 的微妙分叉消除。

**实测**：沙盘 :8097 真机——场景表 colgroup 7 列/minWidth 766 ✓；全片表同值 ✓（766=76+120+76+170+150+64+110）；列宽拖拽 +60px：minWidth 1484→1544、col[4] 110→170（增量精确）✓。

**账**：`01a0204`。

## 批2 · 刀3：全片页列由字段字典派生（F1-L3 前半）✅

**改动**（film.js）：
- `COLS` 手写数组 → `colsOf()`：`fieldsOf('scenes')` in_table 序派生 + 派生列插位规则（「弧线」紧随「价值」、「规模」置尾）。
- arc/size 保留为**派生列声明**（计算列，值由渲染特判；插位规则注释在案）。
- 不改行为：自检断言「派生序 == 老 COLS 7 列」通过；表头 label 随字典（场号/场景名/场景价值…，与场级页字面一致）。

**实测**：沙盘 :8097 真机——全片表 7 列宽 `76/120/76/170/150/64/110`、minWidth 766、10 行，渲染与改前一致 ✓。

**账**：`1f190bd`。

## 批2 · 刀4：字段能力位跨端单源（F2-L2）✅ 核销（已在位）

**核销证据**（grep 全仓复核）：
- **batch**：`selbar.js:35` `fieldsOf(dom.table).filter((f) => f.batch)`——「F2-W14：服务端 batch 能力位派生，不再前端手抄」已在位；三域（shots/beats/scenes）DOMAINS 单点。
- **multiline**：`table.js:13-15` `isMultiline` 走字典（「F1-P5：白名单收进 fields.py 单源」已在位）；`edit.js` 的 `cfg.multiline` 由调用方传字典派生值。
- **in_table**：table 列（tableColumns 已走 fieldsOf）/ film 列（刀3 刚落）/ selbar 清单——全部字典派生。
- **editor**：`edit.js editorHandleAt`/`cam-editor`（相机复合控件）/`hbedit.js editor*`（热盒编辑）= 编辑器的**实现**，非「字段能力位」消费点——无手抄字段名面，无需收编。
- **AI 字段**：`AI_FIELDS` 随 meta 下发（`ai_fields`），前端消费单点。

**结论**：F2-L2 本体（能力位 + meta 下发 + 前端三张清单派生）已被前批完整落地，本刀为核销刀（零代码改动）。台账留痕。

**账**：无代码笔（核销记录随本章节入册）。

## 批2 · 刀5：组级初稿卡 seq → cardLife（F4-L2 前半）✅

**改动**（draft.js，8+/8−）：
- `openPromptDraft` 自造的 5 处 seq 口令（`let seq`/`stop`/`++seq`/`pd!==self||seq!==my`/catch 判定）→ **cardLife 单点**。
- 语义等价映射：换轮作废=新轮 `life=cardLife()` + `close` 旧 life；卡替换/关闭= `my.alive()`；启动失败态= `my.alive()` 守卫。
- 生命周期第二套（seq+hidden）拆解第一步；hidden 存活标志仍留 `pollScene`（场次草稿卡，下刀续收）。

**实测**（沙盘 :8097 真机）：提示词编辑面 → ✦ 初稿 → 卡开（flex）→ 失败文案（沙盘无 key，走 job.error 分支）→ ✕ 关闭 ✓ → 重开 ✓ → 再关 ✓（cardLife 全周期无误）。

**账**：`9532f75`。

## 批2 · 刀6：摄影机旧格式清洗 + 单源校验（F1-L4 + F2-L4 并刀）✅

**改动**（5 文件，+190/−16）：
- **NEW `scripts/migrate_cam_embed.py`**：一次性迁移——串内嵌焦段/景深剥净（值以「显示为准」＝串内嵌值定稿）；干跑默认 / `--apply` 真跑（VACUUM INTO 热备 → 单事务 → `source='system'` 痕迹 → 幂等自检）。
- **`server/core/ops/write.py`**：写路径单源校验——`shots.shot_size` 禁内嵌 `\d+mm`/`·(浅|中|深)`（update/batch 同源）。
- **`web/js/edit.js`**：删 commitCam 的 dof 补写垫片（2 行）。
- **`web/js/table.js`**：lensSave 简化（改焦段只写 focal，不再重写串）+ import 去 parseCam。
- **`server/tests/test_ops.py`**：+TestCamEmbedGuard（4 例）→ **292/292**。

**数据执行**：
- 真库扫描 31 条内嵌（#2 已被用户手动编辑归一，率先达标）；
- 迁移写库 31 条、**0 残留**；痕迹 33 条 `system`；
- 热备：`data/snapshots/migrations/studio-cam-migrate-20260924-232744.db`。

**实测**：
- 真服务拒内嵌：update/batch 全拒（文案「摄影机值不能内嵌焦段/景深…」）、零写入复核 ✓；
- 沙盘真机：显示三行零变化（#02/#13/#22 = 50mm/50mm/35mm）✓；批量设值 2 格「中景 ★★★」（改 1 处＝正确跳过同值）；编辑摄影机表单（槽位→commit→focal/dof 正确保留）✓；
- 页面对照截图（#02「近景 ★★★★ / 50mm」）✓。

**咬合**：
1. 首跑 `--apply` 炸出 `history.source` CHECK 约束（`('manual','ai','system')`）——`'migrate'` 被拒 → 修正 `'system'`（事务已回滚、零半写）。
2. 迁移前发现 #2 有用户实时编辑痕迹（23:16 焦段三连改）——恰好实证「编辑时归一垫片」工作形态 + 「显示为准」口径与用户选择一致（50mm）。

**账**：`8d063a1`。

## 批2 · 刀7：浮层定位与生命周期统一层（F2-L3）✅

**改动**（7 文件，+53/−39）：
- **ui.js**：新增 `stableRepaint(draw)`——稳定帧复绘单点（M5 批1·N2 机制自 selection 上提；「首绘 + 下一帧补绘」house 规则落点）。
- **selection.js**：`queueStabilize` 迁移至 `stableRepaint`（等价）。
- **edit.js**：presets 定位补绘（`doFlip` 首绘 + rAF 补绘；`classList.toggle('above')` 对称化——首绘判错时补绘可纠）；头注释同步 F1-L4 陈迹。
- **menu.js**：菜单定位抽 `doPos` + 补绘（打开瞬间锚点 rect 可能吃重排前值）。
- **drawer.js**：点外过滤链收编——`onDoc` 内建「浮卡/菜单豁免（`floatPrompt` 开则含提示词列）→ 钉住不触发 → onOutside」；各抽屉只写「真点外要做什么」。
- **scriptdrawer.js / hotbox.js**：两处逐字同构的手抄链退役（删 menuEl 冗余检查——`.menu` 已在 isFloatTarget 名单）；import 收窄。

**实测**（沙盘 :8097 真机）：
- 菜单定位：机位菜单开 → `gapBelow=4`（placeFlip 默认值）+ 左对齐 ✓；
- presets 翻上：表底行开运镜编辑面 → `above=true`、strip 稳定于格上方 ✓（1s 无抖动）；
- 抽屉点外链三连：真点外→关 ✓ / 钉住→点外不关 ✓ / 点选区条（名单成员）→不关 ✓；
- 选区框多次点击正常（迁移无回归）✓；292/292 全绿 + nimport lint PASS。

**咬合**：
1. **offsetParent 陷阱**：fixed 定位浮层（.menu）的 `offsetParent === null`——用它判「可见」恒假（本轮查询工具自咬、误报『菜单没开』两次，靠截图目检纠正）。查浮层可见性一律 rect/computedStyle。
2. Esc 层级复核（不动）：菜单 capture+stopPropagation → 编辑器/抽屉收不到；drawer `bindDrawerEsc` 阶梯（菜单自管→别家抽屉→输入中→顶层→onEsc）已在位。
3. 滚轮护栏覆盖面复核：`.drawer` 已挂 `.float-card` → 天然入护栏名单 ✓（F1-1 同源项闭合）。

**账**：`377e0e9`。

## 批2 · 刀8：抽屉↔块库卡「几何契约」层（F3-L1）✅

**改动**（3 文件，+69/−57）：
- **drawer.js**：新增契约三件——`geom()`（open/pinned/dock/entering + frame 四元组，offset* 免疫动画期 rect 漂）、`onGeom(cb)`（rAF 合并订阅，返回退订）、`setUnder(mode)`（贴紧类写侧收编+镜像抑制）；`st.entering` 状态单点；emitGeom 挂点＝freeRect 尾（单点覆盖拖拽/缩放/浮动重夹）、applyDockGeom 尾、close、playIn 结束（flashClass onEnd）、bounce 接手。
- **ui.js**：`flashClass` 支持 `opts.onEnd`（可选结束回调）。
- **blockcard.js**：`edgeValue/frameRect`（5 处 parseFloat 反解）与全属性观察者（`obs`）、`applyUnder/underApplied`、`scheduleLayout` 全退役；layout 改读 `dr.geom()`；贴紧类改调 `dr.setUnder()`；入场浮现判定改读 `g.entering`（不再嗅探 `.enter` 类）；订阅改 `dr.onGeom(() => layout())`（emitGeom 已按帧合并）。
- 取舍记录：立项建议「bc-under 由抽屉在 applyDock 内自维护」—完整自维护不可行（卡的可见性/开合只有卡知道），以「**写侧收编**」落实：blockcard 不再直接操作抽屉类，全部经 `setUnder`。

## 批2 · 刀9：任务卡编排单点 runTaskCard（F4-L2 余部）✅

**改动**（3 文件，+118/−91）：
- **aicard.js**：新增 `runTaskCard(spec)`——guard（sceneId：假值即 onFail('找不到当前场次')，真值经 start(sid) 相传）→ 启动请求 → joined toast → 轮询（tolerant 口径内收）→ 终态分派（abort 静默 / done → onDone / gone·timeout·error → onFail(failText)）；`startPrefix` 保草稿族「启动失败：」口径；`onFail(msg, launched)` 区分启动失败/终态失败（草稿卡「启动失败留表单、终态失败回表单」原口径保真）。
- **aiwrite.js**：单格卡与批量卡 start 收编（原 ~21 行×2 → ~15 行 spec×2）；`setLoadingErr` 打洞句随编排归位（onFail 内联 renderFail）；import 清理（pollJob/failText/joinedToast 退役）。
- **draft.js**：场次草稿卡**sceneLife 轮次令牌**（收起/换轮即停——hidden+`D.jobId===jid` 双存活判据退役；且修复「收起后 sleep 窗口内重开，旧轮复活的边角」）；startRun+pollScene 收编（pollScene 整函数删除）；组级初稿 run 收编。
- 四卡归一到 `taskShell + cardLife + runTaskCard` 三层。

**真机验证 5/5**（沙盘 :8097，副本库）：单格卡失败态（右键→两级菜单→改写）✓；批量卡失败态（选区 2 格）✓；组级初稿卡（「生成失败：未配置 API Key」→ ✕ 关）✓；场次草稿卡全流程（s100 新场→表单→真键盘输入 56 字→生成→toast「生成失败：…」+回表单保输入→取消收起（hidden 复用）→重开（lastScript 回填））✓；四卡均走新编排（失败路径行为等价）。
**教训**：CDP 点击的 rect 提取与事件派发**必须同一原子窗口**（跨调用复用旧坐标——菜单重开/重定位后位置已变，会点空或点到旧菜单的残留项）；顶栏 chip 溢出视口时先 `scrollIntoView` 再取 rect。

## 批2 · 刀10：块序语义单源（F3-L2 (a)，用户拍板：服务端统一）✅

**改动**（6 文件，+188/−18）：
- **服务端 blocks.py**：`_place_block` 段感知——置顶段恒在普通段前：① 非置顶块不进置顶段（夹到普通段首）；② 置顶块被放到普通段＝自动取消置顶；③ b 以最新行参与判定（同一事务内 pinned 刚改过）；`block_update` 置顶拨位——单独置顶＝拨到置顶段尾（显示即刻到位），带 position/category_id 的复合更新（撤销重放形状）由位置分支定夺、不二次拨位；`block_move` 段守卫（非置顶不跨进置顶段、置顶不落进普通段）。
- **前端**：`sortedBlocks` 删 pinned 比较键（置顶前置改由拨位固化进 position，显示序 ≡ position 序）；`moveBlockTo`/`togglePin` 撤销负载带 pinned 快照与旧位置（复合负载防重放二次拨位）；`bc-list.js` 拖放全放开——「置顶块退出拖序」B1/B7 四处特判（不可拖起/不作落点/dragstart 早退/off 类）整段退役；`app.css` 删 `.bco-grip.off`。
- **NEW `scripts/migrate_block_order.py`**：既有 pinned 块归位迁移（干跑/--apply、热备、幂等）；真库干跑＝无需迁移（7 块 0 置顶）。

**设计拍板（拍板口径只定「后端统一」，以下为本刀实现口径）**：pin＝拨到置顶段尾（后置顶排后）；unpin＝松钉留守（零跳位——修掉原「凭空跳位」）；拖非置顶到置顶区＝夹取（进不去）；拖置顶出段＝自动取消置顶（Ctrl+Z 可撤，复合负载精确还原）。

**真机验证 6/6**（沙盘 :8097 副本库，同步复核真值）：pin 45 跳前（库 45/0/1）✓；拖 45 到 42 下＝自动 unpin 落位（库 42/0/0 + 45/1/0）✓；Ctrl+Z 复合还原（★回+回位，库 45/0/1）✓；非置顶 42 拖到置顶 45 上方＝夹取不顶掉 ✓；unpin 留守（45 位置不动仅清 ★）✓；294/294（+2 例）✓。

**教训**：对浮卡内元素做 CDP 点击前先断言「卡可见」——卡已 hidden 时元素 rect 全 0，点击会落到 (0,0) 附近＝点外，误收浮层（本轮 ⑤ 首次执行即踩、重开后复测通过）。

## 批2 · 刀11：块库轻量 DnD 与手势单点（F3-L3）✅

**改动**（3 文件，+58/−66）：
- **ui.js `trackDrag` 增强**：收尾幂等 + `blur`/`pointercancel` 兜底（F1-P4 列宽用的同机制并入公共件——焦点被抢/指针取消也收工）；drawer×2、blockcard 白拿增强。
- **table.js**：`startColResize` 手写 mousemove/mouseup/blur/pointercancel 四件套退役 → `trackDrag(onMove, finish)`（F1-P4 交付物收进公共件）。
- **bc-list.js**：`bindDropTarget(el, spec)` 骨架单点（原 `bindRowDrop`/`bindRowsDrop`/`bindHeadDrop` 三份手写 dragover/dragleave/drop 骨架收编）；`BCO_DROP` 落点词汇表单点（清理不再写死类名）。

**已在前批完成、本刀核销**：`dropIndex` 单点（blocks.js）、F2-B3（selection `e.buttons` 自愈）、F1-P4（列宽兜底）、trackDrag 三件套（F3-W27①）。**取舍**：drag.js 记账版（多元素组高亮）与 bc-list 单元素版标记账户形态差异正当（语义不同、同为 O(1)），不做强行合并——「统一」落在词汇表 + 骨架 + 手势件三处。

**真机验证 6/6**（沙盘 :8097 副本库）：行拖（43 跨类落 45 后，库值 category_id=4/pos2 ✓）；组头拖（43 回骨架 ✓）；空白区拖（43→未分类→拖回骨架，全链净回 ✓）；列宽 CDP 真鼠标拖（+36px、拖中 `.col-resizing=true`、拖后清 ✓）；列宽双击恢复默认（344→300 ✓）；台本抽屉头拖（dx −90/dy +54 精确 ✓）。

**测试技术备注**：grip 窄条（7px）CDP 命中曾失败一次——重试成功；宽/双击/拖三路径均通过。教训同刀10：操作前先断言目标可见（rect 有效）。

**294/294** 全绿（服务端无涉改）。

## 批2 · 刀12：块库「写→刷新→筛选」链收口（F3-L4）✅

**改动**（6 文件，+135/−105）：
- **服务端写响应信封**：`_guard.run_actions` 加 `post(con, out)` 钩子（同连接内补发字段）；`blocks_op` 写响应附全量 `state`（blocks+categories）——写成功即事实成立，前端不再跟一发全库 GET。
- **`blocks.js` 就地套用 + 防倒退**：`cacheGen` 写世代；`blockOp` 见 `res.state` 即套用（`cacheGen+1`，在途旧快照落地时 gen 对不上即弃）；无 state 时保留 force 重取回退（向后兼容）。写路径 RT 从 2 次降为 1 次，且快照必然晚于写事务（B4 语义天然满足）。
- **`blockcard.js` 状态收拢（W16/W11）**：13 个模块级散变量（dr/root/innerEl/listRefresh/insertCb/active/upMode/pendShow/tAppear/mem/pinnedOnly/query/loadErr）→ 单一 `card = {...}`；ctx 折叠状态与落盘捆绑为 `isFolded`/`toggleFold`（消费者不再直持 Set）；删幽灵键 `ctx.list`（全仓 0 消费者）。机械替换带三重闸门：词界一遍过、对象键名位零改动（前后计数一致断言）、双前缀哨兵。
- **`bc-list.js`**：三处 `ctx.folded/foldSave` 收编为 `isFolded/toggleFold`。
- **测试**：+1 例「块写响应含 state」（mock conn_rw 走内存库）；**295/295**。

**已在前批完成、本刀核销**：搜索防抖（W18）、`opts {mode, draggable}` 参数面（W11）、色板/catColorOf 上收（W17）、foldAll 单改 folded（W19①）、空白判定结构化（W13）、PLACEHOLDERS 键表单点（W7）。**不做**：blockMatch haystack 预建（块规模无感；防抖已消主热点）、订阅者变更集局部重绘（全量重建在块库规模无感——留待规模驱动）。

**真机 8 项**（沙盘 :8097 副本库）：① curl create/delete 响应含 state（8→7 块）；② **fetch 探针：pin 写 POST 1 次、GET 0 次**（旧行为 2 次）；③ 库真值复核 43 pinned=1；④ 折叠收起/展开（toggleFold）；⑤ 搜索平铺/清空（防抖生效、零写请求）；⑥ 同段拖放两次（45↔42 往返）；⑦ 卡收起（bc-clps）/展开；⑧ 库全量净回（42/0、43/0/0、45/1、7 块）+ 截图目检。

**弯路**：机械替换初版吞了对象键名（`card.dr: null`）——node 语法闸咬住 → 回滚 6 文件 → 统一「键名位排除 + 计数一致断言」重跑全绿。教训：**机械替换的键名位必须显式保护并配哨兵断言**。

## 批2 · 刀13：抽屉「会话协议」上收壳（F4-L1）✅

**改动**（4 文件，+76/−50）：
- **壳（drawer.js）**新增会话协议三件：`tryLeave()`（切前静默保存——save() 非 {ok:false} 即放行，失败调用方留在原处）、`softRefresh()`（开着且非编辑态才重绘）；**DRS 名册** + `dr.repaint` 插槽 + **`repaintDrawers(sceneId)` 广播导出**（遍历已建抽屉各走自家 repaint；单家异常不阻塞别家，async rejection 亦捕获）。
- **hotbox**：opts 交 `save/isEditing/render` 三回调；`dr.repaint` 挂载（原 `releaseComposer` 退役）；openPromptDrawer 切镜保存段、commitClose 保存段 → `tryLeave`；`refreshDrawerSoft` → `softRefresh`。
- **scriptdrawer**：同上（原 `scriptsOnRepaint` 退役；`renderSoft` → `softRefresh`；openScriptDrawer/commitClose → `tryLeave`）。
- **scene.js**：`releaseComposer()` + `scriptsOnRepaint()` 两行 → `repaintDrawers(data.scene.id)` 一行。

**范围核销**：W39（Esc 阶梯单点 `bindDrawerEsc` + ACTIVE 栈）与 W40（`addStandardButtons` 标准钮组）已在先前批次落地，本刀核销；两消费者的 12 行排除表与按钮装配均已不在。

**真机 6/6**（沙盘 :8097 副本库）：① 编辑面改字 → 钉住点行切镜 → **POST set_text + 组1 保存**（tryLeave 路径）；② 净回两刀（组1/组2 探针全清，库终查 0）；③ 查看态切场 → 抽屉关（hotbox repaint）；④ Esc 两级：编辑→查看→关（未钉住）；**钉住不关=正确行为**；⑤ 台本钉住跟场（标题「剧本 · s010」→「剧本 · s030」）；⑥ saveBtn 保存链独立验证（POST + toast + 跳镜）。

**弯路（测试技术）**：点「提示词格」切镜的路径被抽屉自身遮挡（`elementFromPoint` = 抽屉 textarea）——正确真机路径＝**钉住后点行的非提示词列**（hotbox 钉住浏览监听）。教训：**抽屉打开时，被其覆盖的表格区域不可作为点击目标；命中诊断用 `elementFromPoint` 先行**。

## 批2 · 刀14：提示词视图单点（F4-L4）✅

**改动**（1 文件，+11/−6）：
- **`refreshPreviewsForGroup(gid)` 行内引用化**：按组内镜（`g.member_shots` → `shot_no` 找镜）定向取 `tr.detail[data-for=id] .prompt-box` 重画——原**全表扫 `.prompt-box` + 逐盒判组**退役。写响应扇出（set_text 后）与组态扇出共用此单点，定向后免全表遍历。

**核销**（先前批次已落）：② `groupsMap` 单点+就地更新（`scene.js promptGroupsMap = groupsById(data)` + `adoptGroups` 就地同步，盒的 `PB_CTX.groups` 与其同对象）；③ `groupOrdinalMap` 随组态失效（置 null 重建）；④ `refreshCover` 差量写类（F4-W4c）；① 提示词格 `updatePromptCell` 行单点（F4-W41）。

**论证不做**：`syncPromptCells`/`refreshAllPreviews` 保全表——组序号=「场内全部组一起数」（byPosition 排名），**任何组态变更（merge/detach/split）都会位移多个组的序号** → 显示「组N」徽标的镜集≈全表，全表是**正确性要求**非浪费；「只重画受影响的镜」的前提是序号域名（如仅显示本组序号）——不采纳。「提示词格并进表侧单点（refreshShotCell 覆盖虚拟列）」与 W41 深修同族：hotbox 已是行单点，表侧收编将引入 table↔hotbox 双向依赖——账记未采纳。

**真机 3 项**（沙盘 :8097 副本库）：① 展开 02 镜 detail → pb 挂载 ✓；② 抽屉改字「存 → 下一镜」→ **02 的 pb 定向更新**（文本含探针）✓ 跳镜正常；③ 净回（探针全清、抽屉关、detail 折回）；库终查 0 残留。**295/295**。

## 批2 · 刀15：编辑与选择层「格/域」契约收口（F2-L1）✅

**侦察结论**：立项三套形状（cfg 15 键 / batchWrite opts 7 键 / 域分派 10 处）绝大部分已在前批落地——cfg：写口单点 `cfg.write || api.update`（W6 两出路已落其一）+ save 分支 + `rollback()` 单点；batchWrite：`{domain:{table,resolve,refresh,noun,place}, write, fail}` 已收且缺省 shots 派生；域分派：`DOMAINS` 表（table/prefix/fbtn/count/apply）+ `batchItems` 服务端 batch 能力位派生（F2-W14）+ 三落值函数各持一份 domain 声明。W6「258 注释漂移」已不复存在（openCamForm 注释含 dbl/refreshSiblings）。

**本刀两小笔**（2 文件 +14/−4）：
1. `edit.js`：`attachEditable` 入口契约校验——`getValue/onLocal/renderCell` 三件必填，缺则构造时抛错。原「openEditor 守卫静默退化 vs save/commitField 裸调抛错」两种硬度统一为「必填+入口即拒」（10 个调用方全合规，零现有行为变化）；cfg 注释补全键表（write/aiOpen/presets/walk 等）。
2. `selbar.js`：`pickField` 前缀解析数据化——原 `'b:'/'s:'` if 链逐域手抄 → 遍历 `DOMAINS` 表按 prefix 匹配（shots 空前缀兜底）。行为等价（b:/s:/裸键三路）。

**论证不做**：cfg 三段式大重构（`{domain,edit,policy}`）——8+ 调用方、字段面稳定、风险>收益，留待新字段压力驱动。

**真机 6 项**（沙盘 :8097）：① 普通格双击开编辑面 + Esc 关（入口校验不炸）✓；② 拖选 2 格 →「设值…」菜单开（34 项）✓；③ 点「节拍名称」→ 按钮变「节拍·节拍名称」+ 输入框「值…（回车套到 1 个节拍）」（b: 域解析 + `selBeatIds` 计数）✓；④ 重开菜单 `.current`=「节拍名称」（pick.field 保持）✓；⑤ 回切「运镜」→ 按钮「运镜」✓；⑥ 点「场景价值」→ 按钮「场景·场景价值」+ 输入框「值…（回车套到本场）」（s: 域 `place` 路径）✓；截图目检 + 清选区收尾。**295/295**。

**弯路记录（E2E 新坑）**：选区菜单是**高溢出滚动容器**（34 项 > 视口）——底部项的 `rect` 存在但被容器裁剪，`elementFromPoint` 可正常通过但**视觉/命中被裁剪时点击穿透到下层表格 → 清选区**。先滚 `scrollIntoView({block:'nearest'})` 再**命中验证（elementFromPoint === 项）**再点，方可稳定；且「selbar 的 DOM 存在 ≠ 可见」——验收一律看 `getBoundingClientRect().width > 0`。
