# 12 · 第二波台账（◆立项 35 条）

**口径**：第 2 波按两批推进——批1 S 系 15 条（服务端结构收口）、批2 F 系 20 条（前端结构）。逐刀施工、每刀全量回归、每刀入账。
**基线**：AUDIT-M8 修复侧全清（248/248）之上；「刀」= 一个自洽的施工单元，刀内可含多条立项。

## 批1 · 刀1：拆包双子（S1-L1 + S3-L1）✅

**改动**：
- `server/core/ops.py`（854 行）→ `core/ops/` 包：write ~170 / snapshot ~101 / structure ~438 / numbering ~58 / restore ~73 + `__init__` 全量重导出（63 名）；`from core import ops` 调用面零改动。
- `server/core/prompts.py`（486 行）→ `core/prompts/` 包：groups ~258 / blocks ~164 / text ~31 + `__init__` 重导出（33 名）。
- 搬迁法：块级切片 + 自动依赖分析（注释/字符串剥离防 docstring 假依赖）+ 环检测；断环归置：prompts 的 `MAX_BLOCK_TEXT`/`MAX_CAT_NAME`/`is_id` → text（共用判据单点）。
- 伴随修正：`test_ops.py` `mock.patch.object(ops, "_guarded_set")` → `ops.write._guarded_set`（Python 名字绑定语义——patch 须打查找位置）。

**验证**：回归 248/248 全绿；test_ops 直跑 57 OK；nimport PASS；py_compile ✓。

**核销/留项**：
- S3-L1 的「api/prompts.py 分派表分家」→ **核销**：S3-P7② 重构后已收口为 BLOCK_SPEC/PROMPT_SPEC（~150 行），无拆分必要。
- structure ~438 行（超 ~400 软线）→ 再切 `scenes`（create/move/duplicate/delete/renumber_scene + _scene_payload，~110 行）随刀2 顺手。
- 提交：刀1a `e01fc40`、刀1b 见下。

**批1 刀序（依序）**：✅刀1 拆包双子 → 刀2：S1-L4 db 边界（paths.py/open_ro/rw）+ structure 再切 → 刀3：S1-L2+S3-L2 api 收口 → 刀4：S1-L3 路由安全网 + S4-L2 dump_db 归位 + S4-L4 咬合力 → 刀5：S2 三连（key 迁移 / scene_ctx / canonicalizer）→ 刀6：S3-L3 ai_out + S3-L4 任务簿 + S4-L1余 / S4-L3余。

## 批1 · 刀2：db 边界重构（S1-L4）✅

**改动**：
- NEW `core/paths.py`：ROOT / DB_PATH / 快照目录单点——db 与 ops 共同依赖。
- NEW `core/snapshot.py`：快照族迁出（ensure_daily_snapshot / _prune_snapshots / _snapshot_date / _snapshot_lock / _snapshot_done / SNAPSHOT_RETAIN_DAYS）——只依赖 paths，**断掉 db↔ops 旧循环**（延迟导入删除）。
- `db.connect(rw=…)` → **`open_ro()` / `open_rw()` 显式入口**：库缺失统一 FileNotFoundError（双入口同型）；写前快照落于写边界（open_rw 语义化）；db 退回纯连接器（ROOT/DB_PATH 兼容保留）。
- ops 归位：`kv_set`→write、`lock_scene`→scenes（db.DB_PATH→paths）；ops/snapshot.py 除名。
- **structure 再切 scenes**（438 → 335 / 118，双达标 ≤400；刀1 留项销账）。
- 调用面全迁：生产 13 文件（api×6 / core×4 / scripts×3）+ 测试 patch 面 **双入口同拦**（helper 经 patch.multiple，防迁移漏网）。

**验证**：248/248 · nimport PASS · py_compile 全 ✓ · 烟测（缺库双入口同型 / 真库双入口 / 临时库快照相对落盘 / ops 兼容面 / core.snapshot 无 db 依赖 / 真服务 :8094 热重载后健康）。

**账**：`e3126c1`（22 文件 +274/−234）。刀2 完。

## 批1 · 刀3a：api 连接信封与错误收口（S1-L2 + S3-L2 前半）✅

**改动**：
- `core/db.py`：+`conn_ro()` / `conn_rw()`（contextmanager，with 版连接——出 with 自动 close；与 open_ro/open_rw 工厂双层）。
- `api/_guard.py`：+`err(msg, code)`（错误信封单点）/ `read(fn)`（读信封）/ `write(fn)`（写信封：open_rw + ValueError→400）/ `scene_or_404(con, no)`（场装载 + 404 双判单点）；`run_actions` 内联样板收编走 write。
- `api/handlers.py`：14 handler 全收编——13 处 try/finally 样板除名、31 处自拼信封→err()、`isinstance(x, int)`→`fields.is_id`（判据单点）、scene/renumber 的 404 三连归一（文案统一长版「—先跑迁移脚本」）。
- `api/audit.py`：8 函数全收编；`body = body or {}` ×3 除名（app.py 边界归一契约 S1-P4② 成立）。
- **核销**：S3-L2 的「qint/body_int/_req_str」——守卫单点已归 `api/params.py`（as_int/req_int/req_str，S3 修复批已做）→ 复核通过、无新对象。

**验证**：248/248 · nimport PASS · py_compile ✓ · 沙盘 :8097 真机 **11 例**——update 坏体 400 / batch 逐项降级语义实测（单项错误进 results[].error，非 500）/ 写路径真实落库 + 复查 / scene·renumber 404 文案 / issue 坏 action 400 / lock 守卫 400 / read·write 信封端到端 / 正常 batch 200。

**账**：`de7461c`。**刀3b 余**：ai / draft / recipes / export / prompts 五文件收编（下刀）。
