# 分镜工作台 · 快捷键速查卡

> 适用：shotlist-studio（:8094）场级页 · 表格操作流。macOS 上 Ctrl 即 Cmd。
> 键位实现单点：`selection.js`（走格/选区）· `edit.js`（编辑面）· `main.js`（全局撤销）。

## 走格与选区（未开编辑时）

| 键 | 作用 |
|:--|:--|
| ↑ ↓ ← → | 走格（移动焦点） |
| Shift + 方向键 | 扩选（矩形扩展） |
| Tab / Shift+Tab | 右移 / 左移一格 |
| Home / End | 跳行首 / 行尾 |
| Ctrl+A | 全选本表（全部可写列） |
| Ctrl+C / Ctrl+X / Ctrl+V | 复制 / 剪切 / 粘贴——粘贴以选区左上为锚点，Excel 的块直接贴入会向右下铺开 |
| Ctrl+D | 向下填充：多行选区＝首行铺满下方；单格＝取上方值填入 |
| Delete / Backspace | 清空选中格 |
| Esc | 取消选区 |

## 打字即编

- 选中格后**直接打字**＝进入编辑（首字替换原值；中文输入法首键同样直进）
- 枚举列（机位/运镜等）打字＝弹出选择菜单

## 编辑面（单元格已打开）

| 键 | 作用 |
|:--|:--|
| Enter | 保存；单行字段保存后自动下移一格（多行字段＝换行） |
| Ctrl+Enter | 多行字段保存 |
| Tab / Shift+Tab | 保存并走格（右 / 左） |
| Esc | 取消（还原原值，不写库） |
| 点空白处 | 自动保存（失焦提交） |

打开方式：**双击**单元格（或 Enter / F2）。
注意：编辑器内的 Ctrl+A / Ctrl+Z 是文本自身的全选 / 撤销——不会触发全局撤销。

## 全局

| 键 | 作用 |
|:--|:--|
| Ctrl+Z | 撤销上一步——编辑 / 拖动 / 整组搬家 / 批量设值 / 清空 / 填充 / 剪切 / 粘贴 / 插入 / 副本 / 删除 / 整理镜号 / 合组拆组 / 场次操作，一步一撤全可逆 |

## 鼠标 · 拖动

| 操作 | 作用 |
|:--|:--|
| 拖拽出格 | 框选；Shift 点选＝扩选 |
| 行首把手拖动 | 行换序 / 跨节拍搬家；**选区跨多行时**拖任意选中行＝整组搬家（组内顺序不变） |
| 节拍头拖动 | 整节拍重排 |
| 场签拖动 | 调整场次顺序 |

## 右键菜单

| 位置 | 项目 |
|:--|:--|
| 单元格 | 复制本格 / 复制整行 / 粘贴 / ✦ AI 改写… / 创建行副本 / 上方插入空行 / 下方插入空行 / 清空本格 / 删除本行 |
| 节拍头 | 创建节拍副本（含 N 镜）/ ✦ AI 改写…（节拍概述）/ 删除节拍 |
| 场签 | 创建场次副本 / 删除场次（含 N 镜） |

## 选区条（多选时表底浮出）

计数 · 设值…（镜头 12 / 节拍 14 / 场景 6 个字段，拾取即套、一步撤销）· 并为一组 / 独立成组 · 复制（TSV）· 删除行（N）· 清空 · ✦ 一句话改写（预览制）· ✕

## 表头与场务

- 表头：点击＝排序（视图级，不改行序）；列宽拖拽（双击复位）；列显隐＝场务 ⋯ → 列设置
- 场务 ⋯：整理镜号 / 锁定本场（留底快照）/ 自动换行 / 列设置 / 痕迹
